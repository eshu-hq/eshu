interface:
  display_name: "Cypher Query Rigor"
  short_description: "Eshu Cypher reads, graph writes, indexes, and NornicDB dialect"
  default_prompt: "Use $cypher-query-rigor to write or review the Cypher reads, graph writes, and indexes for this change."
