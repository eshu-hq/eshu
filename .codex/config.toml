# Codex project-level config. Enable the prompt router and PostToolUse
# doc-keeper hook in .codex/hooks.json for trusted project sessions.
#
# See https://developers.openai.com/codex/hooks for the full spec.

[features]
hooks = true
