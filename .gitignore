# Go build artifacts
go/bin/
go/admin-status
go/api
go/bootstrap-data-plane
go/bootstrap-index
go/collector-git
go/eshu-admin-status
go/eshu-api
go/eshu-bootstrap-data-plane
go/eshu-bootstrap-index
go/eshu-collector-git
go/eshu-ingester
go/eshu-mcp-server
go/eshu-projector
go/eshu-reducer
go/eshu-workflow-coordinator
go/golden-corpus-gate
go/ingester
go/mcp-server
go/eshu
go/projector
go/proof-of-value
go/reducer
*.exe
*.exe~
*.dll
*.dylib
*.test
*.out
*.omc

# Python bytecode (generated by mkdocs and fixture tooling)
__pycache__/
*.py[codz]

# C extensions
*.so

# Distribution / packaging
build/
dist/

# Environments
.env
.env.remote-e2e
.envrc
.venv
env/
venv/

# IDEs and editors
.vscode/
.idea/
*.swp
*.swo

# mkdocs documentation
/site
docs/site/

# Cursor
.cursorignore
.cursorindexingignore

# Repo-local tooling and scratch files. .claude/* (not .claude/) so git still
# descends into the directory and the negations below can re-include the
# project-level hook, settings, and skill shipped with the repo.
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/hooks/**
!.claude/skills/
!.claude/skills/**
.worktrees/
node_modules/
dist/
site-dist/
# Marketing-site browser review artifacts (screenshots + JSON summary) from
# `npm run site:review`. Durable locally, never committed (binary screenshots).
site-review-artifacts/
*.tsbuildinfo
.eslintcache
.mcp.json
.vercel/
mcp.json
*.bak
*.temp
*.tmp
*.log
playground/
todo.md
.eshu-compose-runs/

# macOS system files
.DS_Store
.omc/
.serena/
.superpowers/

# Doc-keeper drift state (regenerated by hook on each tool use)
.eshu-doc-state/

# Planning scratch files generated by local agent tooling.
.planning/
.playwright-cli/

# Console live E2E gate artifacts (issue #3326): screenshots, Playwright trace,
# JSON report, logs, and the local-only env file with the throwaway API key.
# Durable LOCAL proof only; never commit binaries or the local key.
e2e-artifacts/

# Operator-local identity-provider proof manifests and generated summaries.
# Keep Okta tenant URLs, app identifiers, users, groups, tokens, assertions,
# cookies, and aggregate proof packets out of commits unless explicitly moved
# into a reviewed public-safe docs path.
.proof-artifacts/
okta-oidc-proof*.json
okta-oidc-proof*.md
okta-oidc-live-proof*.json
okta-oidc-live-proof*.md
okta-saml-proof*.json
okta-saml-proof*.md
okta-saml-live-proof*.json
okta-saml-live-proof*.md

# Claude Code per-user overrides; .claude/settings.json is committed so the
# project hook works for everyone who clones.
.claude/settings.local.json

# Stray compiled cmd binaries from a bare `go build ./cmd/<name>` (build to a tmp dir instead)
/go/capability-inventory
/go/skillgen

# Per-deployment override for the skillgen generator. The default
# capability set is all-collectors-enabled; contributors can drop a
# capabilities.local.yaml at the skill-fragments/ root to disable a
# subset for their local render. Never commit it.
skill-fragments/capabilities.local.yaml
go/.gocache/
go/.gocache2/
