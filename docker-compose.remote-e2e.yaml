# Remote collector end-to-end proof stack.
#
# This wrapper is the stable operator entrypoint. Service ownership lives in
# the included fragments so remote proof changes stay below the agent line cap.

name: ${ESHU_REMOTE_E2E_PROJECT_NAME:-eshu-remote-e2e}

include:
  - docker-compose.remote-e2e.foundation.yaml
  - docker-compose.remote-e2e.runtime.yaml
  - docker-compose.remote-e2e.seed.yaml
