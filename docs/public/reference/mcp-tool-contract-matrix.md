# MCP Tool Contract Matrix

This matrix is the prompt-readiness contract for the current `ReadOnlyTools`
set. "Prompt-ready" means the tool has a bounded scope contract, deterministic
result order where the handler lists rows, a limit or explicit singleton key,
MCP envelope negotiation through `Accept: application/eshu.envelope+json`, and
a clear drilldown path when more data is available.

## Prompt-Family Audit

Use this table to pick the named tool before falling back to diagnostics-only
Cypher.

| Prompt family from docs | Primary current MCP path | Current status | Remaining tracked work |
| --- | --- | --- | --- |
| Cross-repo service story, onboarding, runbooks | `get_service_story`, `investigate_service` | Story-first path returns bounded service dossiers. | None |
| Symbol discovery and implementation lookup | `find_symbol`, `find_code`, `execute_language_query` | Definition lookup is bounded, paged, source-handle backed, and does not require raw Cypher. | None |
| Broad code-topic and implementation investigation | `investigate_code_topic` | Content-index investigation returns ranked files, symbols, searched terms, coverage, truncation, and follow-up handles. | None |
| Callers, callees, imports, call chains | `trace_route_callers`, `get_code_relationship_story`, `find_function_call_chain`, `investigate_import_dependencies`, `inspect_call_graph_metrics` | Relationship reads resolve ambiguity first, anchor graph traversal by exact route, entity, or repository scope, and expose paging/truncation. | None |
| Dead code and code quality | `find_dead_code`, `find_most_complex_functions`, `inspect_code_quality` | Complexity, long-function, high-argument, refactoring, and dead-code prompts use first-class bounded tools. | None |
| Security hardcoded secrets | `investigate_hardcoded_secrets` | Redacted content-index investigation returns severity, confidence, suppression notes, source handles, paging, and truncation. | None |
| Deployment, GitOps, and resource tracing | `trace_deployment_chain`, `trace_resource_to_code`, `investigate_deployment_config`, story tools | Deployment and resource prompts use story or bounded trace routes. | None |
| Environment comparison | `compare_environments` | Workload/environment comparison returns a bounded story packet with evidence, limitations, coverage, and next calls. | None |
| Runtime and indexing status prompts | `get_hosted_readiness`, `get_operator_control_plane`, `list_dead_letter_work_items`, `get_freshness_causality`, `get_collector_readiness`, `get_hosted_governance_status`, `get_index_status`, `get_semantic_capability_status`, `get_answer_narration_status`, `get_capability_catalog`, `get_surface_inventory`, `list_collectors`, `list_ingesters`, `get_ingester_status`, `list_component_extensions`, `get_component_extension_diagnostics` | Runtime prompts use shipped status tools instead of stale job-status names. Hosted readiness separates process, dependency, queue, collector, shared projection, and query-readback checks; hosted governance reports redacted policy mode, state, readiness booleans, aggregate counts, and reason codes; semantic extraction reports unavailable when no provider is configured, or redacted provider profile rows when hosted profiles are configured; answer narration reports disabled-by-default presentation posture while deterministic answer packets remain canonical. Component extension diagnostics read configured registry state only, return unavailable when no component home is configured, and never treat community index membership as trust. Dead-letter listing is bounded by required `limit` and `timeout_ms`, filterable by failure class/domain/scope/collector/time window, and scoped to granted component scopes. | None |
| Documentation/confluence prompts | story routes, `list_documentation_facts`, plus `build_evidence_citation_packet` | Exact source, docs, manifest, collected documentation facts, and deployment proof use bounded fact reads or citation packets from returned handles. | None |
| Semantic observations and code hints | `list_semantic_documentation_observations`, `list_semantic_code_hints` | Optional semantic evidence is exposed through opt-in, bounded fact reads with truth basis, freshness, provider profile, prompt version, redaction version, and policy state metadata. Deterministic documentation fact reads do not include code hints unless callers use the semantic hint surface. | None |
| Structural code inventory | `inspect_code_inventory` | Content-index inventory covers functions, classes, file-local entities, decorators, methods, and file-level counts. | None |
| Incident response prompts | `get_incident_context`, `list_work_item_evidence` | Incident context returns PagerDuty source evidence, declared/applied/live routing slots, fallback change candidates, explicit missing Jira/PR/build/commit slots, deployable/runtime/image slots only when explicit service-catalog, image-identity, and Kubernetes reducer evidence exists, build/commit slots only when reducer-owned CI/CD run correlations match the selected image, PR slots from provider merged-PR evidence tied to the selected commit, and work-item slots from Jira remote links or issue-key evidence. Work-item evidence reads expose Jira source facts directly when the caller starts from a ticket, project, scope, observed window, or URL fingerprint. | Root-cause attribution, service-health inference, blast-radius inference, and Jira-only PR verification remain out of scope. |

| Tool | Scope contract | Bound | Envelope | Prompt readiness |
| --- | --- | --- | --- | --- |
| `find_code` | case-sensitive entity name required; repository selector optional; global substring requires at least three Unicode characters and uses the content entity-name index | `limit` max 200 | yes | prompt-ready for bounded name lookup; set `exact=true` for complete names, including shorter global names; use content-search tools for source text |
| `find_symbol` | repository selector optional; symbol name required | `limit` | yes | prompt-ready for exact or fuzzy symbol lookup |
| `inspect_code_inventory` | repository, language, entity-kind, file-path, symbol, decorator, method, and class filters optional by prompt shape | `limit` and `offset` | yes | prompt-ready for structural inventory without raw Cypher; returns source handles and truncation |
| `investigate_import_dependencies` | repository, language, source-file, source-module, and target-module filters optional by prompt shape; target-file applies only to cycle and cross-module queries; at least one scope anchor required | non-negative `limit` and `offset`; 25,000 internal candidates, then 422 | yes | prompt-ready for imports by file, module importers, package imports, Python file import cycles, and cross-module calls without raw Cypher; returns one canonical row key by query type |
| `inspect_call_graph_metrics` | repository required; language optional | positive `limit` and non-negative `offset` | yes | prompt-ready for recursive function and high-degree hub-function prompts without raw Cypher; returns canonical `functions` rows with hub call counts, source handles, recursion evidence, and truncation |
| `dispatch_taint_path` | repository required; language, symbol, file path, and line optional | positive `limit` max 100 | yes | prompt-ready for bounded taint-path evidence; rows are labeled `derived_reducer_evidence`, unsupported languages and ambiguity stay explicit, and no semantic provider key is required |
| `dispatch_reaching_def` | repository required; language, symbol, file path, and line optional | positive `limit` max 100 | yes | prompt-ready for bounded reaching-definition summaries from exact parser-emitted `dataflow_functions` facts; unsupported languages, missing evidence, freshness, and truncation stay explicit |
| `dispatch_cfg_summary` | repository required; language, symbol, file path, and line optional | positive `limit` max 100 | yes | prompt-ready for bounded CFG summaries from exact parser-emitted `dataflow_functions` facts with evidence handles and explicit coverage |
| `dispatch_pdg_summary` | repository required; language, symbol, file path, and line optional | positive `limit` max 100 | yes | prompt-ready for partial PDG summaries combining available def-use and control-dependence facts; labeled `partial_derived_summary`, never whole-program PDG truth |
| `trace_route_callers` | exact route `path` plus repository id, service/workload id, or service/workload name scope; service selectors resolve through materialized `Workload` endpoint ownership; optional HTTP method must match `HANDLES_ROUTE.http_method` exactly when supplied | `max_depth` default 2 max 5; `limit` default 25 max 100 | yes | prompt-ready for route-first caller/callee tracing; resolves exactly one endpoint and one `HANDLES_ROUTE` handler before any `CALLS` traversal, returns unsupported when no exact handler edge exists, reports workload/repository impact from materialized graph labels, and rejects ambiguity instead of guessing |
| `investigate_code_topic` | topic plus optional repository selectors | `limit` | yes | prompt-ready for topic summaries with handles |
| `get_code_relationship_story` | canonical `entity_id` or name plus optional repo; optional `relationship_types` multi-type filter; optional `min_confidence` confidence floor | `limit`, `offset`, and an optional `token_budget` that trims rows to fit and reports `summary.token_budget` (`dropped`, `available_before_budget`, `guidance`); rows ranked by bounded `centrality` (`coverage.ranked_by`) and include per-row `provenance` blocks | yes | prompt-ready after target is resolved; budget-aware and centrality-ranked so the most-connected neighbors survive a small budget; omitted confidence floor preserves low-confidence and missing-confidence rows |
| `analyze_code_relationships` | exact target plus relationship query type; callers/callees/importers route through relationship story and accept `relationship_types`; optional `min_confidence` for relationship-story routes; call-chain queries accept exact start/end entity selectors | `limit`, `offset`, `max_depth` for transitive aliases, and an optional `token_budget`; relationship-story aliases include per-row `provenance` blocks | yes | compatibility prompt alias for relationship-story, call-chain, class hierarchy, and override routes without raw Cypher; omitted confidence floor preserves ambiguous and missing-confidence rows |
| `find_dead_code` | repository selector optional; authoritative profile required | `limit` | yes | prompt-ready for bounded candidate scans |
| `investigate_dead_code` | repository selector optional; authoritative profile required; reports coverage and language maturity | `limit` and `offset` | yes | prompt-ready investigation packet; JavaScript/TypeScript candidates stay ambiguous until corpus precision is proven |
| `find_cross_repo_dead_code` | producer repository selector required; optional consumer repository selectors bound liveness evidence | `limit` | yes | prompt-ready cross-repo dead-code packet; deterministic consumer evidence marks symbols live, ambiguous ownership and missing/stale evidence stay unknown, and the read path avoids unbounded graph traversal |
| `investigate_hardcoded_secrets` | repository selector and language optional; finding kind filters optional | `limit` and `offset` | yes | prompt-ready redacted security investigation packet; returns suppression notes, source handles, and truncation coverage without raw secret values |
| `find_dead_iac` | repository selector required or explicit broader scan | `limit` | yes | prompt-ready for bounded IaC candidate scans |
| `find_unmanaged_resources` | AWS scope/account required; authoritative profile required | `limit` and `offset` | yes | prompt-ready for bounded IaC management scans with `safety_summary`, redacted sensitive evidence values, and refused import-plan actions |
| `get_iac_management_status` | AWS scope/account plus exact ARN | singleton | yes | story-first status for one AWS resource; read-only with `safety_gate` and no Terraform/cloud mutation |
| `explain_iac_management_status` | AWS scope/account plus exact ARN | singleton | yes | grouped reducer evidence for one AWS resource; raw tags remain provenance-only, sensitive values are redacted, and `safety_gate` names review/refusal state |
| `propose_terraform_import_plan` | AWS scope/account required; exact ARN optional | `limit` and `offset` | yes | prompt-ready read-only Terraform import-plan candidates; returns import blocks only for safety-approved supported cloud-only findings and refusal reasons for the rest |
| `compose_replatforming_plan` | `scope_kind` plus AWS scope/account required; authoritative profile required | `limit` and `offset` | yes | prompt-ready read-only replatforming plan; composes a provider-neutral migration packet per finding with source state, safety gate, ambiguity-reasoned owner candidates, and ready/refused import candidates; local_lightweight returns `unsupported_capability` |
| `list_aws_runtime_drift_findings` | AWS scope/account required; exact ARN optional | `limit` and `offset` | yes | prompt-ready drift read surface with exact/derived/ambiguous/stale/unknown outcomes, rejected promotion status, evidence, and no raw Cypher |
| `list_terraform_config_state_drift_findings` | exact Terraform state-snapshot `scope_id` required | `limit` and `offset` | yes | prompt-ready, provider-neutral config-vs-state drift read surface with exact/ambiguous outcomes (stale/derived/unresolved/rejected are not emitted; see `go/internal/correlation/drift/tfconfigstate/doc.go`), evidence, and no raw Cypher; never runs Terraform |
| `get_replatforming_rollups` | AWS scope/account required; authoritative profile required | `limit` and `offset` | yes | prompt-ready bounded drift and readiness rollups by account, environment, and service over the source-state taxonomy plus import-ready/needs-review/refused readiness; ambiguous and missing attribution stay in explicit buckets and per-item source state is preserved |
| `find_unmanaged_resource_owners` | AWS scope/account required; authoritative profile required | `limit` and `offset` | yes | prompt-ready bounded ownership packet per drift finding with owner/repository/module/service/environment candidates, confidence, freshness, and safety gate; a single candidate is derived (never exact), conflicting candidates carry explicit ambiguity reasons, raw tags stay provenance-only, and missing attribution is surfaced rather than guessed |
| `calculate_cyclomatic_complexity` | entity id, function name, or repository selector | singleton or `limit` | yes | prompt-ready; list calls return `truncated` |
| `find_most_complex_functions` | optional repository selector | `limit` | yes | prompt-ready; deterministic ordering and `truncated` |
| `inspect_code_quality` | optional repository selector plus optional language/function scope | `limit` and `offset` | yes | prompt-ready for complexity, long-function, high-argument, and refactoring-candidate prompts with source handles and `truncated` |
| `execute_cypher_query` | explicit read-only Cypher supplied by caller | `limit` plus timeout | yes | diagnostics-only; use named tools first |
| `visualize_graph_query` | explicit read-only Cypher supplied by caller; authoritative profile required | `limit` plus injected LIMIT and timeout | yes | executes the query and returns a bounded node/edge visualization packet projected from returned graph entities; `truncation` block plus `truncated` flag; scalar-only results yield an explicit unsupported packet |
| `search_registry_bundles` | required `query` or `ecosystem` scope over the package registry bundle catalog (unscoped requests are rejected) | `limit` | yes | prompt-ready for bounded catalog lookup |
| `list_indexed_repositories` | explicit whole-index inventory | `limit` and `offset` | yes | prompt-ready; returns `truncated` for paging |
| `count_repositories_by_language` | explicit language family | count-only via `limit=0` | yes | prompt-ready; avoids per-repo coverage fan-out |
| `list_repositories_by_language` | explicit language family | `limit` and `offset` | yes | prompt-ready; returns repository handles and `truncated` |
| `get_repository_language_inventory` | explicit whole-index language inventory | `limit` and `offset` | yes | prompt-ready; aggregate language buckets only |
| `get_repository_stats` | repository selector optional; empty selector returns inventory | singleton or inventory | yes | prompt-ready; canonical truth envelope (content-index basis for content-backed counts, hybrid when a graph backend verifies identity) plus an additive `result_limits` drilldown block and explicit `partial_reasons` slot on both singleton and inventory forms; existing coverage `partial_results`/`truncated`/`timeout` fields preserved |
| `execute_language_query` | language and entity type filters, optional repository selector | `limit` | yes | prompt-ready for bounded language scans; feature-parity claims are capped by `specs/language-feature-parity-ledger.v1.yaml`, so partial or absent ledger rows remain unsupported for framework, route, outbound-contract, dead-code, or cross-repo parity prompts |
| `find_function_call_chain` | start/end names or exact `start_entity_id` and `end_entity_id`; optional repository scope for name resolution | `max_depth` | yes | prompt-ready when both endpoints are known; exact IDs avoid ambiguous name broadening |
| `get_ecosystem_overview` | explicit whole-index ecosystem overview | singleton summary | yes | prompt-ready |
| `get_graph_summary_packet` | optional `repo_id` scope; without it returns bounded ecosystem-wide label counts plus a needs-repo note | `limit` (default 10, max 100) bounds hot entities; counts are per-label/per-type bounded scans | yes | prompt-ready summary-first packet (hot entities by call degree, key relationship type counts, ecosystem map); never runs a whole-graph hot-entity scan |
| `trace_deployment_chain` | service name required | singleton trace | yes | prompt-ready after service is resolved |
| `investigate_deployment_config` | service name or workload id plus optional environment | per-section `limit` | yes | prompt-ready for image tags, runtime settings, resource limits, values layers, rendered targets, and read-first file handles |
| `find_blast_radius` | target id required | `limit` | yes | prompt-ready after target is resolved; returns `truncated` |
| `find_infra_resources` | optional query plus category/kind/provider/environment/resource_service/resource_category structured scope, including `cloud` | `limit` | yes | prompt-ready for bounded infra search, including canonical cloud resource graph nodes |
| `list_cloud_resource_inventory` | optional provider (aws/gcp/azure), canonical scope (scope_id/account_id/project_id/subscription_id), and management_origin (declared/applied/observed) filters; unsupported on lightweight local runtime | `limit` and `cursor` | yes | prompt-ready for bounded readback of reducer-owned canonical CloudResource identities with provider, normalized identity, management_origin, per-layer evidence flags, provider-neutral source state, and keyed tag value fingerprints when present; never returns raw provider locators or raw tag values |
| `list_cloud_runtime_drift_findings` | required canonical scope (scope_id/account_id/project_id/subscription_id); optional provider (aws/gcp/azure), cloud_resource_uid, and finding_kind filters; unsupported on lightweight local runtime | `limit` (default 100, max 500) and `offset` | yes | prompt-ready for bounded readback of reducer-owned provider-neutral runtime drift findings with provider, normalized identity, finding_kind, management_status, provider-neutral source state, and refusal-safety posture; unsafe findings are reported as rejected, not omitted; never returns raw provider locators |
| `export_cloud_runtime_drift_packet` | canonical scope (`scope_id`, `account_id`, `project_id`, or `subscription_id`) plus optional provider and exact cloud resource uid | `max_source_facts` lower cap; packet layer defaults stay enforced by the shared builder | yes | prompt-ready investigation evidence packet for provider-neutral runtime drift; forwards to HTTP without rebuilding layers, preserving source-state, safety/refusal posture, missing evidence, reproduce handles, and share-safe redaction |
| `count_infra_resources` | optional category/kind/resource_type/provider/environment/resource_service/resource_category scope | scope-only, no `limit` | yes | prompt-ready for bounded infra rollups by provider, environment, and label |
| `get_infra_resource_inventory` | optional scope filters plus `group_by` (provider / environment / resource_category / resource_service / label) | `limit` (default 100, max 500), bounded `offset` (max 10000) | yes | prompt-ready for bounded infra inventory grouped by one dimension |
| `investigate_resource` | query or canonical resource id plus optional resource family/environment | per-section `limit` and `max_depth` | yes | prompt-ready for resource-first prompts; resolves ambiguity before traversal and returns workloads, repository provenance paths, source handles, limitations, and next calls |
| `analyze_infra_relationships` | target plus relationship type | bounded graph read | yes | prompt-ready after target is resolved |
| `get_repo_summary` | repository selector required | singleton summary | yes | prompt-ready |
| `get_repo_context` | repository selector required | singleton context | yes | prompt-ready |
| `get_relationship_evidence` | resolved relationship id required | singleton evidence packet | yes | prompt-ready; returns `confidence_basis`, `resolution_source`, `evidence_type`, and `evidence_kinds` so clients can compare correlation edge confidence without treating it as code `resolution_method` |
| `list_admission_decisions` | domain, scope_id, and generation_id required; optional state plus anchor_kind/anchor_id for service, repository, workload, cloud resource, package, incident, or domain anchors | `limit` max 200, `truncated`, optional evidence rows | yes | prompt-ready; explains admitted, rejected, ambiguous, stale, missing-evidence, permission-hidden, unsupported, and unsafe reducer candidates without treating them as canonical graph edges |
| `export_deployable_unit_packet` | `scope_id` and `generation_id` required; optional repository selector narrows to one reducer-owned repository anchor | `max_source_facts` lower cap; packet layer defaults stay enforced by the shared builder | yes | prompt-ready investigation evidence packet for deployable-unit admission truth; preserves reducer decisions, graph/query answers, missing evidence, reproduce handles, refusal state, and share-safe redaction |
| `build_evidence_citation_packet` | explicit [citation handles](evidence-citation-handles.md); current runtime hydrates file and entity handles; input array capped at 500 | `limit` up to 50 | yes | prompt-ready for source, docs, manifest, and deployment citations without graph traversal |
| `derive_visualization_packet` | `view` plus a source response already returned by an authorized answer route or tool | node cap 60, edge cap 120 | yes | prompt-ready for service-story, evidence-citation, and incident-context subgraph rendering without a new query |
| `list_package_registry_packages` | package id, ecosystem, or name filter | `limit` | yes | prompt-ready; returns malformed identity rows under `identity_issues[]` with explicit missing-evidence reasons |
| `list_package_registry_versions` | package id required | `limit` | yes | prompt-ready |
| `list_package_registry_dependencies` | package id or version id required | `limit` default 50, optional cursor | yes | prompt-ready; returns `next_cursor` when truncated |
| `list_package_registry_correlations` | package id or repository id required | `limit` default 50, optional cursor | yes | prompt-ready; separates ownership candidates, publication evidence, and consumption truth |
| `count_package_registry_packages` | optional ecosystem, registry, namespace, package_manager, or visibility scope | singleton totals envelope | yes | prompt-ready; cheap-summary count + per-ecosystem rollup over the graph (:Package) corpus using the cookbook Area-5 grouped-count hot path; replaces page-and-iterate caller workflow for ecosystem totals questions |
| `get_package_registry_package_inventory` | optional ecosystem, registry, namespace, package_manager, or visibility scope; `group_by` selects dimension | `limit` default 100, max 500, deterministic ordering, `next_offset` on truncation | yes | prompt-ready; paginated grouped count of (:Package) nodes along one dimension (ecosystem, registry, namespace, package_manager, visibility) anchored on indexed graph properties |
| `list_ci_cd_run_correlations` | scope id, repository id, commit sha, provider run id plus provider when run-only, artifact digest, image ref, or environment required | `limit` default 50, optional cursor | yes | prompt-ready; reports exact, derived, ambiguous, unresolved, and rejected outcomes without promoting CI success to deployment truth; repository-scoped responses include `evidence_summary` separating static workflow files, live run correlation rows, bridge artifact evidence, and public-safe missing-hop classes |
| `count_ci_cd_run_correlations` | optional scope id, repository id, commit sha, provider, artifact digest, image ref, environment, or outcome scope | singleton totals envelope | yes | prompt-ready; cheap-summary count and per-outcome / per-environment / per-provider rollup over reducer-owned run correlation facts; replaces page-and-iterate caller workflow for ecosystem totals questions |
| `get_ci_cd_run_correlation_inventory` | optional scope id, repository id, commit sha, provider, artifact digest, image ref, environment, or outcome scope; `group_by` selects dimension | `limit` default 100, max 500, deterministic ordering, `next_offset` on truncation | yes | prompt-ready; paginated grouped count of reducer-owned run correlations along one dimension (outcome, environment, repository_id, provider) |
| `list_service_catalog_correlations` | scope id, entity ref, repository id, service id, workload id, or owner ref required | `limit` default 50, optional cursor | yes | prompt-ready; reports catalog ownership and drift outcomes without promoting catalog declarations over graph truth, and repository-scoped pages include local descriptor evidence plus external confirmation state |
| `list_codeowners_ownership` | `repository_id` required | `limit` default 50 max 200, optional three-part keyset cursor (`after_order_index`, `after_pattern`, `after_ref`) | yes | prompt-ready; returns a repository's Phase 3 DECLARES_CODEOWNER graph edges plus an `effective_owner` resolved by manifest-vs-codeowners precedence -- a service-catalog manifest declaration with an exact or derived reducer outcome wins, otherwise the CODEOWNERS last-match-wins rule (highest order_index) applies, otherwise `effective_owner` is empty |
| `list_kubernetes_correlations` | scope id, cluster id, workload object id, namespace, image ref, or source digest required | `limit` default 50, optional cursor | yes | prompt-ready; reports live Kubernetes workload ownership and drift outcomes (exact, derived, ambiguous, unresolved, stale, rejected) without promoting a tag coincidence or label selector to exact ownership |
| `list_secrets_iam_identity_trust_chains` | scope id, chain id, workload object id, service account join key, or iam role fingerprint required | `limit` default 50, optional cursor | yes | prompt-ready; reports reducer-owned secrets/IAM identity trust chains (workload to ServiceAccount to IAM role to Vault policy) with state exact, partial, unresolved, stale, permission_hidden, or unsupported, as provenance-only fingerprints and join keys, never exposing secret values, raw paths, or token claims, and never promoting a non-exact chain to graph truth |
| `list_secrets_iam_privilege_posture_observations` | scope id or observation id required | `limit` default 50, optional cursor | yes | prompt-ready; reports reducer-owned risky broad or partial posture observations (for example external trust without sts:ExternalId) by risk type and severity, as provenance-only evidence the reducer never promotes to an exact path |
| `list_secrets_iam_secret_access_paths` | scope id, path id, chain id, or vault mount join key required | `limit` default 50, optional cursor | yes | prompt-ready; reports reducer-owned Vault policy-to-KV metadata access paths reachable from an exact identity chain, as fingerprints and capabilities only, never as a secret value |
| `list_secrets_iam_posture_gaps` | scope id, gap id, or service account join key required | `limit` default 50, optional cursor | yes | prompt-ready; reports reducer-owned missing, stale, permission_hidden, or unsupported evidence that blocks exact trust-chain truth, surfaced rather than silently dropped |
| `count_secrets_iam_posture` | scope id required | none (single bounded rollup) | yes | prompt-ready; reports reducer-owned secrets/IAM posture as provenance-only grouped counts (identity chains by state, privilege observations by risk type and severity, secret access paths by state, posture gaps by gap type) for one scope, exposing no fingerprints, paths, or evidence |
| `list_observability_coverage_correlations` | scope id, provider, coverage signal, observability object ref, target uid, or target service ref required | `limit` default 50, optional cursor | yes | prompt-ready; reports which monitored cloud resources and services have alarm/dashboard/log/trace coverage versus gaps, as structural correlation only, never a health assertion from telemetry values |
| `list_container_image_identities` | digest, image ref, source repository id, OCI repository id, or outcome required | `limit` default 50, optional cursor | yes | prompt-ready; reports digest-first reducer identity rows with source bridge metadata, source layers, and evidence fact ids without promoting weak, ambiguous, unresolved, stale tags, or OCI repository identity into source-repository truth |
| `count_container_image_identities` | optional digest, image ref, source repository id, OCI repository id, or outcome scope | singleton totals envelope | yes | prompt-ready; cheap-summary count and per-outcome / per-identity-strength rollup over reducer-owned container image identity facts; source-scoped zeroes expose `source_bridge.missing_evidence` instead of only aggregate zero |
| `get_container_image_identity_inventory` | optional digest, image ref, source repository id, OCI repository id, or outcome scope; `group_by` selects dimension | `limit` default 100, max 500, deterministic ordering, `next_offset` on truncation | yes | prompt-ready; paginated grouped count of reducer-owned identities along one dimension (outcome, identity_strength, repository_id) |
| `list_advisory_evidence` | CVE, advisory id, package id, repository id/selector, service id, or workload id required; optional source narrows anchored pages | `limit` default 50, optional cursor | yes | prompt-ready; repository/service/workload scopes derive advisory anchors from reducer-owned impact findings, then group source-only GHSA/CVE/OSV/NVD/EPSS/KEV/CWE/range/fixed-version/withdrawn evidence without implying additional impact or promoting provider-alert-only rows |
| `get_vulnerability_scanner_read_contract` | optional route name; unknown routes rejected before any read-model access | singleton contract | yes | prompt-ready; machine-readable scanner filter support, route bounds, backing read models, CVE-to-owner remediation packet schema, missing-evidence semantics, and provider-only separation |
| `list_supply_chain_impact_findings` | CVE, advisory id, package id, repository id or selector, subject digest, image ref, impact status, ecosystem, service, workload, environment, severity, priority bucket, or `min_priority_score > 0` required; optional `profile=precise` (default) or `comprehensive` | `limit` default 50, optional cursor; `sort` can use finding id or priority score | yes | prompt-ready; keeps CVSS, EPSS, KEV, priority contributions, source snapshot freshness, reachability, fixed version, missing evidence, and per-row `detection_profile` separate |
| `count_supply_chain_impact_findings` | optional CVE, advisory id, package id, repository id or selector, subject digest, image ref, impact status, ecosystem, service, workload, environment, severity, priority, suppression scope, or `profile=precise` (default) / `comprehensive` | singleton totals envelope | yes | prompt-ready; cheap-summary count and by-priority/severity rollup over reducer-owned impact facts; preserves API detection-profile parity and replaces page-and-iterate caller workflow for ecosystem totals questions |
| `get_supply_chain_impact_inventory` | optional CVE, advisory id, package id, repository id or selector, subject digest, image ref, impact status, ecosystem, service, workload, environment, severity, priority, or suppression scope; `group_by` selects dimension | `limit` default 100, max 500, deterministic ordering, `next_offset` on truncation | yes | prompt-ready; paginated grouped count of reducer-owned impact findings along one dimension (impact_status, priority_bucket, severity, repository_id, ecosystem) |
| `explain_supply_chain_impact` | finding id, or advisory/CVE plus package, repository, image digest/ref, workload, or service required | singleton explanation with evidence packet handle | yes | prompt-ready; hydrates only the finding's bounded evidence fact ids and returns `no_finding` or `ambiguous_scope` refusal envelopes instead of performing whole-graph explain or inventing reachability/deployment truth |
| `export_supply_chain_impact_packet` | exact finding id preferred; advisory/package/repository/image/workload/service selectors optional with ambiguity refusal when underspecified | `max_source_facts` lower cap; packet layer defaults stay enforced by the shared builder | yes | prompt-ready investigation evidence packet for supply-chain impact; preserves the same explanation truth labels, missing evidence, refusal state, reproduce handles, and share-safe redaction as HTTP and CLI packet export |
| `list_security_alert_reconciliations` | repository id or selector, provider, package id, CVE, or GHSA required; provider state and reconciliation status filter anchored pages only | `limit` default 50, optional cursor | yes | prompt-ready; keeps provider alert state separate from Eshu impact state and exposes `reason_code` plus `missing_evidence[]` for provider-only, stale, unsupported, and ambiguous rows |
| `count_security_alert_reconciliations` | optional repository id or selector, provider, package id, CVE, GHSA, provider state, or reconciliation status scope | singleton totals envelope | yes | prompt-ready; cheap-summary count and per-reconciliation-status / per-provider / per-provider-state rollup over reducer-owned reconciliation facts; replaces page-and-iterate caller workflow for ecosystem totals questions |
| `get_security_alert_reconciliation_inventory` | optional repository id or selector, provider, package id, CVE, GHSA, provider state, or reconciliation status scope; `group_by` selects dimension | `limit` default 100, max 500, deterministic ordering, `next_offset` on truncation | yes | prompt-ready; paginated grouped count of reducer-owned reconciliations along one dimension (reconciliation_status, provider, provider_state, repository_id, package_id) |
| `list_sbom_attestation_attachments` | subject digest/image digest, document id, document digest, repository id or selector, workload, or service required | `limit` default 50, optional cursor; warning summaries return a fixed duplicate-collapsed preview with `warning_summary_count` and `warning_summaries_truncated` | yes | prompt-ready; preserves attached verified, unverified, parse-only, subject mismatch, ambiguous subject, unknown subject, unparseable states, and parser-warning visibility; repository/workload/service scoped reads expose missing image and image-to-SBOM evidence without turning components into vulnerability impact |
| `count_sbom_attestation_attachments` | optional subject digest, document id, document digest, repository id or selector, workload, service, attachment_status, or artifact_kind scope | singleton totals envelope | yes | prompt-ready; cheap-summary count and per-attachment-status / per-artifact-kind rollup over reducer-owned attachment facts; scoped zero remains scoped zero and carries source/image missing-evidence classes instead of falling back to global totals |
| `get_sbom_attestation_attachment_inventory` | optional subject digest, document id, document digest, repository id or selector, workload, service, attachment_status, or artifact_kind scope; `group_by` selects dimension | `limit` default 100, max 500, deterministic ordering, `next_offset` on truncation | yes | prompt-ready; paginated grouped count of reducer-owned attachments along one dimension (attachment_status, artifact_kind, subject_digest) |
| `get_repo_story` | repository selector required | singleton story | yes | prompt-ready |
| `get_repository_coverage` | repository selector required | singleton coverage | yes | prompt-ready |
| `get_repository_freshness` | repository selector required; optional `expected_commit` | singleton verdict | yes | prompt-ready; reports `current`/`building`/`behind`/`unobserved`/`unknown` per repository, separating the repo's own reducer/projector drain from cross-repo `shared_enrichment` backlog so one repository's freshness never blames another's cross-repo materialization |
| `trace_resource_to_code` | resource id or selector required | `max_depth` and `limit` | yes | prompt-ready; returns `truncated` |
| `explain_dependency_path` | source and target required | bounded path search | yes | prompt-ready |
| `trace_exposure_path` | `source` (with `repo_id`) or `source_entity_id` required | `max_depth` (1-10) | yes | derived (symbol-level reachability, never value-flow); reports `unresolved` when a code-to-cloud bridge edge is not materialized, never fabricates a path |
| `find_change_surface` | entity scope required | `limit` | yes | legacy entity-scoped path; prefer `investigate_change_surface` for code-topic and changed-path prompts |
| `investigate_change_surface` | changed path, topic, or entity scope required | bounded investigation | yes | prompt-ready |
| `analyze_pre_change_impact` | changed path list or structured changes with `repo_id`, optional base/head provenance for a caller-derived diff; optional target/topic scope | `limit`, `max_depth`, `offset`; fail-closed unsupported profile, backend-unavailable status, and explicit partial/truncated metadata | yes | prompt-ready pre-change workflow over the same change-surface evidence and AnswerPacket contract |
| `plan_developer_change` | changed path list or structured changes with `repo_id`, optional base/head provenance and `developer_intent`; optional target/topic scope | `limit`, `max_depth`, `offset`; read-only plan, fail-closed unsupported profile, backend-unavailable status, and explicit partial/truncated metadata | yes | prompt-ready `developer_change_plan.v1` workflow with actions, patch guidance, recommended tests, and bounded next calls |
| `compare_environments` | workload plus two environments | per-environment `limit` | yes | prompt-ready; returns story, summary, shared/dedicated resources, evidence, limitations, next calls, and side-specific truncation coverage |
| `resolve_entity` | exact case-sensitive name/query; global calls require one supported `type`, except canonical `content-entity:` handles and workload resolution; repository/directory/file require repository scope | `limit` max 100 | yes | prompt-ready for exact typed resolution before drilldowns; repository-selected calls retain graph-backed resolution |
| `get_entity_context` | canonical entity id required | singleton context | yes | prompt-ready after `resolve_entity`; carries the canonical truth envelope plus an additive `result_limits` drilldown block (bounded relationship limit, deterministic ordering, `truncated`, `get_relationship_evidence` drilldown, context path) and an explicit `partial_reasons` slot |
| `get_workload_context` | canonical workload id required | singleton context | yes | prompt-ready after workload resolution; carries the canonical truth envelope plus an additive `result_limits` drilldown block (bounded limit, deterministic ordering, instance/dependent/consumer counts, `truncated`, `get_workload_story` drilldown, context path) and an explicit `partial_reasons` slot |
| `get_workload_story` | canonical workload id required | singleton story | yes | prompt-ready after workload resolution; carries the canonical truth envelope plus an additive `result_limits` drilldown block (bounded limit, deterministic ordering, `truncated`, `get_workload_context` drilldown, context path) and an explicit `partial_reasons` slot |
| `get_service_context` | `workload_id` selector required; value may be a canonical workload ID or service name | singleton context | yes | prompt-ready |
| `get_service_story` | `workload_id` selector or `service_name` plus optional repository selector (`repo`, `repository_id`, or `repo_id`); `workload_id` may be a canonical workload ID or service name | singleton story | yes | prompt-ready; repository-scoped calls use the same HTTP disambiguation path as API clients |
| `get_service_intelligence_report` | `workload_id` selector or `service_name` plus optional `service_id`, `repo`, and `environment`; same selector and HTTP disambiguation path as `get_service_story` | singleton report (fixed five sections) | yes | prompt-ready; composes the service story into the `service_intelligence_report.v1` report with preserved truth labels, evidence handles, limitations, bounded next calls, and suggested investigations; dispatches to the same route the API serves |
| `investigate_service` | service name plus optional environment/question | bounded investigation | yes | prompt-ready |
| `get_incident_context` | provider incident id required; optional provider, scope id, service id, and time window narrow the read | `limit` default 25, max 100 | yes | prompt-ready for on-call incident packets; separates exact provider evidence, intended/applied/live routing evidence, fallback service/time change candidates, derived edges, ambiguous candidates, drifted or permission-hidden routing, and explicit missing evidence |
| `list_work_item_evidence` | Jira scope id, project key, work-item key, provider issue id, URL fingerprint, external URL, or observed-after window required | `limit` default 25, max 200, optional `after_fact_id` cursor | yes | prompt-ready for ticket-first source evidence; separates exact provider facts, missing evidence, stale evidence, permission-hidden rows, unsupported link types, and rejected unsafe payloads without returning raw URLs or verifying PR/commit/deploy/runtime/image/service truth |
| `get_generation_lifecycle` | scope id, repository, collector kind, source system, generation id, or status filter optional; named scope/repository/generation that matches nothing returns not-found | `limit` default 50, max 500, `truncated` flag | yes | prompt-ready for active/pending/superseded/completed/failed generation drilldown with current active generation, trigger kind, freshness hint, observed/activated/superseded timestamps, per-generation queue status, and latest failure when present |
| `get_changed_since` | exactly one mutually exclusive scope id or canonical repository is required plus a since reference (`since_generation_id` or RFC3339 `since_observed_at`); unknown scope returns scope-not-found, an unresolved since reference returns not-found, a scope with no current active generation returns an explicit unavailable diff, and retention-pruned history returns `unavailable_reason=retention_expired` | `sample_limit` default 25, max 200, per-classification `truncated` flag; counts are exact | yes | prompt-ready for repository-scope changed-since deltas diffing the prior generation fact set against the current active generation fact set by stable fact key; per-category (files, content entities, facts) added/updated/unchanged/retired/superseded counts with bounded sample handles; retired and superseded are never collapsed into unchanged; retention-expired truth points the next check to `get_generation_lifecycle` |
| `get_service_changed_since` | `service_id` and `since_generation_id` required; unknown service returns service-not-found, an unresolved since reference returns not-found, and a service with no current active generation returns an explicit unavailable diff | `sample_limit` default 25, max 200, per-classification `truncated` flag; counts are exact | yes | prompt-ready for service-scope changed-since deltas diffing a prior service materialization generation's evidence snapshot set against the current active generation's set by generation-independent `service_evidence_key`; all six evidence families ship (ownership, deployment, runtime, dependencies, docs, incidents, vulnerabilities); per-family added/updated/unchanged/retired/superseded counts with bounded sample handles; retired and superseded are never collapsed into unchanged |
| `get_file_content` | repository selector and relative path required | singleton file | yes | prompt-ready for exact source read |
| `get_file_lines` | repository selector, relative path, and line range required | explicit line range | yes | prompt-ready for citations |
| `get_entity_content` | canonical entity id required | singleton entity source | yes | prompt-ready after entity resolution |
| `search_file_content` | pattern plus optional repository selectors | `limit` and `offset` | yes | prompt-ready; unsupported filters are not advertised |
| `search_entity_content` | pattern plus optional repository selectors | `limit` and `offset` | yes | prompt-ready; unsupported filters are not advertised |
| `list_documentation_findings` | optional finding, source, document, status, truth, freshness, and updated-since filters | `limit` and `cursor` | yes | prompt-ready for durable documentation truth finding review; reads the same fact-backed route as HTTP |
| `count_documentation_findings` | optional scope_id, finding_type, source_id, document_id, status, truth_level, or freshness_state scope | singleton totals envelope | yes | prompt-ready; cheap-summary count and per-status / per-truth-level / per-freshness-state rollup over durable documentation finding facts; inherits the same per-document read permissions as the list endpoint; replaces page-and-iterate caller workflow for ecosystem totals questions |
| `get_documentation_finding_inventory` | optional scope_id, finding_type, source_id, document_id, status, truth_level, or freshness_state scope; `group_by` selects dimension | `limit` default 100, max 500, deterministic ordering, `next_offset` on truncation | yes | prompt-ready; paginated grouped count of documentation findings along one dimension (status, truth_level, freshness_state, finding_type, source_id); inherits the same per-document read permissions |
| `list_documentation_facts` | scope, source, document, or section anchor required except `fact_kind=source`; optional fact kind and text query including link target URIs | `limit` and `cursor` | yes | prompt-ready for collected Confluence and source-neutral documentation facts before findings exist |
| `list_semantic_documentation_observations` | source, document, repository, service, fact, provider, freshness, status, policy, or observation scope required | `limit` and `cursor` | yes | prompt-ready for opt-in semantic observation review; exposes truth basis, freshness, provider profile, prompt version, redaction version, policy state, and redacted provenance without raw prompt payloads or provider responses |
| `list_semantic_code_hints` | repository, path, entity, topic, fact, provider, freshness, status, policy, or corroboration scope required | `limit` and `cursor` | yes | prompt-ready for opt-in non-canonical code hint review; preserves corroboration and promotion state, exposes redacted provenance, and stays separate from deterministic documentation fact reads |
| `search_semantic_context` | repository id, query, mode, limit, and timeout required; optional service, workload, environment, and source-kind filters narrow the repository-bounded corpus | `limit` max 100, `timeout_ms`; no request-time corpus rebuild | yes | prompt-ready for curated semantic retrieval from the persisted active search-document index; returns derived truth basis, explicit search method, truncation, source handles, and snake-case graph handles without raw Cypher or unbounded fallback |
| `get_documentation_evidence_packet` | finding id required | singleton evidence packet | yes | prompt-ready for documentation updater evidence snapshots |
| `check_documentation_evidence_packet_freshness` | packet id required, saved packet version optional | singleton freshness result | yes | prompt-ready for stale-packet checks before updater publication |
| `list_query_playbooks` | catalog read, no caller scope required | catalog size | yes | prompt-ready for listing deterministic starter-prompt workflows with required inputs, steps, expected truth, evidence, and failure modes |
| `resolve_query_playbook` | catalog playbook id plus declared string inputs | resolved call count | yes | prompt-ready for resolving one workflow into ordered bounded calls; does not execute calls or expose raw Cypher |
| `list_investigation_workflows` | catalog read, no caller scope required | catalog size | yes | prompt-ready for listing guided investigation workflows with input shape, required and optional evidence, expected output packet, grouped atomic tools, starter prompts, and missing-evidence routes |
| `resolve_investigation_workflow` | catalog workflow id, declared string inputs, and observed missing-evidence keys | recommended-next-call count | yes | prompt-ready for resolving one guided workflow into bounded next calls selected from caller-provided missing-evidence state; does not execute calls, read tenant data, or expose raw Cypher |
| `list_collectors` | explicit runtime inventory | singleton status list | yes | prompt-ready for collector diagnostics; distinguishes coordinator-managed, direct-mode, disabled, and unregistered status evidence |
| `list_component_extensions` | explicit runtime component registry inventory with optional `limit`; requires the API/MCP runtime to have `ESHU_COMPONENT_HOME` configured, otherwise returns unavailable | bounded local registry readback with `count`, `total_count`, `limit`, and `truncated` | yes | prompt-ready for optional component diagnostics; exposes component ID, version, manifest digest, lifecycle states, activation config handles, and policy diagnostics without local paths or index-as-trust shortcuts |
| `get_component_extension_diagnostics` | component id required; requires the API/MCP runtime to have `ESHU_COMPONENT_HOME` configured, otherwise returns unavailable | singleton component drilldown | yes | prompt-ready for one optional component; separates installed, enabled, claim-capable, revoked, incompatible, and failed states and redacts manifest/config paths |
| `list_collector_extraction_readiness` | advisory collector extraction policy catalog with optional `limit`; static in-binary policy data needing no component home, graph, or registry state | bounded catalog readback with `count`, `total_count`, `limit`, and `truncated` | yes | prompt-ready for advisory extraction readiness; exposes each tracked collector family's classification, per-criterion checklist, and blockers; does not move code |
| `get_collector_extraction_readiness` | collector family required; returns not_found when the family is not tracked by the extraction policy | singleton family drilldown | yes | prompt-ready for one collector family; exposes the per-criterion checklist and rationale; advisory only and does not move code |
| `list_fact_schema_versions` | core fact-kind to supported schema-version registry with optional `limit`; static in-binary registry data needing no component home, graph, or registry state | bounded registry readback with `count`, `total_count`, `limit`, and `truncated` | yes | prompt-ready for the core fact-schema-version registry; exposes the schema version each core consumer supports per fact kind; does not move code |
| `get_fact_schema_version` | fact_kind required; optional `candidate` classifies a collector version; returns not_found when the fact kind is not core-owned | singleton fact-kind drilldown with optional compatibility verdict | yes | prompt-ready for one core fact kind; classifies a candidate as supported, unsupported_major, unsupported_minor, or unknown_kind so an incompatible collector version is detected safely; advisory only |
| `list_ingesters` | explicit runtime inventory | `limit` and `offset` | yes | prompt-ready for runtime diagnostics |
| `get_ingester_status` | ingester id required | singleton status | yes | prompt-ready for runtime diagnostics |
| `get_index_status` | optional repository selector | singleton status | yes | prompt-ready for runtime diagnostics |
| `get_hosted_readiness` | whole hosted deployment status from API status snapshot plus bounded graph readback | singleton status, optional text summary through HTTP | yes | prompt-ready fail-closed readiness report for process, dependency, queue drain, collector completion, shared projection backlog, and API/MCP query readback |
| `get_collector_readiness` | whole collector fleet promotion readiness from API status snapshot | singleton status | yes | prompt-ready per-collector-family promotion readiness (state, reducer readback, evidence counts, last proof time, blockers, recommended next action); reuses status promotion-proof vocabulary, mirrors the API route so MCP and HTTP agree, and redacts credentials and raw provider payloads |
| `get_operator_control_plane` | whole runtime operator read model from API status snapshot; tenant-bounded for scoped tokens | singleton status | yes | prompt-ready unified read model combining queue depth with claim-latency and stuck-work signals, reducer-domain backlogs, collector-family promotion verdicts with the newest proof artifact, and dead-letter state classed by reducer domain and collector-generation commit; mirrors the API route so MCP and HTTP agree, and withholds raw correlation IDs and instance labels from scoped tokens |
| `list_dead_letter_work_items` | fact_work_items dead letters joined to ingestion scope collector metadata; tenant-bounded for scoped tokens | required `limit` plus `timeout_ms`; optional `failure_class`, `domain`, `scope_id`, `collector_kind`, `updated_after`, `updated_before` | yes | prompt-ready bounded dead-letter triage list with deterministic `updated_at DESC, work_item_id ASC` ordering and a `truncated` flag; component-extension authors see only granted component scopes and do not need database access |
| `list_reducer_input_invalid_facts` | scope_id and generation_id required; tenant-bounded for scoped tokens | required `scope_id`, `generation_id`, `limit`, and `timeout_ms`; optional `domain`, `fact_kind` | yes | prompt-ready durable read of reducer_input_invalid_facts (issue #4630): facts the reducer quarantined during typed-payload decode for a missing or null required field, with deterministic `decided_at DESC, fact_id ASC, missing_field ASC` ordering and a `truncated` flag; scoped tokens are restricted to granted repository or scope ids |
| `get_freshness_causality` | whole runtime freshness causality from API status snapshot; tenant-bounded for scoped tokens | singleton status | yes | prompt-ready stale-answer causality: each closed freshness cause with runtime/per-answer observability and a bounded next-check, the generation lifecycle including retired generations, and pending projection work; mirrors the API route so MCP and HTTP agree, and withholds raw scope/generation identifiers from scoped tokens |
| `get_hosted_governance_status` | whole hosted governance runtime posture | singleton status | yes | prompt-ready redacted governance status for policy mode, state, source kind, readiness booleans, aggregate counts, semantic and extension posture, and low-cardinality reasons; no raw policy, tenant, workspace, source, credential, endpoint, prompt, response, private path, or token values |
| `get_semantic_capability_status` | explicit runtime semantic extraction capability status | singleton status | yes | prompt-ready for optional semantic extraction diagnostics; no-provider mode returns unavailable, hosted provider profiles are redacted and source-policy gated, semantic queue/budget/audit readbacks are aggregate-only, and deterministic paths remain unaffected |
| `get_answer_narration_status` | explicit optional answer narration runtime posture | singleton status | yes | prompt-ready for governed narration diagnostics; default mode returns unavailable with deterministic fallback available, retention metadata-only, validator reason codes, and no provider traffic, prompts, provider responses, credential handles, source identifiers, private paths, or canonical truth changes |
| `get_capability_catalog` | whole reconciled capability catalog from the embedded artifact | optional `maturity` and `owner` filters, `limit`/`offset` paging | yes | prompt-ready deterministic read of capability maturity, public surfaces, proof signals, owner package, known gaps, and linked issues; static embedded artifact with fresh truth and no live backend read <!-- product-claim: id=docs.mcp-tool-contract.capability-catalog-read --> |
| `get_surface_inventory` | whole generated surface inventory from the embedded artifact | optional `category` and `readiness` filters, `limit`/`offset` paging | yes | prompt-ready deterministic read of every platform surface (command, collector, reducer_domain, api_route, mcp_tool, console_page) with its readiness lane, owner, promotion proof, docs, and notes; static embedded artifact with fresh truth and no live backend read |
| `investigate_contract_impact` | contract family plus provider, consumer, repository, route, topic, or service scope; HTTP provider lookups require a provider repository | `limit` default 25, max 100; unsupported topic/gRPC family states are explicit | yes | prompt-ready first slice for deterministic cross-repo API contract impact; proxies `/api/v0/impact/contracts`, never infers edges from string similarity or semantic output, and stays fail-closed for scoped tokens until tenant-filtered consumer projection lands |
| `ask` | natural-language question over the caller's stack; default-off (requires `ESHU_ASK_ENABLED=true` and an `agent_reasoning` provider profile) | single question + optional format; returns answer_prose, artifacts, truth_class, evidence_handles, query_trace, partial, limitations | yes via POST /api/v0/ask | default-off; returns `state=unavailable` when disabled or provider absent; when enabled, wires the bounded Tier-1 engine in-process; read-only; scope enforced at the query layer; no provider prompts, raw provider bodies, or credentials in the response; SSE streaming and Tier-2 sandbox wiring are planned follow-ups |
| `list_relationship_edges` | verb required; optional `source_tool` (closed enum from canonical vocabulary, validated before forwarding — unknown value rejected with error); optional `limit` 1–200 | `limit` default 50, max 200; `truncated` set when more edges exist | yes | prompt-ready; proxies POST /api/v0/relationships/edges; `source_tool` narrows Tier-2 cross-repo edges by source-tool label; Tier-1 and Tier-3 verbs always return empty for any source_tool filter (documented in API contract) |
| `list_repository_files` | `repo_id` required; optional `language` (open token, alias-expanded by the API e.g. typescript also matches tsx); optional `path` subdirectory scope; optional `recursive` subtree expansion; optional `ref` commit SHA for indexed-ref validation | implicit 50 000-file limit; `truncated` set when exceeded | yes | prompt-ready; proxies GET /api/v0/repositories/{repo_id}/tree; `language` restricts file entries to one language family; the predicate is pushed into the content-store read, so the 50 000-file cap applies to the matching set (a language whose files sort beyond the cap in the full repository is still returned, and `truncated=true` then means the matching set itself exceeded the cap); unrecognised language token yields empty listing rather than error, mirroring the API |

## Verification

No-Regression Evidence: focused query, MCP, and telemetry tests cover:

- MCP tool response shaping preserves canonical envelopes and plain JSON
  handler payloads as `structuredContent`, with resource blocks for exact
  serialized evidence. `go test ./internal/mcp -count=1` covers the
  text-summary, structured-payload, and envelope-error paths.
- Terraform import-plan candidates, including safe S3 and Lambda import blocks,
  request-derived S3 account and region hints, sensitive-resource refusal,
  ARN-only `resource_id` handling, OpenAPI response fields, MCP tool
  definitions, route mapping, envelope negotiation, bounded paging, and span
  names.
- Dead-code investigation routes, including HTTP route shape, OpenAPI entries,
  MCP tool definitions, route mapping, envelope negotiation, bounded paging,
  and TypeScript ambiguity policy.
- Legacy impact and environment-comparison no-cache bounds, including returned
  limit and truncation behavior.
- Environment comparison story packets, deployment configuration influence, and
  resource-first investigation route contracts.
- Incident context route contracts, explicit missing evidence slots, explicit
  PagerDuty service URL to runtime/image evidence, image-to-build/commit
  evidence, commit-to-PR evidence, Jira work-item enrichment, ambiguous
  provider-scope errors, OpenAPI fields, and MCP route mapping.
- Semantic search route contracts, including required repository scope, explicit
  retrieval mode, limit, timeout, source-kind filtering, scoped-token
  no-grant/not-found behavior, OpenAPI fields, MCP schema, and route mapping.
- Broader MCP dispatch and query contracts through `go test ./internal/mcp ./internal/query -count=1`.

Observability Evidence: `telemetry.SpanQueryIaCTerraformImportPlan` (`query.iac_terraform_import_plan`) wraps the import-plan candidate HTTP/MCP route with stable `http.route` and `eshu.capability` attributes. `telemetry.SpanQueryDeadCodeInvestigation` (`query.dead_code_investigation`) wraps the dead-code investigation route with the same route and capability attributes. `telemetry.SpanQueryIncidentContext` (`query.incident_context`) wraps the incident-context route with stable route and capability attributes. `telemetry.SpanQueryWorkItemEvidence` (`query.work_item_evidence`) wraps the work-item evidence route with stable route and capability attributes. `telemetry.SpanQuerySemanticSearch` (`query.semantic_search`) wraps the semantic-search route with stable route and capability attributes. Existing MCP `dispatch tool` debug logs, HTTP response envelopes, query handler errors, graph `neo4j.query` spans, Postgres `postgres.query` spans, service query timing logs, search result `indexed_document_count`, `corpus_limit`, `corpus_may_be_truncated`, and bounded `coverage.limit`/`coverage.truncated` response fields diagnose whether a prompt call was scoped, complete, or needs a follow-up page.
