# AWS Collector Scanner Coverage

Use this page for the AWS `service_kind` values backed by production scanner
adapters. Each scanner self-registers from
`go/internal/collector/awscloud/services/<svc>/runtimebind/init()`. The
collector-aws-cloud command pulls every binding through
`go/internal/collector/awscloud/awsruntime/bindings/bindings.go`, and the
runtime entry point `awsruntime.DefaultScannerFactory.Scanner` dispatches
through the resulting registry.

The collector is metadata-only. It emits reported facts for reducer admission.
It does not mutate AWS resources, read protected payloads, or write graph truth.

## Supported Service Kinds

`allowed_services` may include:

| Service kind | Coverage |
| --- | --- |
| `iam` | Roles, managed policies, instance profiles, trust relationships. |
| `ecr` | Repositories, lifecycle policies, image references, pagination checkpoints. |
| `ecs` | Clusters, services, tasks, relationships, redacted task definitions. |
| `ec2` | VPC, subnet, security group, security-group rule, ENI topology. The EC2 scanner owns the ENI surface and may carry instance target evidence on ENI attachments, but does not emit `aws_ec2_instance` resources; VPC network-fabric resources live in the `vpc` scanner. |
| `vpc` | Route tables, internet gateways, NAT gateways, network ACLs, VPC peering connections, VPC endpoints (gateway and interface), Elastic IPs, DHCP option sets, customer gateways, virtual private gateways, and site-to-site VPN connections plus relationships back to EC2-owned VPCs, subnets, ENIs, and instances. No VPN tunnel pre-shared keys or `CustomerGatewayConfiguration` XML bodies. |
| `transitgateway` | Transit gateways, transit gateway route tables, attachments (VPC, VPN, Direct Connect gateway, peering, Connect), peering attachments, multicast domains, and policy tables. Relationships: attachment-to-VPC/VPN-connection/Direct-Connect-gateway/peer, route-table-to-attachment, attachment-to-transit-gateway, route-table/multicast-domain/policy-table-to-transit-gateway, and peering-to-remote-transit-gateway. Cross-account peer transit gateway IDs, owner accounts, and Regions are surfaced as AWS reports them and flagged `cross_account` for downstream org-context joins; the scanner never resolves the remote account identity. No transit gateway routes, multicast group memberships, or policy table rule bodies. |
| `transfer` | AWS Transfer Family servers (ARN, server id, domain S3/EFS, endpoint type, protocols SFTP/FTPS/FTP/AS2, identity provider type, state, security policy, user count) and service-managed users (ARN, user name, home directory type/path, home-directory mappings as paths only, role ARN). Relationships: server-to-VPC-endpoint (bare `vpce-` id, targets `aws_vpc_endpoint`), server-to-Elastic-IP (bare `eipalloc-` id, targets `aws_vpc_elastic_ip`), server-to-ACM-certificate for FTPS (cert ARN, targets `aws_acm_certificate`), server-to-CloudWatch-log-group (log-group ARN, targets `aws_cloudwatch_logs_log_group`), server-to-logging-IAM-role and user-to-IAM-role (role ARN, targets `aws_iam_role`), user-home-directory-to-S3-bucket (synthesized partition-aware `arn:<partition>:s3:::<bucket>`, targets `aws_s3_bucket`), and user-home-directory-to-EFS-file-system (synthesized partition-aware `arn:<partition>:elasticfilesystem:<region>:<account>:file-system/<fs-id>`, targets `aws_efs_file_system`). Metadata only: never host key fingerprints, host key material, SSH public keys, user policy JSON, POSIX UID/GID material, or login banners; home directories are recorded as paths, never object or file contents. The SDK adapter read surface is `ListServers`/`DescribeServer`/`ListUsers`/`DescribeUser` only, with every mutation and key-material API excluded by a reflection gate. |
| `networkfirewall` | Firewalls (name, ARN, VPC id, subnet mapping ids, delete/subnet/policy-change protection flags, readiness status, configuration sync state), firewall policies (name, ARN, status, stateless/stateless-fragment/stateful default-action names, consumed rule capacity, association count - never the full policy rule body), rule groups (name, ARN, type STATEFUL/STATELESS, configured capacity, association count - never the rule source / Suricata signature bodies), and TLS inspection configurations (name, ARN, status, association count - never certificate bodies or TLS scope rule bodies). Relationships: firewall-to-VPC (bare vpc-id, targets `aws_ec2_vpc`), firewall-to-subnet (bare subnet-id, targets `aws_ec2_subnet`), firewall-to-firewall-policy, policy-to-rule-group, and policy-to-TLS-inspection-configuration. Rule group metadata is read through `DescribeRuleGroupMetadata`, not `DescribeRuleGroup`, so the rule source (threat intelligence) is unreachable by construction; the SDK adapter read surface excludes every mutation and the rule-body read by a reflection gate. No Network Firewall mutation APIs. |
| `directconnect` | Direct Connect connections (id, name, location, bandwidth, state, partner/provider name, MACsec capability flag), virtual interfaces (VLAN, BGP ASN, type private/public/transit, state), Direct Connect gateways, and link aggregation groups (LAGs). Relationships: virtual-interface-to-Direct-Connect-gateway, virtual-interface-to-connection, connection-to-LAG, Direct-Connect-gateway-to-transit-gateway, and Direct-Connect-gateway-to-virtual-private-gateway (the last two derived from gateway associations). The Direct Connect gateway resource uses `resource_type=aws_direct_connect_gateway` with the bare gateway id, which is the target the `transitgateway` scanner already emits, so the transit-gateway-to-Direct-Connect-gateway edge resolves once this scanner runs; gateway associations target `aws_ec2_transit_gateway` and `aws_vpc_vpn_gateway` by bare AWS id. Never reads or persists the BGP authentication key (`authKey`) on a virtual interface or BGP peer, never persists MACsec connectivity association key names (CKN) or secret ARNs (only the capability flag), and never calls `DescribeRouterConfiguration` (which renders the auth key). No Direct Connect mutation APIs. |
| `elb` | Classic (v1) Load Balancers (name, DNS name, scheme, VPC id, Availability Zones, subnets, security groups, source security group name, listeners (ports/protocols, certificate ARN only - never certificate bodies or private keys), health-check configuration, registered instance count, creation time). Classic ELBs carry no AWS-assigned ARN, so the scanner synthesizes a partition-aware `arn:<partition>:elasticloadbalancing:<region>:<account>:loadbalancer/<name>` from the scan boundary (commercial, GovCloud, and China resolve; `arn:aws:` is never hardcoded) and uses it as the resource id and edge source. Relationships: load-balancer-to-instance (`aws_ec2_instance` by bare `i-` id, derived from `DescribeLoadBalancers`, a forward reference until an EC2 instance scanner exists), load-balancer-to-subnet (`aws_ec2_subnet` by bare `subnet-` id), load-balancer-to-security-group (`aws_ec2_security_group` by bare `sg-` id), load-balancer-to-VPC (`aws_ec2_vpc` by bare `vpc-` id), and load-balancer-to-certificate for an HTTPS/SSL listener (`aws_acm_certificate` for an `:acm:` `SSLCertificateId` ARN, `aws_iam_server_certificate` for an `:iam:` server-certificate ARN; the latter is a forward reference until an IAM server-certificate scanner exists). The scanner never calls `DescribeInstanceHealth`; live instance health is excluded and only an aggregate `instance_count` plus the registered-instance edges survive. The scanner never reads certificate bodies or private keys and never mutates any ELB resource (no Create/Delete/Register/Deregister/Attach/Detach/Modify/Set/Configure); the accepted SDK surface is `DescribeLoadBalancers` and `DescribeTags` only, proven by a reflective guard test on the SDK adapter `apiClient` interface. This is the Classic-ELB counterpart to the `elbv2` scanner; a claim is scanned by exactly one of the two. No redaction key required. |
| `elbv2` | Load balancers, listeners, listener rules, target groups, routing relationships. |
| `lambda` | Functions, aliases, event-source mappings, image URIs, execution roles, network joins, redacted environment values. |
| `licensemanager` | License configurations (`ListLicenseConfigurations`): identity (ARN, id, name), status, license counting dimension (vCPU/Instance/Core/Socket), configured and consumed license counts, hard-limit flag, license-rule and product-information counts, owner account id, license expiry, and the association count plus the sorted set of associated resource types. Resource associations (`ListAssociationsForLicenseConfiguration`, paged per configuration) record which AWS resources consume licenses. Relationship: configuration-applies-to-instance (`aws_ec2_instance` by bare `i-` id extracted from the association `ResourceArn`, a forward reference until an EC2 instance scanner exists), emitted only for `EC2_INSTANCE` associations whose ARN yields an instance id. `EC2_HOST`, `EC2_AMI`, `RDS`, and `SYSTEMS_MANAGER_MANAGED_INSTANCE` associations have no resolvable target node today, so they are recorded as configuration metadata (`association_count`, `associated_resource_types`) but never keyed as a dangling edge. No `arn:aws:` is hardcoded; the instance id is read from the ARN, never synthesized. Metadata only: the scanner never grants, checks out, checks in, or mutates a license, never reads a license entitlement token, access token, or usage record, and never reads license-rule or product-information match expressions; the accepted SDK surface (`ListLicenseConfigurations`, `ListAssociationsForLicenseConfiguration`, `ListTagsForResource`) excludes every checkout and mutation API by construction, proven by a reflection guard test on the adapter interface. |
| `lightsail` | Instances (name, ARN, blueprint, bundle, state, public/private IP, IPv6, static-IP flag, placement, SSH key name), managed relational databases (name, ARN, engine, version, state, blueprint/bundle, master database name and user name, endpoint host and port, public-accessibility and backup-retention flags, placement - never the master password or certificate body), load balancers (name, ARN, state, DNS name, protocol, instance port, public ports, IP address type, HTTPS-redirection flag, attached instance names), block-storage disks (name, ARN, state, path, size, IOPS, attachment flags, attached instance), and static IPs (name, ARN, address, attachment flag, attached instance). Relationships are Lightsail-internal: load-balancer-to-instance (targets `aws_lightsail_instance` by bare instance name from the instance-health summary), instance-to-disk (targets `aws_lightsail_disk` by bare disk name, sourced on the instance name in `Disk.AttachedTo`), and instance-to-static-IP (targets `aws_lightsail_static_ip` by bare static IP name, sourced on the instance name in `StaticIp.AttachedTo`). Every node resource_id and every edge join key is the bare Lightsail resource name, so the internal edges resolve the nodes this scanner publishes; resource ARNs come from the Lightsail API and are used directly, never synthesized or hardcoded. The scanner never creates, deletes, reboots, starts, stops, snapshots, attaches, or detaches a Lightsail resource, and never reads instance access details, default key-pair private keys, or database master passwords; the accepted SDK surface excludes those operations by construction, proven by a reflective guard test on the SDK adapter `apiClient` interface. |
| `location` | Amazon Location Service maps (ARN, name, map style data source, configured style, political view, custom layer names), place indexes (ARN, name, geocoding data source, intended use SingleUse/Storage), trackers (ARN, name, KMS key id, geospatial-query and EventBridge flags, position-filtering mode), geofence collections (ARN, name, KMS key id, geofence count - NOT the geofence geometries), and route calculators (ARN, name, routing data source). The `List*` entries report names only, so the adapter calls the matching `Describe*` to obtain each resource ARN (the join key), KMS reference, and remaining metadata; the Describe reads return tags inline so no separate tag call is made. Relationships: tracker-to-KMS-key and geofence-collection-to-KMS-key (targets `aws_kms_key` by the reported key identifier, `target_arn` set only when ARN-shaped, matching the KMS scanner's published key resource_id), and tracker-consumes-geofence-collection (from `ListTrackerConsumers`, whose reported consumer ARN is the geofence collection ARN the geofence-collection node also publishes as its resource_id, targets `aws_location_geofence_collection`). Maps, place indexes, and route calculators report no KMS key and no cross-service reference, so they are emitted as nodes only. Metadata only: the scanner reads only ListMaps/DescribeMap, ListPlaceIndexes/DescribePlaceIndex, ListTrackers/DescribeTracker/ListTrackerConsumers, ListGeofenceCollections/DescribeGeofenceCollection, and ListRouteCalculators/DescribeRouteCalculator; it NEVER reads or persists device positions or position history (ListDevicePositions, GetDevicePosition*, BatchGetDevicePosition), geofence geometries (ListGeofences, GetGeofence), place-search or geocoding results (SearchPlaceIndexFor*, GetPlace), route calculations (CalculateRoute, CalculateRouteMatrix), map tiles/glyphs/sprites/style descriptors, or API key material (ListKeys, DescribeKey), and never mutates Location Service state. The accepted SDK surface is `List`/`Describe`-only by construction, proven by a reflection guard test on the adapter interface that also blocks the device-position, geofence, place-search, route, map-tile, and key reads. No `ESHU_AWS_REDACTION_KEY` required. |
| `eks` | Clusters, node groups, add-ons, OIDC providers, IAM roles, network join evidence. |
| `elasticbeanstalk` | Applications (name, ARN, description, configuration template names, version labels), environments (name, ARN, environment id, status, tier name/type, platform ARN, solution stack, CNAME, endpoint URL, health, health status, deployed version label, template name), and application versions (label, status, source S3 bucket/key, source repository, build ARN). Relationships: environment-to-application, environment-to-VPC (from the `aws:ec2:vpc/VPCId` option setting, targets `aws_ec2_vpc`), environment-to-instance-profile (from `aws:autoscaling:launchconfiguration/IamInstanceProfile`, targets `aws_iam_instance_profile`), environment-to-service-role (from `aws:elasticbeanstalk:environment/ServiceRole`, targets `aws_iam_role`), environment-to-load-balancer (from DescribeEnvironmentResources; an ELBv2 ALB/NLB ARN targets `aws_elbv2_load_balancer` with a real `target_arn`, while a bare Classic Load Balancer name falls back to `aws_resource` without a fabricated ARN), environment-to-Auto-Scaling-group (targets `aws_autoscaling_group`), environment-to-launch-template (targets `aws_ec2_launch_template`), and environment-to-application-version. Every environment option-setting value routes through the shared redact library: option names and namespaces are kept, values never are, because they can carry secret environment variable values. The scanner never reads application-version source bundle object contents or environment-info presigned-URL bundles, and never mutates any Elastic Beanstalk resource (no Create/Update/Delete/Terminate/RebuildEnvironment/SwapEnvironmentCNAMEs); the accepted SDK surface excludes those operations by construction. Requires `ESHU_AWS_REDACTION_KEY`. |
| `emr` | EMR on EC2 clusters (running + recently terminated), uniform instance groups, instance fleets, security configurations (name only), EMR Serverless applications, EMR Studios, and Studio session mappings under one `service_kind`; resource types distinguish the surfaces. Relationships: cluster-to-subnet/security-group/IAM-role/instance-profile/security-configuration/KMS-key and cluster-to-instance-group/fleet; application-to-subnet/security-group/KMS-key; studio-to-VPC/subnet/security-group/IAM-role/KMS-key and studio-to-session-mapping. The cluster-to-VPC and application-to-VPC joins are derived from subnet membership downstream because the EMR cluster and EMR Serverless APIs do not report a VPC id; EMR Studio reports its VPC id directly. No step command lines (`Args`), bootstrap action script bodies, security configuration policy bodies, or EMR Serverless job-run `SparkSubmit.entryPointArguments`: the scanner never calls ListSteps, DescribeStep, ListBootstrapActions, DescribeSecurityConfiguration, or any EMR Serverless job-run reader. |
| `batch` | Compute environments (name, type, state, service role ARN, orchestration type, ECS/EKS cluster ARN, instance-profile role, subnets, security groups, launch template), job queues (name, state, priority, compute-environment order), job definitions (name, type, container image URI, job/execution IAM role - never the container command list, never environment values in clear text), scheduling policies (name and ARN only - never fair-share weight state), and recent active jobs (id, status, job-definition reference - never job parameters or container overrides). Relationships: job-queue-to-compute-environment, compute-environment-to-subnet, compute-environment-to-security-group, compute-environment-to-launch-template, compute-environment-to-IAM-role, job-definition-to-IAM-role, job-definition-to-container-image, and job-definition-to-secret-reference (Secrets Manager / SSM ARN ref only). The compute-environment-to-VPC join is reached transitively through the subnet edge. The scanner never submits, cancels, or terminates jobs, never registers or deregisters job definitions, and never mutates any Batch resource; the adapter read surface excludes those operations by construction. Container environment values route through the shared redact library. Requires `ESHU_AWS_REDACTION_KEY`. |
| `autoscaling` | EC2 Auto Scaling groups (name, ARN, min/max/desired capacity, Availability Zones, health-check type and grace period, status, capacity-rebalance, default cooldown, termination policies, service-linked role ARN, launch template/configuration reference, subnets, target groups, load balancer names), launch configurations (name and ARN only - never UserData, which can hold bootstrap secrets, and never block device mappings, security groups, key name, or instance profile), scaling policies (name, ARN, type, adjustment type, enabled, owning group - never step adjustments, target-tracking configuration, or CloudWatch alarm bindings), lifecycle hooks (name, transition, default result, timeouts, notification target ARN, role ARN - never NotificationMetadata free-form data), and scheduled actions (name, ARN, recurrence, time zone, target capacity, start/end time). Relationships: group-to-launch-template (`aws_ec2_launch_template` by launch-template ID, falling back to name), group-to-launch-configuration, group-to-subnet (`aws_ec2_subnet` by bare subnet ID parsed from `VPCZoneIdentifier`), group-to-target-group (`aws_elbv2_target_group` by target-group ARN), group-to-service-linked-IAM-role (`aws_iam_role` by role ARN), and policy/lifecycle-hook/scheduled-action-to-group (`aws_autoscaling_group` by group name). The Auto Scaling group resource_id is the bare group name, which closes the CodeDeploy `codedeploy_deployment_group_targets_auto_scaling_group` dangling edges. The scanner never creates, updates, or deletes an Auto Scaling resource, never sets desired capacity, and never terminates instances; the adapter read surface excludes those operations by construction, proven by a reflective guard test. No redaction key required. |
| `route53` | Hosted zones and A, AAAA, CNAME, ALIAS DNS record facts. |
| `route53resolver` | DNS resolution and DNS Firewall metadata, distinct from the `route53` hosted-zone scanner. Resolver endpoints (id, name, direction INBOUND/OUTBOUND, status, IP count), resolver rules (name, domain name, rule type FORWARD/SYSTEM/RECURSIVE - never forwarded target query data), resolver rule associations, DNS Firewall rule groups (name, AWS-reported rule count - NEVER the rule bodies), DNS Firewall domain lists (name, AWS-reported domain count - NEVER the domain entries), DNS Firewall rule group associations, and Resolver query log configurations (destination ARN only - never query log records). Relationships: endpoint-to-VPC and endpoint-to-subnet, rule-to-endpoint, rule-association-to-VPC and rule-association-to-rule, firewall-rule-group-association-to-VPC and firewall-rule-group-association-to-rule-group. Firewall rule and domain counts come from the per-resource Get reads, which return only metadata and an aggregate count; the scanner never calls ListFirewallDomains or ListFirewallRules and the adapter read surface excludes them by construction. Resolver endpoint IP address strings are never persisted; only the subnet placement and IP count survive. The scanner never mutates any resource. Requires no `ESHU_AWS_REDACTION_KEY`. |
| `sqs`, `sns`, `eventbridge` | Queue/topic/bus metadata and ARN-addressable relationships. |
| `guardduty` | Detectors, member accounts, filter names, publishing destinations, threat intel/IP set metadata, and aggregate finding counts. |
| `inspector2` | Account scan status, enabled scan features (EC2, ECR, Lambda, Lambda code) carried as account attributes, member accounts (org-admin view), findings filter non-criteria identity (name, action, owner ID), and CIS scan configuration metadata with member-to-administrator and CIS-configuration-to-target-account relationships. No finding details, no filter criteria expressions, no CIS scan results. |
| `macie2` | Highest-redaction scanner in the collector. Macie account session status (enabled/paused, finding publishing frequency, service-linked role ARN), member accounts (org-admin view, email-free), classification job metadata (id, name, type, status, and an aggregate bucket-criteria summary of `target_bucket_count` + `target_account_count` + `uses_bucket_criteria` — never the bucket list or criteria expressions), allow list identities (id, name only), custom data identifier identities (id, name only), findings filter identities (id, name, action only), and aggregate finding counts by severity carried on the session resource. Relationship: member-to-administrator (targets the administrator account's Macie session). Never persists sensitive-data findings (the PII detection results), custom data identifier regex bodies (they are the PII patterns), allow list contents, findings filter criteria, or classification-job bucket criteria. The scanner never calls `GetSensitiveDataOccurrences`, `GetSensitiveDataOccurrencesAvailability`, `GetFindings`, `ListFindings`, `GetCustomDataIdentifier`, `GetAllowList`, `GetFindingsFilter`, `DescribeClassificationJob`, or `DescribeBuckets`; the accepted SDK surface excludes them by construction, proven by a reflective guard test on the SDK adapter `apiClient` interface and struct-reflection tests on the scanner-owned types. |
| `detective` | Amazon Detective behavior graphs (ARN, creation time) and graph member accounts (account id, administrator id, membership status, invitation type, enabled data-source package names; email-free). Relationships: graph-to-member-account (targets `aws_organizations_account` by bare 12-digit account id) and graph-to-GuardDuty-detector (targets `aws_guardduty_detector` by bare detector id, emitted only when a detector id is resolvable for the graph and omitted otherwise so the edge never dangles). The graph node's `resource_id` is the graph ARN, and every outgoing edge is sourced on that ARN; graph ARNs are passed through unchanged so synthesized identities inherit the graph's partition. The `DETECTIVE_CORE` package (which ingests GuardDuty) is recorded as a `sources_guardduty_data` graph attribute. Reads only `ListGraphs`, `ListMembers`, and `ListTagsForResource`; never reads investigations, finding groups, indicators, per-member usage volume, or member contact emails, and never mutates a Detective resource. The accepted SDK surface excludes every investigation, indicator, datasource-detail, and mutation API by construction, proven by reflective guard tests on the scanner `Client` port and the SDK adapter `apiClient` interface. |
| `s3` | Bucket metadata and server-access-log target bucket relationships. |
| `rds` | DB instances, clusters, subnet groups, and reported security/KMS/role/group relationships. |
| `docdb` | DocumentDB DB clusters, cluster instances, cluster parameter groups (name + family + parameter count only - NOT parameter values), cluster snapshot metadata, subnet groups, global clusters, and event subscription metadata with cluster-to-VPC, cluster-to-subnet-group, cluster-to-KMS-key, instance-to-cluster, and global-cluster-to-cluster relationships. No master user passwords, master user secrets, database document contents, collections, indexes, cluster parameter values, or snapshot contents. |
| `ds` | AWS Directory Service directories across all three types (AWS Managed Microsoft AD, Simple AD, AD Connector) including directory type, edition, size, stage/status, short name, alias, access URL, single-sign-on state, and VPC placement; trust relationships (id, direction, type, state, remote domain name, selective auth); shared-directory invitations (owner account, owner directory, shared account, shared directory, share method, share status); and LDAPS client-side settings status (Managed Microsoft AD only). The directory `resource_id` is the bare directory ID (`d-xxxxxxxxxx`), which is the join key the FSx scanner's AD-directory edges target. Relationships: directory-to-VPC (bare `vpc-id`, targets `aws_ec2_vpc`), directory-to-subnet (bare `subnet-id`, targets `aws_ec2_subnet`), trust-to-directory (bare directory id, targets `aws_ds_directory`), shared-directory-to-owner-directory (bare owner directory id, targets `aws_ds_directory`), and shared-directory-to-owner-account (bare 12-digit account id, targets `aws_account`, never a synthesized ARN). No directory admin passwords, RADIUS shared secrets, or AD Connector service-account credentials; the scanner never calls ResetUserPassword or any mutation API. |
| `neptune` | Both Neptune (provisioned) and Neptune Analytics share `service_kind=neptune`; resource types distinguish the two surfaces. Neptune (provisioned): DB clusters, cluster instances, cluster parameter groups (name + family only - NOT parameter values), cluster snapshot metadata, subnet groups, and global clusters. Neptune Analytics: graphs (name, status, vector-search embedding dimension, provisioning shape) and graph snapshot metadata. Relationships: cluster-to-VPC (derived from the subnet group VPC), cluster-to-subnet-group, cluster-to-KMS-key, cluster-to-IAM-role, instance-to-cluster, global-cluster-to-cluster, and graph-to-KMS-key. No master user passwords, master user secrets, cluster parameter values, graph vertex or edge contents, `ExecuteQuery` results, or snapshot contents. The Neptune Analytics graph data-plane (ExecuteQuery/CancelQuery/GetQuery/ListQueries) is unreachable from the scanner by interface construction. |
| `redshift` | Provisioned clusters, cluster parameter groups, cluster subnet groups, cluster snapshot metadata, scheduled action metadata, Serverless namespaces, Serverless workgroups, and reported VPC/subnet/security-group/KMS/IAM/snapshot/scheduled-action/namespace-workgroup relationships. Provisioned and Serverless share `service_kind=redshift`; resource types distinguish the two surfaces. |
| `dynamodb`, `cloudwatchlogs` | Table or log-group metadata and KMS relationships. |
| `efs` | File system metadata (performance mode, throughput mode, encryption status, lifecycle policy transition summary), access points, mount targets, and replication configurations with mount-target-to-subnet, mount-target-to-security-group, file-system-to-KMS-key, access-point-to-file-system, and replication-to-target-file-system relationships. No NFS file system policy bodies and no file contents. |
| `fsx` | File system metadata across all flavors (Windows File Server, Lustre, NetApp ONTAP, OpenZFS) including file system type, deployment type, storage type, storage and throughput capacity, lifecycle, and AWS Managed Microsoft AD directory ID; backups (type, lifecycle, size, source file system, KMS key); volume snapshots; NetApp ONTAP storage virtual machines; and NetApp ONTAP and OpenZFS volumes. Relationships: file-system-to-VPC, file-system-to-subnet, file-system-to-KMS-key, file-system-to-AD-directory, backup-to-file-system, SVM-to-file-system, SVM-to-AD-directory, volume-to-SVM, and volume-to-file-system. No Active Directory self-managed credentials (Windows/ONTAP self-managed AD password, service-account user name, administrators group, DNS server IPs, or domain-join secret ARN), no ONTAP fsxadmin password, no SVM admin password, and no file contents. |
| `datasync` | Transfer task metadata (name, ARN, status, source/destination location ARNs, schedule expression, task mode, CloudWatch log group ARN), transfer location metadata (ARN, type S3/EFS/FSx/NFS/SMB/object-storage/HDFS/Azure Blob, and a host/path-only URI), and agent metadata (ARN, status, endpoint type, platform version). Relationships: task-to-source-location and task-to-destination-location (targets `aws_datasync_location` by the API-reported location ARN), task-to-CloudWatch-log-group (targets `aws_cloudwatch_logs_log_group` by the log-group ARN with its trailing `:*` trimmed), location-to-S3-bucket (targets `aws_s3_bucket` by the synthesized `arn:<partition>:s3:::<bucket>`), location-to-EFS-file-system (targets `aws_efs_file_system` by the synthesized file-system ARN), location-to-FSx-file-system (targets `aws_fsx_file_system`; FSx for NetApp ONTAP locations report the file-system ARN directly, the other FSx flavors synthesize it), and location-to-IAM-role (targets `aws_iam_role` by the API-reported S3 bucket-access or EFS file-system-access role ARN). Task, location, and agent ARNs come from the API and are used directly; the S3/EFS/FSx backing-storage ARNs are synthesized partition-aware so the storage edges join in GovCloud and China. No transferred object or record contents, object-storage access keys, server certificates, SMB/object-storage passwords, or include/exclude filter patterns; the scanner never starts, cancels, or mutates a task, location, or agent, and the SDK adapter read surface excludes every mutation and task-execution read by construction. |
| `datazone` | Amazon DataZone governance metadata: domains (id, ARN, status, the encryption KMS key reference, the domain execution and service IAM role references, portal URL, timestamps), projects (id, parent domain, status, category), environments (id, parent domain/project, provider, blueprint/profile ids, target AWS account/region, status), and data sources (id, parent domain/project/environment, source type, enablement). Relationships: domain-to-KMS-key (targets `aws_kms_key` by the reported key id/ARN/alias; `target_arn` set only for ARN-shaped identifiers), domain-to-IAM-role (targets `aws_iam_role` by the execution-role and service-role ARNs, emitted only for genuine `:iam::…:role/…` ARNs), project-in-domain / environment-in-domain / data-source-in-domain (internal, targets `aws_datazone_domain` by the domain id), data-source-to-Glue-database (targets `aws_glue_database` by the Glue database name from the Glue run configuration), and data-source-to-Redshift-cluster (targets `aws_redshift_cluster` by the partition-aware cluster ARN synthesized from the cluster name plus the config account/region, so the edge joins in GovCloud, China, and cross-account configs). Redshift Serverless workgroups carry an opaque published ARN that cannot be synthesized from the workgroup name, so they are skipped rather than dangled. Metadata only: reads ListDomains, GetDomain, ListProjects, ListEnvironments, ListDataSources, GetDataSource, and ListTagsForResource; never reads or persists business glossaries, glossary terms, catalog asset content, listings, subscriptions, time-series data, lineage, relational filter expressions, or access credentials, and never mutates DataZone state. The accepted SDK surface is List reads plus the GetDomain/GetDataSource describe reads only by construction, proven by a reflection guard test on the adapter `apiClient` interface. |
| `dms` | Database Migration Service replication instances (class, engine version, allocated storage, Multi-AZ, public-accessibility, status, availability zone, network type, KMS key, subnet-group/VPC/subnet/security-group references), replication subnet groups (description, status, VPC id, member subnet ids), endpoints (type source/target, engine name, SSL mode, status, database name, port; resolvable data-store and secret references only), and replication tasks (migration type, status, source/target endpoint and replication-instance ARNs). Relationships: instance-to-subnet-group (targets `aws_dms_replication_subnet_group` by the subnet-group identifier), instance-to-subnet and subnet-group-to-subnet (targets `aws_ec2_subnet` by bare `subnet-id`), instance-to-security-group (targets `aws_ec2_security_group` by bare `sg-id`), subnet-group-to-VPC (targets `aws_ec2_vpc` by bare `vpc-id`), instance-to-KMS and endpoint-to-KMS (targets `aws_kms_key` by the reported key id/ARN), endpoint-to-S3-bucket (targets `aws_s3_bucket` by the synthesized `arn:<partition>:s3:::<bucket>`), endpoint-to-Kinesis-stream (targets `aws_kinesis_data_stream` by the reported stream ARN), endpoint-to-Secrets-Manager-secret (targets `aws_secretsmanager_secret` by the reported secret id/ARN; the secret value is never read), and task-to-source-endpoint/task-to-target-endpoint/task-to-replication-instance (targets `aws_dms_endpoint`/`aws_dms_replication_instance` by the ARN the task reports). RDS and Redshift endpoint targets are not emitted because DMS reports only a server hostname (RDS) or S3 staging config (Redshift), which do not resolve to those scanners' published resource_ids. Metadata only: never reads migrated rows, endpoint passwords, usernames, server names used as credentials, connection attributes, external table definitions, SSL key material, task settings, or table-mapping bodies, and never tests connections, refreshes schemas, reloads tables, starts/stops tasks, or mutates DMS state; the SDK adapter read surface excludes every data-plane, connection-test, schema-refresh, and mutation API by construction. |
| `cloudwatch` | Metric alarms, composite alarms, dashboards (name + last modified only), Contributor Insights rules (name + state only), and metric streams with alarm-to-SNS-topic, composite-alarm-to-child-alarm, metric-stream-to-Firehose, and alarm-to-metric (dimension summary) relationships. No dashboard body JSON, no Contributor Insights rule definitions, no metric data points. Customer-tag-named alarm dimensions are routed through the shared redact library. |
| `cloudfront` | Distribution metadata plus ACM certificate and WAF web ACL relationships. |
| `globalaccelerator` | Accelerator (name, ARN, status, IP address type, DNS name, dual-stack DNS name, static IP sets), listener (port ranges, protocol, client affinity), endpoint group (region, traffic dial percentage, health check protocol/path/port/interval, threshold, port overrides), and endpoint (endpoint id reference, weight, client IP preservation, health state) metadata. Relationships: accelerator-to-listener, listener-to-endpoint-group, endpoint-group-to-endpoint, and endpoint-to-target. The endpoint target edge is typed from the reported endpoint id: `aws_elbv2_load_balancer` for an `:elasticloadbalancing:` ARN, `aws_vpc_elastic_ip` for an `eipalloc-` allocation id, `aws_ec2_instance` for an `i-` instance id, and `aws_resource` otherwise; `target_arn` is set only when the endpoint id is ARN-shaped. Global Accelerator is a global-endpoint service whose control plane is reachable only in us-west-2, so schedule the claim for us-west-2; the SDK adapter pins its client region there. The scanner never mutates a Global Accelerator resource; the adapter read surface excludes Create/Update/Delete, BYOIP advertise/withdraw, and custom-routing traffic allow/deny by construction, proven by a reflective guard test on the SDK adapter `apiClient` interface. |
| `grafana` | Amazon Managed Grafana workspaces (id, synthesized partition-aware ARN, name, description, status, Grafana version, console endpoint, account-access type, permission type CUSTOMER_MANAGED/SERVICE_MANAGED, configured AWS data-source type enums e.g. PROMETHEUS/CLOUDWATCH, notification-destination type enums, authentication provider names e.g. AWS_SSO/SAML, created/modified timestamps). Relationships: workspace-to-IAM-role (`aws_iam_role` by the reported `workspaceRoleArn`, emitted only when ARN-shaped), workspace-to-subnet (`aws_ec2_subnet` by bare subnet id from `vpcConfiguration`), and workspace-to-security-group (`aws_ec2_security_group` by bare group id from `vpcConfiguration`); VPC edges de-duplicate repeated ids. Managed Grafana reports no workspace ARN, so the scanner synthesizes `arn:<partition>:grafana:<region>:<account>:/workspaces/<id>` via `awscloud.PartitionForBoundary` so GovCloud and China workspaces join correctly. Metadata only: never reads dashboards, panels, alert rules, or query results, never reads SAML or IAM Identity Center authentication configuration, and never persists a workspace API key, service-account token, or any credential; the accepted SDK surface (`ListWorkspaces`, `DescribeWorkspace`, `ListTagsForResource`) excludes `DescribeWorkspaceAuthentication`, the API-key and service-account-token APIs, `AssociateLicense`, and every Create/Update/Delete mutation by construction, proven by a reflection guard test on the adapter `apiClient` interface. |
| `wafv2` | Web ACL, customer rule group, IP set (id, name, IP version, address count only), and regex pattern set (id, name, pattern count only) metadata for both the REGIONAL and global CLOUDFRONT scope, plus managed rule set references (vendor + name). Relationships: web-ACL-to-protected-resource (ALB, API Gateway stage, AppSync, App Runner service, Cognito user pool, Amplify, Verified Access), web-ACL-to-rule-group, web-ACL-to-IP-set, and web-ACL-to-regex-pattern-set. CloudFront associations are recorded by the `cloudfront` scanner. No IP set address lists, regex pattern bodies, or rule `Statement` bodies (`AndStatement`/`OrStatement`/`NotStatement`/`ByteMatchStatement` search strings) are read or persisted. WAF Classic (v1) is out of scope by construction; the scanner imports only the WAFv2 SDK. |
| `fis` | AWS Fault Injection Service (FIS) experiment templates (id, ARN, name from the Name tag, description, execution IAM role ARN, fault-injection action ids, target keys and FIS resource-type selectors, CloudWatch and S3 logging destinations, stop-condition count, lifecycle timestamps). Relationships, each keyed to the identity the target scanner publishes: template-to-IAM-role (`aws_iam_role` by the reported execution role ARN), template-to-EC2-instance (`aws_ec2_instance` by the bare `i-` id extracted from the explicit target ARN, with `target_arn` left empty so the bare-id join key is not paired with an ARN and the full ARN preserved in the `instance_arn` edge attribute; forward-reference target type, no EC2 instance scanner yet), template-to-ECS-cluster (`aws_ecs_cluster` by the cluster ARN), template-to-RDS-DB-instance (`aws_rds_db_instance` by the `db:` ARN) and template-to-RDS-DB-cluster (`aws_rds_db_cluster` by the `cluster:` ARN), template-logs-to-CloudWatch-log-group (`aws_cloudwatch_logs_log_group` by the reported log group ARN with its trailing `:*` wildcard trimmed), template-logs-to-S3 (`aws_s3_bucket` by the partition-aware synthesized `arn:<partition>:s3:::<bucket>` the S3 scanner publishes), and template-stops-on-CloudWatch-alarm (`aws_cloudwatch_alarm` by the stop-condition alarm ARN). Target resources selected only by tag or filter, and target ARNs from resource families this scanner does not model, are skipped rather than dangled. Reads `ListExperimentTemplates`, `GetExperimentTemplate`, and `ListTagsForResource` only; the SDK adapter excludes `StartExperiment`, `StopExperiment`, every experiment-run read (`GetExperiment`, `ListExperiments`, `ListExperimentResolvedTargets`), and all Create/Update/Delete/Tag mutations by construction, proven by a reflective guard test on the adapter `apiClient` interface. Metadata only: never action parameter values, target filter values, target-tag selectors, or experiment run results. No `ESHU_AWS_REDACTION_KEY` required. |
| `fms` | AWS Firewall Manager policy metadata (ARN, id, name, governing security service type WAF/WAFV2/SECURITY_GROUPS_COMMON/SECURITY_GROUPS_CONTENT_AUDIT/SECURITY_GROUPS_USAGE_AUDIT/NETWORK_FIREWALL/SHIELD_ADVANCED/DNS_FIREWALL/THIRD_PARTY_FIREWALL/NETWORK_ACL_COMMON, in-scope AWS resource type label, remediation flag, delete-unused-managed-resources flag, admin-scope status). Relationship: policy-to-member-account (`fms_policy_applies_to_account`, keyed on the bare 12-digit Organizations member account id resolved from `ListComplianceStatus`, targets `aws_organizations_account` with no synthesized ARN, so it joins the node the `organizations` scanner publishes). The governing security service type and in-scope resource type are recorded as labels on the policy; the policy does not own those resource nodes. FMS is an organization-wide control plane reachable only from the FMS administrator account, so one claim scans that account's policy fleet. Policy ARNs are used verbatim from the API, so the GovCloud and China partitions survive. No policy rule payloads: the scanner reads only `ListPolicies` and `ListComplianceStatus`; `GetPolicy` (which returns the SecurityServicePolicyData managed service data document) and every FMS mutation API are excluded from the SDK adapter read surface by construction, proven by a reflective guard test. |
| `acm` | Public ACM certificate metadata (ARN, domain name, SANs, status, type, issuer, validity, key and signature algorithms) and certificate-to-using-resource relationships derived from ACM-reported in-use-by ARNs (ELB v2, CloudFront, API Gateway, AppSync, App Runner, and other ARN-shaped targets). No certificate body PEM, no private key material, no `GetCertificate` calls, no `ExportCertificate` calls; ACM Private CA is out of scope. |
| `appmesh` | Service mesh, virtual service, virtual node, virtual router, route, virtual gateway, and gateway route metadata with virtual-service-to-mesh, virtual-node-to-backend-virtual-service, route-to-virtual-router, virtual-gateway-to-mesh, virtual-node-to-ACM-Private-CA-certificate-authority (client TLS validation trust), and virtual-node-to-Cloud-Map-service / DNS-hostname relationships. The client TLS trust edge keys on the ACM Private CA (acm-pca) certificate authority ARN App Mesh reports and targets `aws_acmpca_certificate_authority`; there is no ACM Private CA scanner yet, so the target type is forward-looking. Route shape (path prefix, method, header names and match type) is recorded; sensitive HTTP header match values (Authorization, Cookie, X-Api-Key shaped) are routed through the shared redact library. No client TLS validation certificate bodies, certificate chains, or SDS secret names are read or persisted; client TLS validation is reduced to ACM Private CA certificate authority ARN references. No App Mesh mutation APIs. Requires `ESHU_AWS_REDACTION_KEY`. |
| `apprunner` | Services (name, ARN, status, source-configuration type IMAGE/REPOSITORY, image identifier or code repository URL, autoscaling configuration ARN, observability configuration ARN, health check, instance/access role ARNs, KMS key, VPC connector ARN, egress type, public-accessibility), connections (name, provider type, status), automatic scaling configurations (name, revision, min/max size, max concurrency, default flag), observability configurations (name, revision, trace vendor), VPC connectors (name, revision, subnets, security groups), and VPC ingress connections (name, status, domain name, service ARN, VPC endpoint and VPC id). Relationships: service-to-container-image (ECR image URI, targets `container_image`), service-to-App-Runner-connection, service-to-IAM-role (instance and ECR access role), service-to-KMS-key, service-to-VPC-connector, service-to-autoscaling-configuration, service-to-observability-configuration, service-to-secret-reference (Secrets Manager / SSM ARN ref only), vpc-connector-to-subnet, vpc-connector-to-security-group, and vpc-ingress-connection-to-service. The service `resource_id` is the service ARN, which closes the ACM and WAFv2 dangling edges that target `aws_apprunner_service` by service ARN. Metadata only: source repository credentials and runtime environment-variable values are never read; environment-variable NAMES are kept and runtime secrets are recorded as ARN reference edges only. The scanner never creates, deletes, updates, pauses, resumes, or deploys a service, never deletes a connection, and never mutates any App Runner resource; the SDK adapter read surface excludes those operations by construction, proven by a reflective guard test. No redaction key required. |
| `acm` | Public ACM certificate metadata (ARN, domain name, SANs, status, type, issuer, validity, key and signature algorithms) and certificate-to-using-resource relationships derived from ACM-reported in-use-by ARNs (ELB v2, CloudFront, API Gateway, AppSync, App Runner, and other ARN-shaped targets). No certificate body PEM, no private key material, no `GetCertificate` calls, no `ExportCertificate` calls. ACM Private CA is a separate scanner under `service_kind=acm-pca`. |
| `acm-pca` | ACM Private CA certificate authority metadata (CA ARN, owner account, type ROOT/SUBORDINATE, status, serial, usage mode, key storage security standard, key algorithm, signing algorithm, subject common name, validity, and CRL/OCSP revocation flags). The CA `resource_id` is the CA ARN, which is the join key App Mesh virtual-node client TLS trust edges target (`aws_acmpca_certificate_authority`). Relationships are ARN-driven and conditional: CA-to-KMS-key (target_type `aws_kms_key`, emitted only when AWS reports an ARN-shaped key), subordinate-CA-to-parent-CA (target_type `aws_acmpca_certificate_authority`, emitted only for a SUBORDINATE CA reporting an ARN-shaped parent), and CA-to-S3-CRL-bucket (target_type `aws_s3_bucket`, emitted only when CRL publishing names a bucket). The scanner never synthesizes a KMS, parent, or bucket identity; `DescribeCertificateAuthority` reports no KMS key or parent ARN, so those edges stay unemitted for the standard metadata response. No certificate issuance, no `GetCertificate`, no `GetCertificateAuthorityCsr` (CSR body), no `GetCertificateAuthorityCertificate` (certificate chain body), no private key material, and no CA mutation (Create/Delete/Update/Restore/Import/Revoke); the accepted SDK surface excludes them by construction, proven by a reflective guard test on the SDK adapter `apiClient` interface. Metadata only. |
| `appmesh` | Service mesh, virtual service, virtual node, virtual router, route, virtual gateway, and gateway route metadata with virtual-service-to-mesh, virtual-node-to-backend-virtual-service, route-to-virtual-router, virtual-gateway-to-mesh, virtual-node-to-ACM-Private-CA-certificate-authority (client TLS validation trust), and virtual-node-to-Cloud-Map-service / DNS-hostname relationships. The client TLS trust edge keys on the ACM Private CA (acm-pca) certificate authority ARN App Mesh reports and targets `aws_acmpca_certificate_authority`; the `acm-pca` scanner publishes certificate authority resources keyed by that same CA ARN, so the edge resolves. Route shape (path prefix, method, header names and match type) is recorded; sensitive HTTP header match values (Authorization, Cookie, X-Api-Key shaped) are routed through the shared redact library. No client TLS validation certificate bodies, certificate chains, or SDS secret names are read or persisted; client TLS validation is reduced to ACM Private CA certificate authority ARN references. No App Mesh mutation APIs. Requires `ESHU_AWS_REDACTION_KEY`. |
| `cloudtrail` | Trail (multi-region and per-region), Lake event data store, channel, and Lake dashboard configuration metadata with trail-to-S3-bucket, trail-to-CloudWatch-Logs, trail-to-KMS-key, trail-to-SNS-topic, and event-data-store-to-KMS-key relationships. Event selectors are summarized as counts only; CloudTrail event payloads, Lake query strings, Lake query results, and dashboard widget query SQL are never read or persisted. |
| `cloudformation` | Stack (active and recently deleted), stack set, change set (metadata only), drift detection result (status + per-status counts), stack-set instance, and registered extension type metadata with stack-to-resource-type (from ListStackResources), stack-set-to-instance, stack-to-IAM-role, and stack-to-S3-template-URL relationships. Highest template-body redaction surface in the collector: template bodies (`GetTemplate`/`GetTemplateSummary`), parameter values (including NoEcho and SSM-resolved values, only keys are kept), change-set bodies (`DescribeChangeSet`), stack policies (`GetStackPolicy`), and drift property documents are never read or persisted. Secret-like stack output values are routed through the shared redact library by output key; the stack-set `TemplateBody` is never carried. Requires `ESHU_AWS_REDACTION_KEY`. |
| `apigateway` | REST, HTTP, WebSocket, stage, custom-domain, mapping, access-log, ACM, and integration metadata. |
| `apigatewayv2` | Dedicated HTTP and WebSocket (API Gateway v2) coverage: API (id, name, protocol type, endpoint, disable-execute-api-endpoint), stage (name, auto-deploy), route (route key, route id, target, authorization summary - never request models or parameter mappings), integration (id, type, backend URI/target, connection metadata - never request/response templates, parameter mappings, or the integration credential ARN), authorizer (name, type, identity sources, JWT issuer/audience - never the Lambda authorizer invocation URI, the authorizer credential ARN, or the identity validation expression), custom domain (status, endpoint types, certificate ARNs, API mappings), and VPC link (name, status, subnet ids, security group ids). Relationships: API-to-stage, API-to-route, route-to-integration (derived from the `integrations/<id>` target), integration-to-Lambda-function (joined by function ARN), integration-to-HTTP-endpoint, integration-to-VPC-link, API-to-Cognito-user-pool (joined by the bare pool id parsed from the JWT issuer URL; a non-Cognito issuer becomes an `api-uses-jwt-issuer` edge instead of dangling), domain-to-ACM-certificate (joined by certificate ARN), domain-to-API, and VPC-link-to-subnet/security-group (joined by bare EC2 ids). The classic REST (v1) surface is owned by the `apigateway` scanner. No request/response mapping templates, route request models, authorizer secrets, Lambda authorizer payloads, or stage variable values are read or persisted; the SDK adapter cannot reach ExportApi, GetIntegrationResponse(s), GetRouteResponse(s), GetModel(s), GetModelTemplate, or any mutation API, and the scanner-owned types have no field for those bodies. |
| `appsync` | GraphQL API (id, name, authentication type, log config, X-Ray status, WAF ACL ref), data source (name, type, backing-resource target - never inline credentials or HTTP authorization config), resolver (type name + field name + kind + data-source name + runtime), pipeline function (name, data-source name, runtime), schema metadata (creation status + type count), and API key metadata (id, description, expiry) for each API. Relationships: API-to-data-source, resolver-to-data-source, function-to-data-source, data-source-to-Lambda/DynamoDB-table/OpenSearch-domain/HTTP-endpoint/RDS-cluster, API-to-Cognito-user-pool (joined by bare pool id), and API-to-OIDC-issuer. The schema SDL body (exposes the data model and PII field names), resolver request/response mapping templates (VTL or JS, often containing inline auth conditions), pipeline function code bodies, and API key values are never read or persisted; the SDK adapter cannot reach EvaluateMappingTemplate, EvaluateCode, GetIntrospectionSchema, StartSchemaCreation, or any mutation API, and the scanner-owned types have no field for those bodies. |
| `secretsmanager`, `ssm` | Secret or parameter metadata with KMS relationships; no secret/parameter values. |
| `athena` | Workgroup, data catalog, prepared-statement, and named-query metadata plus workgroup-to-S3-result-bucket, workgroup-to-KMS-key, prepared-statement-to-workgroup, and named-query-to-workgroup relationships. No SQL bodies, query results, query result location object contents, or query history strings. |
| `securityhub` | Hub configuration, enabled standards, controls, member accounts, action targets, insight summaries, and aggregate finding counts; no finding bodies or insight filters. |
| `shield` | AWS Shield Advanced protections (id, ARN, name, protected resource ARN) and the per-account subscription summary (state, auto-renew). Relationship: protection-to-protected-resource, typed from the protected resource ARN's service segment - `aws_elbv2_load_balancer` for `:elasticloadbalancing:` (keyed by the LB ARN), `aws_cloudfront_distribution` for `:cloudfront:` (keyed by the distribution ARN), `aws_globalaccelerator_accelerator` for `:globalaccelerator:` (keyed by the accelerator ARN), `aws_vpc_elastic_ip` for an `:eip-allocation/eipalloc-...` ARN (keyed by the bare `eipalloc-` allocation id, no `target_arn`), and `aws_route53_hosted_zone` for a `:hostedzone/Z...` ARN (keyed by the bare hosted-zone id, no `target_arn`). A protected ARN whose service has no canonical Eshu resource family is skipped rather than emitting an untyped edge. The protected ARN comes from the API already partition-correct and is used directly, so GovCloud and China protections join the real target node. Shield is a global service: one claim per account observes every protection and the single subscription regardless of region, and the SDK adapter pins its client region to us-east-1. Metadata only: subscription limits, time commitment, start/end times, proactive engagement status, and all other billing detail are never read. The scanner never mutates a Shield resource; the SDK adapter read surface excludes Create/Delete/Update protection and subscription, DRT/health-check association, and proactive-engagement operations by construction, proven by a reflective guard test on the adapter `apiClient` interface. |
| `signer` | AWS Signer (code-signing) signing profiles (ARN, name, profile version, profile version ARN, bound platform id and display name, status, signature validity period, resolved signing image format, signing-parameter NAMES only, ACM certificate ARN reference) and signing platforms (bare platform id, display name, category, signing target, partner, maximum signable size, revocation-supported flag). Relationships: profile-to-ACM-certificate (`target_type=aws_acm_certificate`, keyed by the signing-material certificate ARN, which matches the `acm` scanner's published certificate `resource_id`; emitted only for an ARN-shaped value, `target_arn` set, skipped for a non-ARN identifier whose raw value is still kept on the profile node) and profile-to-signing-platform (`target_type=aws_signer_signing_platform`, an internal edge keyed by the bare platform id this scanner also publishes as a signing-platform `resource_id`, no `target_arn` because Signer platforms carry no ARN). No S3 source/destination, KMS, or Lambda code-signing-config edge is emitted because signing profiles do not report those dependencies (an S3 source/destination is reported only on a signing job, which is data-plane and never read); such edges are skipped rather than dangled. Metadata only: the scanner reads only ListSigningProfiles (with `IncludeCanceled` so canceled profiles still surface as metadata), GetSigningProfile (image-format enrichment), and ListSigningPlatforms; it never starts a signing job, never reads signing-job data or revocation status, and NEVER persists signing-material private keys, signed-object payloads, certificate bodies, or signing-parameter values. The accepted SDK surface excludes every signing-job read, sign action, and mutation operation by construction, proven by a reflection guard test on the adapter `apiClient` interface. |
| `cognito` | User pools (name, MFA configuration, Lambda trigger ARNs, password-policy summary, deletion protection; the Cognito API has deprecated the user-pool status field so it is not emitted), user pool app clients (id, name, allowed OAuth flows, callback and logout URLs, supported identity providers - never ClientSecret), identity providers (type and provider name only - never ProviderDetails secrets), resource servers, user pool groups, identity pools, and identity-pool role-attachment summaries, with user-pool-client-to-user-pool, user-pool-to-Lambda-trigger, identity-pool-to-user-pool, and identity-pool-to-identity-provider relationships. Covers both Cognito User Pools (`cognito-idp`) and Cognito Identity Pools (`cognito-identity`). The scanner never reads Cognito user records: ListUsers, AdminGetUser, AdminListGroupsForUser, and ListUsersInGroup are unreachable through the adapter. It never persists app-client secrets, identity-provider ProviderDetails (client_secret, google_client_secret, and similar), custom message Lambda templates, or MFA SMS configuration access tokens. Requires `ESHU_AWS_REDACTION_KEY`. |
| `glue` | Data Catalog database, table, crawler, job, trigger, workflow, and connection metadata plus table-in-database, table-to-S3-location, crawler-to-database, crawler-to-IAM-role, job-to-IAM-role, and trigger-to-job relationships. No script bodies, default-argument values, connection passwords, JDBC credential URLs, workflow graphs, table column sample statistics, or classifier custom patterns. |
| `servicecatalog` | Portfolios (id, ARN, display name, provider name, description, created time), products (id, ARN, name, product type, owner, distributor, status, created time; read through `SearchProductsAsAdmin`), and provisioned products (id, ARN, status, type, product id, provisioning-artifact id, provisioning-artifact name, CloudFormation stack physical id, created time; read through `ScanProvisionedProducts`). Relationships: provisioned-product-to-CloudFormation-stack (emitted only for `CFN_STACK` provisioned products whose physical identifier is a CloudFormation stack ARN, keyed by the stack ARN the `cloudformation` scanner publishes as the stack node `resource_id`; the stack ARN is resolved from the `SearchProvisionedProducts` physical-id index because `ScanProvisionedProducts` omits it), product-to-portfolio (from `ListPortfoliosForProduct`, keyed by the portfolio ARN/id), and portfolio-to-IAM-role (from `ListPrincipalsForPortfolio`, emitted only for fully defined IAM role ARNs; IAM users, groups, and `IAM_PATTERN` wildcard principals are skipped). Metadata only: the scanner never provisions, updates, or terminates products, never associates or disassociates principals or portfolios, never mutates constraints, and never reads provisioning-artifact template bodies, launch-constraint policy documents, provisioning parameter values, or record output values; the accepted SDK surface excludes them by construction, proven by a reflection guard test on the client interface. |
| `elasticache` | Cache clusters, replication groups, parameter and subnet groups, users, user groups, and snapshot metadata (name/source/status only); cluster-to-VPC, cluster-to-subnet, cluster-to-KMS, replication-group-to-cluster, and user-group-to-user relationships. No AUTH tokens, user passwords, user access strings, cache contents, or snapshot data. |
| `memorydb` | Managed Redis-compatible store (mirrors the ElastiCache shape). Clusters (name, ARN, status, node type, engine version, num shards/replicas, encryption/TLS status, ACL name), subnet groups, parameter groups (name, family, description, and tags - NOT parameter values), users (name, authentication type, password count, and a non-secret access-string-present summary - never the access string or passwords), ACLs (identity, status, member user names), and snapshot metadata (name/source/status only); cluster-to-subnet-group, cluster-to-KMS-key, cluster-to-SNS-topic, and ACL-to-user relationships. No user passwords, AUTH tokens, user access strings, cache contents, or snapshot data. The accepted SDK surface excludes every Create/Delete/Update/Modify/Reset/Failover mutation by construction, proven by a reflective guard test on the SDK adapter `apiClient` interface plus struct-reflection tests on the scanner-owned User, ACL, and snapshot types. |
| `mgn` | AWS Application Migration Service (rehost migration). Applications (ARN, id, name, description, wave id, archival status, aggregated health/progress status, total source server count), source servers (ARN, id, lifecycle state, data-replication state, replication type, recommended target instance type, OS full string, non-secret identification hints hostname/FQDN/AWS-instance-id, launched cutover/test EC2 instance id), launch configurations per source server (launch disposition, boot mode, target-instance right-sizing method, referenced EC2 launch template id, copy-tags/copy-private-ip flags; keyed by the synthesized `<source-server-id>/launch-configuration` id since launch configurations have no ARN), and jobs (ARN, id, type, status, initiator, timestamps, participating source server ids). Relationships: application-contains-source-server and job-targets-source-server (target `aws_mgn_source_server` by the bare MGN source server id the source-server node publishes, no synthesized ARN), source-server-launched-EC2-instance (targets `aws_ec2_instance` by the bare `i-` instance id MGN reports, a forward reference until a dedicated EC2 instance scanner exists), and launch-configuration-uses-launch-template (targets `aws_ec2_launch_template` by the bare `lt-` launch template id). Metadata only: never reads or persists replication-agent credentials, replication configuration secrets (the scanner never calls `GetReplicationConfiguration` or the replication-configuration-template reads), staging-area credentials, the `DataReplicationInfo.ReplicatorId`, or replicated disk contents, and never mutates MGN state; the accepted SDK surface is `DescribeSourceServers`/`ListApplications`/`GetLaunchConfiguration`/`DescribeJobs` only, with every replication-secret read and mutation API excluded by construction, proven by a reflection guard test on the SDK adapter `apiClient` interface. |
| `dax` | Amazon DynamoDB Accelerator (DAX), the in-memory accelerator in front of DynamoDB. Clusters (name, ARN, status, node type, active/total node counts, network type, endpoint encryption type, server-side-encryption status, assumed IAM role ARN, parameter/subnet group names, security-group memberships, discovery endpoint address/port as plain connection metadata), subnet groups (name, description, VPC id, member subnet ids - keyed by name, no ARN), and parameter groups (name and description only - NOT parameter values, keyed by name); cluster-to-subnet-group, cluster-to-security-group, cluster-to-IAM-role, subnet-group-to-VPC, and subnet-group-to-subnet relationships. DAX does not report a server-side-encryption KMS key ARN, so no cluster-to-KMS-key edge is emitted, and a DAX cluster does not name a DynamoDB table, so no cluster-to-table edge is emitted. No cached DynamoDB item data, query results, node endpoint payloads, or individual parameter values. The accepted SDK surface excludes every Create/Delete/Update/Increase/Decrease/Reboot mutation and DescribeParameters by construction, proven by a reflective guard test on the SDK adapter `apiClient` interface plus a struct-reflection test on the scanner-owned Cluster type. |
| `databrew` | AWS Glue DataBrew datasets (name, ARN, input source kind, file format, S3 input bucket/key references, Glue Data Catalog database/table/catalog-id references, the Glue connection name backing a database input, lifecycle timestamps), recipes (name, ARN, version, associated project, and step COUNT only - never the step expressions, transformation operations, or their parameters), jobs (name, ARN, type, processed dataset/recipe references, assumed IAM role ARN, encryption mode, distinct S3 output bucket names, lifecycle timestamps), and projects (name, ARN, bound dataset/recipe, assumed IAM role ARN). Datasets and recipes publish their resource_id as their NAME so internal job/project edges join; jobs and projects publish their ARN. Relationships: dataset-to-S3 (`target_type=aws_s3_bucket` keyed by the partition-aware synthesized `arn:<partition>:s3:::<bucket>` the S3 scanner publishes), dataset-to-Glue-table (`target_type=aws_glue_table` keyed by the `<database>/<table>` identity the Glue table scanner publishes), job-to-S3 output, job-to-IAM-role and project-to-IAM-role (`target_type=aws_iam_role` by the reported role ARN), job-to-dataset, project-to-dataset, and project-to-recipe (internal, keyed by the dataset/recipe name). A dataset whose input is a Redshift/JDBC database carries NO Redshift cluster edge: DataBrew reports only a Glue connection name and table name for such inputs, never a cluster ARN or identifier, so the edge would dangle and is skipped (the connection name is recorded as metadata). Metadata only: the scanner reads only ListDatasets, ListRecipes, ListJobs, and ListProjects and never reads or persists recipe step expressions, custom SQL query strings, dataset-parameter values, path-option expressions, sample data, or job output object data; the accepted SDK surface is `List`-only by construction (no Describe* detail read, no mutation), proven by a reflection guard test on the adapter interface. |
| `opensearch` | OpenSearch Service provisioned domains (engine version, instance type/count, dedicated master, zone awareness, at-rest and node-to-node encryption status, fine-grained access control summary, VPC config) and OpenSearch Serverless collections (name, type, status, KMS key), security configuration summaries (id, type, version), and managed VPC endpoints under one `service_kind`, plus custom package metadata (name, type, status - never the package body). Relationships: domain-to-VPC, domain-to-subnet, domain-to-security-group, domain-to-KMS-key, domain-to-IAM-role (master-user mapping/access-policy role refs), package-to-domain, and collection-to-KMS-key. No collection-to-VPC-endpoint edge is emitted: Serverless reports no collection-to-endpoint binding in either record, so a per-endpoint edge would be a misleading cross-product. The scanner never reaches the OpenSearch HTTP API (`_search`, `_msearch`, `_index`, `_doc`, `_bulk`, and similar) - that API is reachable only over the domain HTTP endpoint, which the scanner never constructs, and the SDK adapter interface carries no such method (nor `GetIndex` on either service). It never persists master user passwords (`DescribeDomains` does not return the password and the domain model has no password-shaped field), domain endpoint contents, the `Endpoints` map, access policy bodies, custom package bodies, or serverless saved-object bodies. |
| `opensearchserverless` | First-class Amazon OpenSearch Serverless (aoss) `service_kind`, distinct from `opensearch` (which surfaces Serverless collections only as a domain-scan side slice). Emits `aws_opensearchserverless_collection` (id, name, type, status, standby replicas, KMS key reference, timestamps), `aws_opensearchserverless_security_policy` (name, type, version, description, timestamps - never the policy document body), and `aws_opensearchserverless_vpc_endpoint` (id, name, status, VPC/subnet/security-group references) resources. Relationships: collection-to-KMS-key (`aws_kms_key` by key ARN, resolved from the matching encryption security policy with AWS's "most specific rule wins" precedence; AWS-owned-key policies emit no edge), VPC-endpoint-to-VPC (`aws_ec2_vpc` by bare `vpc-` id), VPC-endpoint-to-subnet (`aws_ec2_subnet` by bare `subnet-` id), and VPC-endpoint-to-security-group (`aws_ec2_security_group` by bare `sg-` id). Reads `ListCollections`/`BatchGetCollection`, `ListSecurityPolicies`/`GetSecurityPolicy`, `ListVpcEndpoints`/`BatchGetVpcEndpoint`, and `ListTagsForResource` only; the SDK adapter excludes every Create/Update/Delete mutation and every OpenSearch HTTP data-plane (index, search, bulk, document) method by construction, proven by a reflective guard test on the adapter `apiClient` interface. The encryption-policy body is parsed only to extract the customer-managed `KmsARN` and collection patterns, then discarded; access-policy and security-policy document bodies, collection and dashboard endpoints, and saved-object bodies are never persisted. |
| `msk` | MSK cluster, broker configuration, and replicator metadata with subnet, security-group, KMS-key, IAM-role, and configuration relationships; no broker `server.properties` bodies, broker logs, bootstrap broker endpoints, SCRAM secrets, or Kafka topic data. |
| `mq` | Amazon MQ broker and broker-configuration metadata for ActiveMQ and RabbitMQ engines (name, engine type/version, deployment mode, instance type, status, security-group refs, encryption options, broker usernames) with broker-to-subnet, broker-to-security-group, broker-to-KMS-key (customer-managed only), broker-to-configuration, and broker-to-CloudWatch-log-group relationships; no broker user passwords, configuration XML bodies, or queue/topic message contents. |
| `mwaa` | Amazon Managed Workflows for Apache Airflow (MWAA) environments (name, ARN, status, Airflow version, webserver access mode, environment class, endpoint management, scheduler/worker/webserver counts, created-at). Relationships: environment-to-S3-bucket (the DAG bucket, `aws_s3_bucket` by source-bucket ARN; a bare bucket name is synthesized to `arn:<partition>:s3:::<name>`), environment-to-subnet (`aws_ec2_subnet` by bare subnet id), environment-to-security-group (`aws_ec2_security_group` by bare group id), environment-to-IAM-execution-role (`aws_iam_role` by role ARN), environment-to-KMS-key (`aws_kms_key`), and environment-to-CloudWatch-log-group (one per enabled Airflow log module: DAG processing, scheduler, task, webserver, worker; `aws_cloudwatch_logs_log_group` by the non-wildcard log group ARN, with MWAA's trailing `:*` trimmed). Never persists Apache Airflow configuration option values, connection strings, the Celery executor queue ARN, the database VPC endpoint service, the webserver URL, or any secret; the scanner-owned environment type has no field to hold one. The adapter calls `ListEnvironments` and `GetEnvironment` only and never creates, updates, or deletes an environment, mints an Airflow CLI or web-login token, invokes the Airflow REST API, publishes metrics, or tags resources, proven by a reflection guard on the SDK adapter read surface. No `ESHU_AWS_REDACTION_KEY` required. |
| `kinesis` | Kinesis Data Streams (name, shard count, retention, stream mode, encryption), Kinesis Data Firehose delivery streams (name, source, destination type, encryption), and Kinesis Video Streams (name, status, KMS key, retention) under one `service_kind`; resource types distinguish the three sub-services. Relationships: data-stream-to-KMS-key, video-stream-to-KMS-key, Firehose-to-S3/Redshift/OpenSearch/Splunk/HTTP-endpoint, Firehose-to-Lambda-transform, and Firehose-to-IAM-role. No stream records, no video media fragments, no Firehose processing-configuration Lambda body, no HTTP endpoint access keys, no Splunk HEC tokens, and no Redshift passwords. |
| `kinesisanalyticsv2` | Amazon Managed Service for Apache Flink (Kinesis Data Analytics v2) applications (name, ARN, status, runtime environment, application mode STREAMING/INTERACTIVE, description, current version id and version count, service execution role ARN, snapshots-enabled flag, code content FORMAT and S3 code location reference, Flink parallelism posture: auto-scaling flag, configuration type, parallelism / parallelism-per-KPU / current parallelism, lifecycle timestamps, and snapshot names and status). Relationships, each keyed to the identity the target scanner publishes: application-reads-from / application-writes-to Kinesis data stream (`aws_kinesis_data_stream` by reported SQL input/output stream ARN), application-reads-from / application-writes-to Firehose delivery stream (`aws_firehose_delivery_stream` by reported SQL input/output delivery stream ARN), application-uses-S3-code-bucket (`aws_s3_bucket` by reported code bucket ARN, object key only - never the code body), application-uses-subnet (`aws_ec2_subnet` by bare `subnet-` id) and application-uses-security-group (`aws_ec2_security_group` by bare `sg-` id) from the VPC configurations, application-uses-IAM-role (`aws_iam_role` by service execution role ARN), and application-logs-to-CloudWatch-log-group (`aws_cloudwatch_logs_log_group` by the log group ARN derived from each reported CloudWatch logging-option log stream ARN, trimming the `:log-stream:` segment and any trailing `:*` wildcard). Reads `ListApplications`, `DescribeApplication`, `ListApplicationSnapshots`, and `ListTagsForResource` only; the SDK adapter excludes every Create/Update/Delete/Start/Stop/Add/Rollback mutation and any application-code reader/writer by construction, proven by a reflective guard test on the adapter `apiClient` interface. Metadata only: never application code bodies, SQL text, the verbose job plan, environment property values, or run-configuration content. No MSK edge is emitted: a Managed Flink application references MSK only through environment property values (which are never persisted), so the control-plane describe output exposes no structured MSK reference to key an edge on. No `ESHU_AWS_REDACTION_KEY` required. |
| `firehose` | Amazon Data Firehose delivery streams as a first-class `service_kind`, emitting `aws_firehose_delivery_stream` resources (name, ARN, status, delivery stream type, source type, encryption mode/status, customer-managed KMS key ARN, creation time, and a destination-class summary). Relationships, all keyed to the identity the target scanner publishes: stream-to-S3-bucket (`aws_s3_bucket` by reported bucket ARN, primary and staging/backup buckets), stream-to-Redshift-cluster (`aws_redshift_cluster` by the cluster identifier parsed from the JDBC URL host - no fabricated ARN), stream-to-OpenSearch-domain (`aws_opensearch_domain` by reported domain ARN, covering both the Amazon OpenSearch Service and legacy Elasticsearch destination shapes), stream-sourced-from-Kinesis-stream (`aws_kinesis_data_stream` by reported source ARN), stream-uses-IAM-role (`aws_iam_role` by reported delivery role ARN), stream-uses-KMS-key (`aws_kms_key` by customer-managed key ARN; AWS-owned keys emit no edge), stream-logs-to-CloudWatch-log-group (`aws_cloudwatch_logs_log_group` by reported log group name), and stream-uses-Lambda-transform (`aws_lambda_function` by reported processor LambdaArn). Reads `ListDeliveryStreams`, `DescribeDeliveryStream`, and `ListTagsForDeliveryStream` only; the SDK adapter excludes PutRecord, PutRecordBatch, every Create/Update/Delete, Start/StopDeliveryStreamEncryption, and Tag/UntagDeliveryStream by construction, proven by a reflective guard test on the adapter `apiClient` interface. No delivery records, no Splunk HEC tokens, no HTTP endpoint access keys, no Redshift passwords, and no processing-configuration Lambda body. No `ESHU_AWS_REDACTION_KEY` required. Note: the `kinesis` scanner also surfaces Firehose delivery streams under `aws_kinesis_firehose_delivery_stream`; this scanner is the dedicated first-class Firehose owner with the additional source-stream, KMS-SSE, and CloudWatch-log-group joins. |
| `codebuild` | Build projects (name, source type/location, environment image/compute type, timeout), report groups (type, status, S3 export destination), and recent builds (id, status, phase, duration - never build logs) with project-to-IAM-role, project-to-VPC/subnet/security-group, project-to-KMS-key, project-to-S3-source, project-to-Git-source (GitHub/GitHub Enterprise/CodeCommit/Bitbucket/GitLab), project-to-S3-artifact, project-to-Secrets-Manager-secret, and project-to-SSM-Parameter-Store-parameter relationships. Never persists buildspec.yml bodies (`Source.Buildspec`/`BuildspecOverride`), environment-variable PLAINTEXT values (name and type only; the value is routed through the shared redact library), build logs, or source-credential tokens. PARAMETER_STORE and SECRETS_MANAGER environment variables keep only their parameter name or secret reference to drive relationship edges. The adapter never calls CreateProject, UpdateProject, DeleteProject, StartBuild, StopBuild, RetryBuild, BatchDeleteBuilds, ImportSourceCredentials, or DeleteSourceCredentials. Requires `ESHU_AWS_REDACTION_KEY`. |
| `codecommit` | Repositories (name, ARN, repository id, default branch name, clone-URL host only, KMS key id, creation/last-modified timestamps, tags) with repository-to-KMS-key (encryption) and repository-to-SNS-topic (trigger) relationships. The repository resource is a code-to-cloud correlation anchor: it publishes the repository name and full clone URLs as correlation anchors so a CodeBuild project (`codebuild_project_sourced_from_repository`), CodePipeline source action, or Amplify app whose Git source location is the clone URL joins the repository node. The repository-to-KMS-key edge uses the key CodeCommit reports directly (bare key id keyed to the KMS scanner's `resource_id`, or key ARN when ARN-shaped); the repository-to-SNS-topic edge is emitted only for SNS-topic trigger destinations (non-SNS trigger destinations stay as a trigger count, never a dangling edge). Every ARN on a CodeCommit fact comes from the API, so no ARN is synthesized and no partition is hardcoded. Metadata only: the scanner never reads commits, refs, blobs, file contents, pull-request bodies, or comment text, and never mutates any CodeCommit resource; the SDK adapter read surface (`ListRepositories`, `BatchGetRepositories`, `GetRepositoryTriggers`, `ListTagsForResource`) excludes every content reader and mutation by construction, proven by a reflection guard test. AWS no longer onboards new CodeCommit customers, but existing repositories remain in scope. No redaction key required. |
| `codedeploy` | Applications (name, compute platform), deployment groups (deployment style, auto-rollback config, EC2/on-premises tag-filter summaries), deployment configurations (minimum-healthy-hosts), and recent deployments (id, status, revision summary) with deployment-group-to-application, deployment-group-to-IAM-role, deployment-group-to-Auto-Scaling-group, deployment-group-to-ECS-service, deployment-group-to-Lambda-function, and deployment-group-to-SNS-topic relationships. Never persists appspec.yml bodies (the scanner-owned types have no field for the revision content; only revision-source references are kept); on-premises instance tag values route through the shared redact library. The adapter never creates, updates, deletes, or starts deployments or any CodeDeploy resource. Requires `ESHU_AWS_REDACTION_KEY`. |
| `amp` | Amazon Managed Service for Prometheus workspaces (ARN, workspace id, alias, status, optional customer-managed KMS key ARN, Prometheus endpoint URL, creation time), rule-groups namespaces (NAME only, ARN, owning workspace id, status - the recording-rule and alerting-rule definition body is never read), and managed-collector scrapers (ARN, scraper id, alias, status, IAM role ARN, EKS source cluster ARN, destination workspace ARN, EKS VPC subnet and security-group ids). Relationships: workspace-to-KMS-key (targets `aws_kms_key` by the reported key ARN, emitted only for a customer-managed key; `target_arn` set only for an ARN-shaped identifier), rule-groups-namespace-to-workspace (keyed by the workspace ARN the workspace node publishes, targets `aws_amp_workspace`), scraper-to-EKS-cluster (targets `aws_eks_cluster` by the EKS source cluster ARN the EKS scanner publishes), scraper-to-workspace (targets `aws_amp_workspace` by the destination workspace ARN), scraper-to-subnet (targets `aws_ec2_subnet` by the bare `subnet-...` id the EC2 scanner publishes), and scraper-to-security-group (targets `aws_ec2_security_group` by the bare `sg-...` id the EC2 scanner publishes). A non-EKS (MSK/VPC) scraper source carries no EKS cluster, so it emits no EKS, subnet, or security-group edge. An empty account is not an error: the scan returns cleanly with no facts. Metadata only: the scanner reads only ListWorkspaces, ListRuleGroupsNamespaces, and ListScrapers; it never reads ingested time-series samples, query results, alert-manager definitions, rule-group definition bodies (DescribeRuleGroupsNamespace), or scrape-configuration bodies (GetDefaultScraperConfiguration / DescribeScraper), and never mutates AMP state. The accepted SDK surface is `List`-only by construction, proven by a reflection guard test on the adapter interface. Synthesized identifiers are not used; AWS reports every ARN, so partition handling rides the reported ARNs. |
| `codeguru` | Amazon CodeGuru Reviewer repository associations (ARN, association id, name, owner, provider type CodeCommit/GitHub/Bitbucket/GitHubEnterpriseServer/S3Bucket, state, customer-managed KMS key id and encryption option, S3 backing bucket name, CodeStar connection ARN, lifecycle timestamps, tags) and CodeGuru Profiler profiling groups (ARN, name, compute platform Default/AWSLambda, profiling-enabled posture, lifecycle timestamps, tags), under one `codeguru` `service_kind`. Relationship: association-reviews-CodeCommit-repository (targets `aws_codecommit_repository`), emitted only when the provider type is CodeCommit. CodeGuru reports only the repository name and owning account, so the scanner synthesizes the partition-aware CodeCommit repository ARN (`arn:<partition>:codecommit:<region>:<owner>:<name>`) via `awscloud.PartitionForBoundary` to match the CodeCommit scanner's published `resource_id`, and skips the edge when the owner account or region is missing rather than dangling it. Non-CodeCommit providers, the CodeStar connection ARN, and the S3 backing bucket name are recorded as resource attributes only, never promoted to a dangling edge. The profiling-group compute platform is a resource attribute; CodeGuru reports no structured compute resource identifier to key an edge on. Metadata only: the scanner uses only ListRepositoryAssociations, DescribeRepositoryAssociation, and ListProfilingGroups (with descriptions); it never reads code-review findings, recommendation content, analyzed source, profiling samples, aggregated profiles, flame graphs, recommendation reports, or agent telemetry (no DescribeCodeReview, ListRecommendations, ListRecommendationFeedback, GetProfile, ListFindingsReports, or BatchGetFrameMetricData), proven by reflection guard tests on both SDK adapter interfaces. API-reported ARNs are used directly so they stay partition-aware. No redaction key required. |
| `amplify` | Amplify apps (id, ARN, name, platform, repository URL as host/path only, default domain, service/compute IAM role ARNs, production branch) and branches (name, ARN, stage, framework, owning app id) with app-to-source-repository (an external `git_repository` join anchor; CodeCommit/GitHub/GitLab/Bitbucket), app-to-IAM-role (service role and SSR compute role, targets `aws_iam_role` by role ARN), app-to-custom-domain (Route 53 hosted zone by normalized domain root and CloudFront distribution by the `*.cloudfront.net` subdomain DNS record, where resolvable), and branch-to-app relationships. Never persists Amplify environment variables, build-spec bodies, basic-auth credentials, or repository access tokens; repository URLs are reduced to host and path so an embedded token is stripped. The adapter is `List*`-only (ListApps, ListBranches, ListDomainAssociations) and calls no Create/Update/Delete/Start-job/Start-deployment/webhook API, proven by a reflection guard. Synthesized ARNs derive their partition from the scan boundary. No redaction key required. |
| `stepfunctions` | State machine and activity metadata, execution-role relationships, and ARN-only Task-target relationships; no execution payloads, history events, task tokens, or definition literals. |
| `backup` | Backup vault, backup plan, backup selection, recovery point (metadata only - id, source resource ARN, vault, status, creation/expiration), report plan, restore testing plan, and framework metadata with plan-to-selection, selection-to-resource, selection-to-IAM-role, vault-to-KMS-key, recovery-point-to-vault, recovery-point-to-source-resource, and framework-to-control relationships. No recovery point contents, vault access policy bodies, or framework control input parameter values. |
| `accessanalyzer` | Analyzer metadata, archive-rule names, aggregate finding counts, relationships, and unused-access summaries. |
| `kms` | Customer master keys, aliases, and grants with alias-to-key, grant-to-key, and grant-to-grantee-principal relationships. The scanner never calls cryptographic operations (Encrypt, Decrypt, GenerateDataKey, Sign, Verify, ReEncrypt, GenerateMac, VerifyMac, GenerateDataKeyPair, GenerateDataKeyWithoutPlaintext, DeriveSharedSecret, GetPublicKey) or key lifecycle mutations (CreateKey, ScheduleKeyDeletion, EnableKey, DisableKey, EnableKeyRotation, DisableKeyRotation, PutKeyPolicy, CreateGrant, RevokeGrant, RetireGrant, ReplicateKey, ImportKeyMaterial, DeleteImportedKeyMaterial). Key policy Statement bodies, grant encryption contexts, and key material stay outside the scan slice. |
| `codeartifact` | Package-registry domains (name, ARN, owner account, encryption-key ARN, S3 asset-bucket ARN, repository count, asset size, status) and repositories (name, ARN, owning domain, administrator account, external connections, upstream repositories). Relationships: repository-to-domain (targets `aws_codeartifact_domain` by the domain name the domain resource publishes as its `resource_id`), domain-to-KMS-key (targets `aws_kms_key` by the encryption-key ARN AWS reports, ARN-keyed so it joins the KMS key node, emitted only when an ARN-shaped key is reported), repository-to-upstream-repository (targets `aws_codeartifact_repository` by the `<domain>/<upstream>` identity within the same domain), and repository-to-external-connection (targets the labeled non-AWS `public_package_registry` identity, for example `public:npmjs`, with no synthesized ARN). The domain `resource_id` is the domain name; the repository `resource_id` is the repository ARN with the `<domain>/<name>` identity and bare name as correlation anchors. Metadata only: the scanner uses only ListDomains, DescribeDomain, ListRepositories, and DescribeRepository; it never reads, downloads, publishes, copies, or deletes a package version or package asset (no GetPackageVersionAsset, GetPackageVersionReadme, ListPackages, ListPackageVersions, PublishPackageVersion, or CopyPackageVersions), proven by reflection guard tests on the scanner-owned `Client` and the SDK adapter `apiClient` interfaces. API-reported ARNs are used directly so they stay partition-aware. No redaction key required. |
| `organizations` | Organization root, OUs, accounts, policy summaries, policy target bindings, and delegated administrators. |
| `ssoadmin` | IAM Identity Center instances, permission sets (name, description, session duration, relay state), account assignments (principal type/id, permission set, account), application instances, trusted token issuers, and group/user principals resolved from the connected identity store. Relationships cover assignment-to-permission-set, assignment-to-account, assignment-to-principal, permission-set-to-managed-policy (ARN ref), and permission-set-to-customer-managed-policy (name ref). No permission set inline policy bodies, permissions boundary bodies, customer-managed policy bodies, or application access-scope group filters. Principal display names are redacted; only the identity store display name is read. |
| `sagemaker` | Notebook instances, models, endpoints, endpoint configs, training jobs, processing jobs, transform jobs, hyperparameter tuning jobs, projects, pipelines, feature groups (metadata only), Studio domains, user profiles, apps, and inference components, with model-to-S3-artifact, model-to-container-image-URI, model-to-IAM-role, endpoint-to-endpoint-config, endpoint-config-to-model, training-job-to-IAM-role, notebook-to-subnet, domain-to-VPC, and user-profile-to-domain relationships. Metadata only: the scanner never invokes endpoints (InvokeEndpoint / InvokeEndpointAsync) and never persists hyperparameter values, training/processing/transform input or output data references, notebook lifecycle-config script bodies, container environment maps, or pipeline definition bodies. |
| `config` | AWS Config configuration recorders (recorded resource-type scope, recording strategy - never recorded configuration item bodies), delivery channels (S3 bucket, optional SNS topic, snapshot delivery interval), config rules (name, ARN, owner: AWS managed / CUSTOM_LAMBDA / CUSTOM_POLICY, managed-rule source identifier or custom-Lambda function ARN, and resource-type scope carried as a rule attribute), conformance packs (name, deployment status, member-rule count), configuration aggregators (source accounts and regions, or organization role), and retention configurations. Relationships: conformance-pack-to-rule (targets the `aws_config_rule` node by rule name), custom-rule-to-Lambda-function (targets the `aws_lambda_function` ARN), and aggregator-to-source-account (targets the `aws_account` root ARN with the partition derived from the aggregator ARN, never a hardcoded `arn:aws:`). The rule resource-type scope is a rule attribute, not an edge to a synthetic resource-type node. Metadata only: the scanner never calls GetResourceConfigHistory, GetComplianceDetailsByConfigRule, GetDiscoveredResourceCounts, or BatchGetResourceConfig, never persists recorded configuration item bodies (full resource snapshots), and never fetches custom-rule Lambda code (GetCustomRulePolicy). Aggregate per-rule compliance is used only to derive the conformance pack member-rule set and count. |
| `bedrock` | Foundation model availability (read-only list), custom models, model customization jobs, provisioned model throughputs, guardrails, agents, agent action groups, and knowledge bases, with custom-model-to-base-model, custom-model-to-S3-output, custom-model-to-customization-job, provisioned-throughput-to-model, agent-to-foundation-model, agent-to-knowledge-base, agent-to-action-group, action-group-to-Lambda-function, and knowledge-base-to-S3/Confluence/SharePoint/web-crawler relationships. Metadata only: the scanner never calls bedrock-runtime (InvokeModel, Converse) or bedrock-agent-runtime (InvokeAgent, Retrieve, RetrieveAndGenerate), and never persists agent instructions (system prompts), prompt-override configurations, guardrail topic or content policy bodies, knowledge base ingested document content, action-group API schema bodies, or custom-model hyperparameter and training-input data. |
| `ram` | Resource shares the account owns (name, ARN, status, allowExternalPrincipals, owning account id, feature set), the resources each share shares (ARN + RAM `service-code:resource-code` type), the principals each share targets (account id / Organizations OU ARN / organization or root ARN), and the managed permissions each share uses (name, ARN, version, type - never the permission policy document body). Relationships: share-to-shared-resource (targets the shared resource ARN with its reported resource type), share-to-principal-account (targets `aws_organizations_account` by bare account id, joining the organizations scanner for cross-account truth), share-to-principal-OU (targets `aws_organizations_organizational_unit` by OU ARN), share-to-principal-organization (targets `aws_organizations_root` by organization or root ARN), and share-to-permission (targets `aws_ram_permission` by permission ARN). Owner-account inventory only (resource owner SELF). Metadata only: the scanner never calls GetPermission (which returns the permission policy document body) and never mutates a RAM resource (no Create/Delete/Update, Associate/Disassociate, Accept/Reject, Enable/Disable, Promote/Replace, Tag/Untag, or SetDefaultPermissionVersion); the adapter read surface excludes those operations by construction. Pairs with the `organizations` scanner. |
| `resourcegroups` | Resource Groups groups (name, ARN, description, query type TAG_FILTERS_1_0 / CLOUDFORMATION_STACK_1_0, member count) and the membership edges that connect a group to its member resources. Each member ARN is classified by its ARN service segment into its resource family, and the membership edge is keyed by the identity that family's own scanner publishes: ARN-equality for S3 buckets, Lambda functions, DynamoDB tables, SQS queues, SNS topics, Kinesis streams, RDS instances/clusters, ECS clusters/services/tasks, EKS clusters, ELBv2 load balancers, Secrets Manager secrets, and CloudFormation stacks; the bare id for EC2 instances/VPCs/subnets/security-groups/network-interfaces/launch-templates and Elastic IPs (`eipalloc-…`) and KMS keys; and the `/hostedzone/<id>` prefixed id for Route 53 hosted zones. A CloudFormation-stack-backed group also emits a group-to-`aws_cloudformation_stack` edge keyed by the stack ARN. Members whose family the classifier does not recognize are skipped rather than emitted with an empty `target_type`, so no membership edge dangles. Member ARNs come from the `ListGroupResources` API and are used directly (partition-aware). Metadata only: the scanner records the query type only and never persists the resource-query body (tag-filter expression or CloudFormation template JSON) beyond the stack identifier; the adapter calls only ListGroups, GetGroupQuery, and ListGroupResources and never a mutation API (Create/Update/Delete group, UpdateGroupQuery, GroupResources, UngroupResources, Tag, Untag, PutGroupConfiguration), proven by a reflection guard on the adapter interface. |
| `servicediscovery` | AWS Cloud Map (Service Discovery) namespaces (id, name, type DNS_PUBLIC/DNS_PRIVATE/HTTP, service count, backing Route 53 hosted-zone id for DNS namespaces, HTTP discovery name for HTTP namespaces) and services (id, name, parent namespace id/name, DNS config summary of routing policy and DNS record types/TTLs, and instance COUNT only). Relationships: service-to-namespace (keyed by the Cloud Map namespace id) and namespace-to-Route53-hosted-zone (keyed by the `/hostedzone/<id>` resource id the `route53` scanner emits, DNS namespaces only). The Cloud Map service resource is keyed by its `namespaceName/serviceName` identity with resource type `aws_cloud_map_service`, which resolves the App Mesh virtual-node-to-Cloud-Map-service edge that targets the same namespace/service join key. No namespace-to-VPC edge is emitted: Cloud Map's read API does not report the VPC for a private DNS namespace, so the VPC is reached transitively through the private Route 53 hosted zone the `route53` scanner owns. Instance attribute maps are NEVER read or persisted because they can carry caller-defined secrets; the instance count comes from the service summary. The scanner never calls a Cloud Map mutation API (Create/Update/Delete namespace or service, RegisterInstance, DeregisterInstance, UpdateInstanceCustomHealthStatus, TagResource, UntagResource) and never calls an instance discovery/read API (ListInstances, GetInstance, GetInstancesHealthStatus, DiscoverInstances, DiscoverInstancesRevision); the accepted SDK surface excludes them by construction, proven by a reflection guard test on the adapter interface. |
| `lakeformation` | Data-access governance over the Glue Data Catalog. Data-lake settings (data-lake admin and read-only admin principal identifiers only - never the default-database/table permission bodies, authorized session-tag values, or external-data-access flags), registered data locations (`ListResources`: registered location ARN, registering IAM role ARN, hybrid-access flag, federation flag, privileged-access flag, role-access verification status), and principal/resource permission grants (`ListPermissions`: principal identifier, governed resource reference, and the bounded AWS privilege enum names SELECT/ALTER/DROP/ALL/... as grant identity). Relationships: registered-location-to-S3-bucket (targets `aws_s3_bucket` by `arn:<partition>:s3:::<bucket>` derived from the registered location ARN, partition inherited from that ARN), registered-location-to-IAM-role (targets `aws_iam_role` by registering role ARN), permission-to-Glue-database (targets `aws_glue_database` by bare database name, the Glue scanner's database `resource_id`), permission-to-Glue-table (targets `aws_glue_table` by `database/table`, the Glue scanner's table `resource_id`; a table-wildcard grant keys on the bare database name), and permission-to-principal (targets `aws_iam_role` by principal ARN, emitted only when the principal identifier is an IAM role ARN). Data-sensitivity contract: the scanner emits grant identities, principal identifiers, and resource ARNs only. It NEVER persists a permission policy body, a permission condition (LF-Tag) expression, an LF-Tag value, an LF-Tag policy expression, the `AdditionalDetails` RAM-share payload, or principal credentials. The scanner never grants, revokes, registers, deregisters, puts settings, or mutates LF-Tags, and never calls a credential-vending or governed-data read (GetTemporaryGlueTableCredentials, GetTemporaryGluePartitionCredentials, GetTemporaryDataLocationCredentials, GetTableObjects, GetWorkUnits, GetWorkUnitResults, StartQueryPlanning); the accepted SDK surface (GetDataLakeSettings, ListResources, ListPermissions) excludes them by construction, proven by a reflection guard test on the adapter interface. Pairs with the `glue` scanner. |
| `storagegateway` | Gateways (id, name, type FILE_S3/FILE_FSX_SMB/CACHED/STORED/VTL, state, endpoint type STANDARD/FIPS, host environment, audit log group, VPC endpoint), cached/stored iSCSI volumes (type, size, attachment status, gateway ARN), and NFS/SMB S3 file shares (type, status, S3 location, role/KMS/log-group ARNs, gateway ARN). Relationships: volume-to-gateway and file-share-to-gateway (keyed by the gateway ARN the gateway node publishes), file-share-to-S3-bucket (LocationARN reduced to the partition-aware bucket-only ARN the `s3` scanner publishes, access-point LocationARNs skipped), file-share-to-IAM-role, file-share-to-KMS-key (ARN-keyed, matching the EFS/FSx convention), file-share-to-CloudWatch-log-group (audit destination), and gateway-to-VPC-endpoint (emitted only when DescribeGatewayInformation reports the bare `vpce-` id the `vpc` scanner publishes). Metadata only: the scanner never activates, deletes, shuts down, reboots, or updates a gateway, never refreshes a file-share cache, never creates or deletes volumes or shares, and never persists file-share object contents, NFS client allow lists, or SMB admin/user lists; the accepted SDK surface excludes every mutation, cache, tape, and credential operation by construction, proven by a reflection guard test on the adapter interface. |
| `appflow` | Amazon AppFlow flows (ARN, name, status, source/destination connector types, source/destination connector profile names, trigger type) and connector profiles (ARN, name, connector type, connection mode). Relationships: flow-to-S3-source-bucket and flow-to-S3-destination-bucket (targets `arn:<partition>:s3:::<bucket>` with the partition derived from the flow ARN, never a hardcoded `arn:aws:`), flow-to-connector-profile (targets the connector profile node by name, the same key flows reference), flow-to-KMS-key (customer-provided key ARN only; the AppFlow-managed key emits no edge), and connector-profile-to-Secrets-Manager-secret (ARN only, matched by exact `secretsmanager` ARN service segment). Metadata only: the scanner never starts, stops, or runs flows, never reads flow run records (DescribeFlowExecutionRecords), and never reads field mappings (the flow's task transforms, which can encode literal transferred data values), connector credentials, or OAuth tokens. The only credential reference recorded is the connector profile's Secrets Manager credentials ARN, used solely to drive the profile-to-secret edge; the accepted SDK surface is exactly ListFlows, DescribeFlow, and DescribeConnectorProfiles, excluding every mutation, run, and credential-read operation by construction, proven by a reflection guard test on the Client interface. |
| `appconfig` | AWS AppConfig applications (id, synthesized partition-aware ARN, name, description), environments (id, owning application id, lifecycle state, CloudWatch alarm monitor count), configuration profiles (id, owning application id, profile type AWS.AppConfig.FeatureFlags/AWS.Freeform, configuration source location URI reference, validator-type kinds JSON_SCHEMA/LAMBDA), and deployment strategies (id, name, deployment duration, final bake time, growth factor/type, replicate-to target). AppConfig list responses carry no ARN, so each node publishes a synthesized partition-aware ARN (`arn:<partition>:appconfig:<region>:<account>:application/<id>`, `.../environment/<id>`, `.../configurationprofile/<id>`, `:deploymentstrategy/<id>`) derived with the boundary partition so GovCloud and China join real nodes, never a hardcoded `arn:aws:`. Relationships: environment-to-application and profile-to-application (internal, keyed by the application ARN the application node publishes, targets `aws_appconfig_application`), environment-to-CloudWatch-alarm (the deployment monitor, targets `aws_cloudwatch_alarm` by the alarm ARN AppConfig reports, matching the CloudWatch scanner's published alarm resource_id), and environment-to-IAM-role (the IAM role AppConfig assumes to read a monitored alarm, targets `aws_iam_role` by the reported role ARN, emitted only for an ARN-shaped role identifier). Metadata only: the scanner reads only ListApplications, ListEnvironments, ListConfigurationProfiles, and ListDeploymentStrategies, and NEVER reads or persists configuration content, hosted configuration version bodies, or freeform/feature-flag values - it never calls GetConfiguration or GetLatestConfiguration (the appconfigdata data-plane module is never imported), GetHostedConfigurationVersion, StartDeployment/StopDeployment, or any AppConfig mutation API. The accepted SDK surface is `List`-only by construction, proven by a reflection guard test on the adapter interface. An empty account returns cleanly with no facts. No `ESHU_AWS_REDACTION_KEY` required. |
| `keyspaces` | Amazon Keyspaces (for Apache Cassandra) keyspaces (name, ARN, replication strategy, replication regions) and tables (name, ARN, parent keyspace, status, capacity mode, encryption type, customer-managed KMS key identifier, point-in-time-recovery status, TTL/CDC/client-side-timestamp status, and the structural CQL schema: column names and data types plus partition, clustering, and static column names). Relationships: table-to-keyspace (`target_type=aws_keyspaces_keyspace`, keyed by the keyspace ARN, whose partition is taken from the keyspace API ARN or derived from the table ARN, never a hardcoded `arn:aws:`) and table-to-KMS-key (`target_type=aws_kms_key`, emitted only for customer-managed SSE keys reporting an ARN). Schema column names and types are structural metadata, not row data. Metadata only: the scanner reads only ListKeyspaces, GetKeyspace, ListTables, GetTable, and ListTagsForResource; it never executes CQL, never runs ExecuteStatement, BatchStatement, or Select, never reads table rows or cells, never restores tables, and never mutates keyspaces or tables. The accepted SDK surface excludes every data-plane read and mutation by construction, proven by a reflection guard test on the adapter interface. |
| `xray` | X-Ray **configuration** only: trace groups (name, ARN, trace filter expression, insights flags), sampling rules (name, ARN, priority, reservoir size, fixed rate, and service name/type/host match criteria), and the account-region encryption configuration (type NONE/KMS, status, KMS key reference). Relationships: encryption-config-to-KMS-key (target_type `aws_kms_key`, emitted only when encryption type is KMS and AWS reports a key reference; the reported key id/ARN/alias is the join key the `kms` scanner publishes, and `target_arn` is set only when ARN-shaped so a bare id is never given a fabricated ARN) and sampling-rule-to-service (target_type `aws_xray_service_correlation`, a labeled correlation anchor keyed by `<service_name>/<service_type>` that reducers resolve to the real service node by name; a wildcard-only `*`/`*` rule names no service and emits no edge). Configuration only: the scanner uses exactly `GetGroups`, `GetSamplingRules`, and `GetEncryptionConfig`, and NEVER reads or persists X-Ray observability payload — no traces, trace summaries, segments, or service-graph (service-map) data. It never calls `GetTraceSummaries`, `BatchGetTraces`, `GetTraceGraph`, `GetServiceGraph`, `GetTimeSeriesServiceStatistics`, any `GetInsight*`, `GetSamplingTargets`, `GetSamplingStatisticSummaries`, `PutTraceSegments`, `PutTelemetryRecords`, or any group/sampling-rule/encryption mutation; the accepted SDK surface excludes them by construction, proven by reflection guard tests on both the scanner `Client` interface and the SDK adapter interface. The group filter expression is persisted as a configuration string, never expanded into the traces it selects. Requires no `ESHU_AWS_REDACTION_KEY`. |
| `ses` | Amazon Simple Email Service (SES v2) email identities (name, type EMAIL_ADDRESS/DOMAIN, verification status, verified-for-sending and sending-enabled flags, feedback-forwarding flag, default configuration set name, DKIM enabled/status/signing-attributes-origin enums, MAIL FROM domain/status/behavior), configuration sets (sending/reputation/TLS options, sending dedicated-IP-pool name, tracking custom-redirect domain), configuration-set event destinations (enabled flag, matching event types, destination classes), and dedicated IP pools (name only). Relationships: email-identity-to-default-configuration-set (`target_type=aws_ses_configuration_set`, keyed by the configuration set name the SES configuration-set node publishes), configuration-set-to-dedicated-IP-pool (`target_type=aws_ses_dedicated_ip_pool`, keyed by the pool name), event-destination-to-SNS-topic (`target_type=aws_sns_topic`, keyed by the topic ARN SES reports, matching the `sns` scanner's published topic resource_id), event-destination-to-Firehose-delivery-stream (`target_type=aws_firehose_delivery_stream`, keyed by the delivery stream ARN SES reports, matching the `firehose` scanner's published stream resource_id, with the SES-assumed IAM role ARN carried as an edge attribute), and, defensively, email-identity-DKIM-to-KMS-key (`target_type=aws_kms_key`, emitted only if AWS ever reports a customer key on the DKIM attributes, which SES v2 does not surface today). Synthesized identity, configuration-set, and dedicated-IP-pool ARNs derive the partition from the scan boundary via `PartitionForBoundary`, so GovCloud and China resolve correctly; never a hardcoded `arn:aws:`. Metadata only: the scanner reads only ListEmailIdentities, GetEmailIdentity, ListConfigurationSets, GetConfigurationSet, GetConfigurationSetEventDestinations, and ListDedicatedIpPools; it never sends email, never reads message or template bodies, and NEVER persists DKIM private keys, DKIM signing tokens (`DkimAttributes.Tokens`), identity policy documents (`GetEmailIdentity` `Policies`), or SMTP credentials. The accepted SDK surface excludes every send and mutation operation by construction, proven by a reflection guard test on the adapter interface. |
| `timestream` | Amazon Timestream for LiveAnalytics databases (name, ARN, KMS key id, table count, lifecycle timestamps) and tables (name, ARN, parent database, state, memory-store and magnetic-store retention, magnetic-store writes-enabled flag, magnetic-store rejected-data S3 report location, and composite partition-key attribute names). Relationships: table-to-database (keyed by the database ARN the database node publishes, targets `aws_timestream_database`), database-to-KMS-key (targets `aws_kms_key` by the reported key ARN), and table-to-S3 (the magnetic-store rejected-data report bucket, targets `aws_s3_bucket` by the partition-aware synthesized `arn:<partition>:s3:::<bucket>` the S3 scanner publishes). Metadata only: the scanner reads only ListDatabases, ListTables, and ListTagsForResource on the timestream-write management API and never reads time-series records or measures, never runs queries (the timestream-query module is never imported), never calls WriteRecords or any batch-load API, and never mutates Timestream state; the accepted SDK surface is `List`-only by construction, proven by a reflection guard test on the adapter interface. |
| `imagebuilder` | EC2 Image Builder image pipelines (ARN, name, status, platform, referenced recipe/config ARNs, schedule expression, lifecycle timestamps), account-owned image recipes (ARN, name, platform, version, owner, parent image, component ARN references), account-owned container recipes (ARN, name, container type, platform, version, parent image, target ECR repository name, KMS key reference, component ARN references), infrastructure configurations (ARN, name, instance types, instance profile name, subnet, security groups, SNS topic, S3 logging location, key-pair-configured boolean, terminate-on-failure), and distribution configurations (ARN, name, target regions, lifecycle timestamps). Relationships: pipeline-to-image-recipe/container-recipe/infrastructure-configuration/distribution-configuration (keyed by the in-service ARNs Image Builder reports, targeting the respective `aws_imagebuilder_*` nodes), pipeline-to-execution-role (role ARN, targets `aws_iam_role`), infrastructure-configuration-to-instance-profile (synthesized partition-aware `arn:<partition>:iam::<account>:instance-profile/<name>` from the reported NAME, targets `aws_iam_instance_profile`), to-subnet (bare `subnet-` id, targets `aws_ec2_subnet`), to-security-group (bare `sg-` id, targets `aws_ec2_security_group`), to-SNS-topic (topic ARN, targets `aws_sns_topic`), to-S3-log-bucket (synthesized partition-aware `arn:<partition>:s3:::<bucket>` from the reported NAME, targets `aws_s3_bucket`), container-recipe-to-ECR-repository (synthesized partition-aware `arn:<partition>:ecr:<region>:<account>:repository/<name>` from the reported NAME, targets `aws_ecr_repository`), and container-recipe-to-KMS-key (reported key id/ARN/alias, targets `aws_kms_key`, `target_arn` set only when ARN-shaped). The recipe parent AMI is recorded as an attribute, not an edge (Eshu has no EC2 AMI resource type). Metadata only: the scanner reads only ListImagePipelines, ListImageRecipes/GetImageRecipe, ListContainerRecipes/GetContainerRecipe, ListInfrastructureConfigurations/GetInfrastructureConfiguration, and ListDistributionConfigurations/GetDistributionConfiguration; recipes and container recipes are scoped to `Owner=Self`. It never reads or persists component build-document bodies, Dockerfile template bodies, instance user data, EC2 key pair names (reduced to a configured boolean), scan findings, or build artifacts, and never calls a mutation or run-control API; the accepted SDK surface is `List`/`Get` reads only by construction, proven by a reflection guard test on the adapter interface that also blocks the per-built-image, component-body, workflow, and scan-finding reads. |
| `networkmanager` | AWS Network Manager global networks, sites, devices, links, connections, and core networks (`aws_networkmanager_global_network`/`_site`/`_device`/`_link`/`_connection`/`_core_network`), each keyed by its API-reported ARN (Network Manager ARNs carry no region segment). Relationships: core/site/device/link/connection-to-parent-global-network (keyed by the partition-aware global-network ARN the global-network node publishes), device-to-site and link-to-site (keyed by the site ARN), device-to-link association from GetLinkAssociations (keyed by the link ARN), connection-to-device for each endpoint (keyed by the device ARN), and global-network-to-transit-gateway for each registration (targets `aws_ec2_transit_gateway` keyed by the **bare** `tgw-` id the transit gateway scanner publishes, extracted from the reported transit gateway ARN, with `target_arn` left empty). Child records report only parent ids, so parent edges synthesize the partition-aware `arn:<partition>:networkmanager::<account>:...` parent ARN, never a hardcoded `arn:aws:`, so GovCloud and China edges join real nodes. Network Manager is a **global** service: schedule one claim per account; the SDK adapter pins the control-plane region per partition (us-west-2 commercial, us-gov-west-1 GovCloud, cn-north-1 China). Metadata only: the scanner uses DescribeGlobalNetworks, GetSites, GetDevices, GetLinks, GetConnections, GetLinkAssociations, GetTransitGatewayRegistrations, ListCoreNetworks, and GetCoreNetwork, and never reads route analyses, network routes, network telemetry, or routing policy documents, and never mutates Network Manager state; device serial numbers are omitted and the device `subnet_arn` is retained as context only (no subnet node exists yet). The accepted SDK surface excludes every mutation by construction, proven by a reflection guard test on the adapter interface. |
| `appstream` | Amazon AppStream 2.0 fleets (ARN, name, fleet type ALWAYS_ON/ON_DEMAND, instance type, state, platform, stream view, default-internet-access flag, concurrent-session and user-duration limits, image ARN/name, IAM role ARN), stacks (ARN, name, persistent application-settings enablement and S3 bucket name, home-folders storage-connector S3 bucket names), image builders (ARN, name, instance type, state, platform, base image ARN, IAM role ARN), and images (ARN, name, state, visibility, image type, platform, base image ARN; PRIVATE and SHARED visibility only, never the AWS-managed PUBLIC base-image catalog). Relationships: fleet/image-builder-to-subnet and fleet/image-builder-to-security-group (from VpcConfig, targets `aws_ec2_subnet` and `aws_ec2_security_group` keyed by the bare `subnet-`/`sg-` ids the EC2 scanner publishes), fleet/image-builder-to-IAM-role (targets `aws_iam_role` by the reported role ARN), fleet/image-builder-to-image (targets `aws_appstream_image` by the reported image ARN the image node publishes), fleet-to-stack association (ListAssociatedStacks resolves the stack name to the stack node resource_id, targets `aws_appstream_stack`), and stack-to-S3-bucket for the application-settings and home-folders buckets (targets `aws_s3_bucket` by the partition-aware synthesized `arn:<partition>:s3:::<bucket>` the S3 scanner publishes, never a hardcoded `arn:aws:`). Metadata only: the scanner reads only DescribeFleets, DescribeStacks, DescribeImageBuilders, DescribeImages, ListAssociatedStacks, and ListTagsForResource; it never reads streaming sessions or users (DescribeSessions, DescribeUsers, DescribeUserStackAssociations), never mints a streaming URL (CreateStreamingURL), and never mutates AppStream state. The accepted SDK surface is `Describe`/`List`-only by construction, proven by a reflection guard test on the adapter interface. |
| `cloudhsmv2` | AWS CloudHSM v2 clusters (bare cluster id, state, state message, HSM type, mode FIPS/NON_FIPS, network type IPV4/DUALSTACK, backup policy, backup-retention type/value, embedded HSM ENI placement metadata, and certificate-presence flags) and backups (bare backup id, ARN, state, source cluster, HSM type, mode, never-expires flag, source backup/cluster/region, lifecycle timestamps). CloudHSM v2 clusters have no API ARN, so the cluster node publishes the bare `cluster-` id as its resource_id and the backup node publishes the bare `backup-` id (its ARN is carried but is not the join key). Relationships: cluster-to-VPC (targets `aws_ec2_vpc` by bare `vpc-` id), cluster-to-subnet (one per distinct availability-zone-to-subnet mapping, targets `aws_ec2_subnet` by bare `subnet-` id, de-duplicated across zones), cluster-to-security-group (targets `aws_ec2_security_group` by the bare AWS-managed `sg-` id CloudHSM reports), and backup-to-source-cluster (targets `aws_cloudhsmv2_cluster` by the bare cluster id this scanner also publishes). Metadata only: NEVER reads or persists cryptographic key material, certificate PEM bodies, the cluster certificate signing request body, or the cluster's Pre-Crypto Officer (PRECO) password - certificate state is recorded as a boolean presence flag per field, never the body. The scanner reads only DescribeClusters and DescribeBackups (both return resource tags inline) and never calls InitializeCluster (the flow that surfaces the PRECO password), any mutation API, or any resource-policy read; the accepted SDK surface is `Describe`-only by construction, proven by a reflection guard test on the adapter interface. No `ESHU_AWS_REDACTION_KEY` required. |
| `workspaces` | Amazon WorkSpaces virtual desktops (id, parent directory and bundle ids, operational state, computer/user name as identity metadata, root/user volume encryption flags, and the volume-encryption KMS key reference), registered directories (id, name, alias, registration state, directory type, tenancy, IAM role, WorkSpaces security group, subnet ids, IP access control group ids), account-owned bundles (id, name, owner, bundle type, compute type, root/user volume sizes, backing image id, lifecycle timestamps), and IP access control groups (id, name, description, CIDR access rules). The WorkSpaces describe APIs return no ARNs, so each node publishes a synthesized partition-aware WorkSpaces ARN (`arn:<partition>:workspaces:<region>:<account>:<resource>/<id>`) as its resource_id, derived with the boundary partition so the nodes join in GovCloud and China. Relationships: workspace-to-directory and workspace-to-bundle (internal, keyed by the directory/bundle node's synthesized ARN, targets `aws_workspaces_directory`/`aws_workspaces_bundle`), workspace-to-KMS-key (targets `aws_kms_key` by the reported key ARN), directory-to-Directory-Service-directory (targets `aws_ds_directory` by the BARE `d-...` directory id the ds scanner publishes), directory-to-subnet and directory-to-security-group (target `aws_ec2_subnet`/`aws_ec2_security_group` by the bare `subnet-...`/`sg-...` ids), directory-to-IAM-role (targets `aws_iam_role` by the reported role ARN), and directory-to-IP-access-control-group (internal, targets `aws_workspaces_ip_group`). Metadata only: user names are identity metadata and are kept, but the scanner never reads or persists passwords, directory registration codes, WorkSpace IP addresses, or connection state, and never reboots, rebuilds, starts, stops, terminates, modifies, or otherwise mutates a WorkSpace or any WorkSpaces resource. The accepted SDK surface is `Describe`-only (DescribeWorkspaces, DescribeWorkspaceDirectories, DescribeWorkspaceBundles, DescribeIpGroups, DescribeTags) by construction, proven by a reflection guard test on the adapter interface. |
| `outposts` | AWS Outposts outposts (ARN, short id, name, lifecycle status, Availability Zone and AZ id, owner account, parent site id, supported hardware type RACK/SERVER), regional sites (ARN, short id, name, account id — operational identity ONLY), and rack/server assets (asset id, type, rack id, compute lifecycle state ACTIVE/ISOLATED/RETIRING/INSTALLING, and rack-unit elevation). Relationships: outpost-to-site (`target_type=aws_outposts_site`, keyed by the site ARN the site node publishes) and asset-to-outpost (`target_type=aws_outposts_outpost`, keyed by the outpost ARN the outpost node publishes). Assets have no AWS ARN, so the asset `resource_id` is synthesized under the parent outpost ARN (`<outpost-arn>/asset/<asset-id>`), staying partition-aware in GovCloud and China without a hardcoded `arn:aws:`. Intentionally low-edge: cross-references that name an Outpost from another service (a subnet, EBS volume, or load balancer carrying an `OutpostArn`) are reverse edges owned by the VPC/EC2/ELB scanners and are not invented here. Metadata only: the scanner reads only ListOutposts, GetOutpost, ListSites, GetSite, ListAssets, and ListTagsForResource, and NEVER reads or persists physical site street addresses, ISO country codes, city/state/region, free-form site notes, shipping or contact details, or rack physical-property logistics; it never calls GetSiteAddress, any order/billing/connection/catalog/pricing/capacity read, or any Outposts mutation API. The accepted SDK surface excludes every address read and mutation by construction, proven by a reflection guard test on the adapter interface. |
| `pinpoint` | Amazon Pinpoint applications/projects (id, ARN, name, creation time, tags), segments (id, ARN, name, parent application id, segment type DIMENSIONAL/IMPORT, version, an imported-from-S3 presence flag with the import format and aggregate endpoint count, lifecycle timestamps), and per-channel settings (channel type EMAIL/SMS/APNS/GCM/VOICE, enabled and archived flags, version, lifecycle timestamps; for the email channel additionally the referenced SES configuration set name and the SES identity name). Relationships: application-to-segment (`target_type=aws_pinpoint_segment` by the segment ARN, keyed/sourced on the application id the application node publishes), channel-in-application (`target_type=aws_pinpoint_application` by the application id, sourced on the synthesized `<application-id>/<channel-type>` channel id), email-channel-to-SES-identity (`target_type=aws_ses_email_identity`; Pinpoint reports the SES identity ARN, the scanner extracts the bare identity name from the `:identity/<name>` ARN to match the SES email-identity scanner's published resource_id and preserves the reported ARN as `target_arn`, skipping the edge when the value is not an SES identity ARN), and email-channel-to-SES-configuration-set (`target_type=aws_ses_configuration_set` by the configuration set name the SES configuration-set scanner publishes). SNS for non-email channels is recorded as channel metadata, not an edge. Metadata only and PII-free: the scanner reads only GetApps, GetSegments, GetChannels, and GetEmailChannel, and NEVER reads or persists endpoint records, endpoint addresses, phone numbers, the email from-address, message or template content, segment targeting dimensions or segment-group criteria, the import S3 URL, or the import external id; it never sends a message and never mutates Pinpoint state. The accepted SDK surface excludes every endpoint read, send, export, and mutation API by construction, proven by a reflection guard test on the adapter interface. |
| `vpclattice` | Amazon VPC Lattice service networks (name, ARN, id, associated service/VPC/resource-configuration counts, lifecycle timestamps), services (name, ARN, id, status, custom domain name, DNS entry domain name, auth type, ACM certificate ARN reference), target groups (name, ARN, id, type IP/LAMBDA/INSTANCE/ALB, protocol, port, IP address type, status, backing VPC id, served service ARNs, registered target count), and listeners (name, ARN, id, protocol, port, parent service identity). Relationships: service-network-to-VPC (targets `aws_ec2_vpc` by the bare `vpc-` id the EC2 scanner publishes), service-network-to-service and target-group-to-service (target `aws_vpclattice_service` by the service ARN this scanner publishes), listener-in-service (targets `aws_vpclattice_service` by the parent service ARN), target-group-to-VPC (targets `aws_ec2_vpc` by the bare `vpc-` id), service-to-ACM-certificate (targets `aws_acm_certificate` by the `GetService`-reported certificate ARN), and target-group-to-target keyed by the target group type: LAMBDA targets `aws_lambda_function` by the function ARN, INSTANCE targets `aws_ec2_instance` by the bare `i-` id (a forward reference until an EC2 instance scanner exists), and ALB targets `aws_elbv2_load_balancer` by the load balancer ARN. IP target groups register raw IP addresses and emit no target edge, and any target whose id does not match the shape its type implies is skipped rather than dangled. Metadata only: the scanner reads only ListServiceNetworks, ListServiceNetworkVpcAssociations, ListServiceNetworkServiceAssociations, ListServices, GetService, ListListeners, ListTargetGroups, GetTargetGroup, ListTargets, and ListTagsForResource, and never reads or persists an auth-policy body (no GetAuthPolicy), a resource-policy body (no GetResourcePolicy), or any data-plane payload, and never mutates VPC Lattice state; the accepted SDK surface is `List`/read-only-`Get`-only by construction, proven by a reflection guard test on the adapter interface. |
| `verifiedaccess` | Amazon Verified Access instances (id, FIPS-enabled flag, customer-managed-KMS-key encryption flag, attached trust-provider ids, lifecycle timestamps), groups (id, ARN, owning account, parent instance id, encryption flag, lifecycle timestamps), endpoints (id, parent group/instance ids, endpoint type, attachment type, application/endpoint domains, status, ACM certificate ARN reference, attached subnet/security-group ids, backing load-balancer ARN or network-interface id, lifecycle timestamps), and trust providers (id, trust-provider/user-trust-provider/device-trust-provider types, policy reference name, OIDC issuer reference, encryption flag, lifecycle timestamps). Although Verified Access ships under the EC2 SDK it is its own `service_kind` (`verifiedaccess`) with its own `aws_verifiedaccess_*` resource types, distinct from the core `ec2` scanner. Relationships: group-in-instance (targets `aws_verifiedaccess_instance` by the synthesized partition-aware instance ARN the instance node publishes), endpoint-in-group (targets `aws_verifiedaccess_group` by the group ARN the group node publishes), instance-uses-trust-provider (targets `aws_verifiedaccess_trust_provider` by the synthesized trust-provider ARN), endpoint-to-subnet (targets `aws_ec2_subnet` by the bare `subnet-` id the EC2 scanner publishes), endpoint-to-security-group (targets `aws_ec2_security_group` by the bare `sg-` id), and endpoint-to-ACM-certificate (targets `aws_acm_certificate` by the reported certificate ARN, emitted only when ARN-shaped). IAM Identity Center usage is recorded as the `uses_iam_identity_center` attribute rather than a graph edge because AWS exposes no IAM Identity Center instance ARN on the trust provider. Instances, endpoints, and trust providers carry no API ARN, so the scanner synthesizes partition-aware `arn:<partition>:ec2:<region>:<account>:verified-access-<kind>/<id>` ARNs via the boundary partition (GovCloud and China resolve correctly), never a hardcoded `arn:aws:`. Metadata only: the scanner reads only DescribeVerifiedAccessInstances, DescribeVerifiedAccessGroups, DescribeVerifiedAccessEndpoints, and DescribeVerifiedAccessTrustProviders, and never reads or persists trust-provider client secrets, OIDC client identifiers, group/endpoint policy documents, or any data-plane payload, and never mutates Verified Access state; the accepted SDK surface is `DescribeVerifiedAccess*`-only by construction, proven by a reflection guard test on the adapter interface. |
| `quicksight` | Amazon QuickSight data sources (id, ARN, connector type, status, VPC-connection reference, resolvable backing-store reference, secret-configured boolean flag, lifecycle timestamps), datasets (id, ARN, import mode SPICE/DIRECT_QUERY, the data sources its physical tables read), dashboards (id, ARN, published version number, the datasets the published version reads), and analyses (id, ARN, status, the datasets it reads). Relationships, each keyed to the identity the target scanner publishes: data-source-to-Redshift-cluster (`aws_redshift_cluster` by bare cluster id), data-source-to-RDS-instance (`aws_rds_db_instance` by bare DB instance id), data-source-to-Athena-workgroup (`aws_athena_workgroup` by bare workgroup name), data-source-to-S3-bucket (`aws_s3_bucket` by the partition-aware synthesized `arn:<partition>:s3:::<bucket>`, derived from the boundary partition, never a hardcoded `arn:aws:`) — each emitted only when the connector reports a resolvable target id; data-source-to-security-group and data-source-to-subnet (`aws_ec2_security_group`/`aws_ec2_subnet` by bare id, from the resolved VPC connection summary, emitted only when the data source uses a VPC connection); dataset-to-data-source (internal `aws_quicksight_data_source` by data-source ARN); and dashboard-to-dataset and analysis-to-dataset (internal `aws_quicksight_data_set` by dataset ARN). Metadata only: the scanner reads ListDataSources, DescribeDataSet, DescribeDashboard, DescribeAnalysis, ListDataSets, ListDashboards, ListAnalyses, ListVPCConnections, and ListTagsForResource (threading the boundary AWS account id required by nearly every QuickSight API), and never reads or persists data-source credentials, connection passwords, secret connection parameters, the Secrets Manager secret value, custom-SQL query bodies, dataset column data, row-level security values, or dashboard/analysis visual definitions; the accepted SDK surface excludes every mutation, ingestion, embed-URL, and permissions/credential read by construction, proven by a reflection guard test on the adapter interface. A not-subscribed account returns an empty result with a warning rather than failing the scan; genuine authorization failures still fail. No `ESHU_AWS_REDACTION_KEY` required. |
| `computeoptimizer` | AWS Compute Optimizer recommendation summaries (one per resource type: finding-class counts and aggregated savings-opportunity percentage) and per-resource recommendations for EC2 instances, Auto Scaling groups, EBS volumes, and Lambda functions (analyzed resource identity, finding, current shape, top-ranked recommended shape, and savings-opportunity percentage). Relationships: recommendation-to-instance (`target_type=aws_ec2_instance`, keyed by the bare instance id `i-...` extracted from the analyzed instance ARN, the convention EC2 instance targets use; `target_arn` unset), recommendation-to-Auto-Scaling-group (`target_type=aws_autoscaling_group`, keyed by the group name the `autoscaling` scanner publishes, with the analyzed group ARN carried as an edge attribute; `target_arn` unset), and recommendation-to-function (`target_type=aws_lambda_function`, keyed by the function ARN the `lambda` scanner publishes). EBS volume recommendations carry no edge — Eshu does not scan an EBS volume resource and there is no forward-reference anchor, so the volume identity is recorded as metadata rather than a dangling edge. An account not enrolled in Compute Optimizer is not an error: the scan returns cleanly with no facts. Metadata only: the scanner reads only GetRecommendationSummaries, GetEC2InstanceRecommendations, GetAutoScalingGroupRecommendations, GetEBSVolumeRecommendations, and GetLambdaFunctionRecommendations; it never changes enrollment, never writes recommendation preferences, never starts an export, and never persists the CloudWatch utilization metric data points or customer cost data points behind a recommendation. The accepted SDK surface is `Get`-only by construction, proven by a reflection guard test on the adapter interface. |
| `controltower` | AWS Control Tower landing zone (ARN, deployed version, latest-available version, deployment status, drift status), enabled controls (ARN, control identifier, governed OU target, deployment and drift status), and enabled baselines (ARN, baseline identifier, enabled version, governed target, deployment status). Relationships: enabled-control-governs-target and enabled-baseline-governs-target (targets `aws_organizations_organizational_unit` / `aws_organizations_account` / `aws_organizations_root` keyed by the **bare** Organizations id — `ou-…`, the 12-digit account id, or `r-…` — parsed from the Control Tower target ARN so it joins the `organizations` scanner node; `target_arn` is left blank because that scanner keys those nodes by bare id, and the original ARN is kept as a `target_arn` edge attribute), and enabled-baseline-for-landing-zone (internal, targets `aws_controltower_landing_zone` by the single landing-zone ARN the management account governs). An edge is skipped, never dangled, when the target ARN is missing, is not an Organizations ARN, or names an unknown family. Metadata only: the scanner reads only ListLandingZones, GetLandingZone, ListEnabledBaselines, ListEnabledControls, and ListTagsForResource; it never reads or persists the landing-zone manifest JSON body, control or baseline parameter values, or any governance payload, and never enables, disables, resets, creates, updates, or deletes Control Tower state. The accepted SDK surface is List/Get reads only by construction, proven by a reflection guard test on the adapter interface. Pairs with the `organizations` scanner. |
| `proton` | AWS Proton environments (name, ARN, environment template name/version, provisioning mode, deployment status, reported Proton service-role ARN, provisioning account id), services (name, ARN, service template name, status, and source-repository linkage by reference only: branch name, repository id, CodeStar connection ARN), environment templates and service templates (name, ARN, display name, provisioning/pipeline-provisioning mode, recommended version). Relationships: service-to-environment (`target_type=aws_proton_environment`, an internal edge derived from Proton service instances and keyed by the environment ARN the environment node publishes; repeated instances of one service in one environment collapse to a single edge, and a placement that names an unobserved environment is skipped rather than dangled) and environment-to-IAM-role (`target_type=aws_iam_role`, keyed by the reported `ProtonServiceRoleArn`, the role ARN the IAM scanner publishes; a non-ARN identifier emits no edge so no fabricated ARN is keyed). No template-to-S3-bundle edge is emitted because no Proton read API exposes the template bundle bucket on its output. Metadata only: the scanner reads only ListEnvironments, ListServices, GetService, ListEnvironmentTemplates, ListServiceTemplates, ListServiceInstances, and ListTagsForResource, and NEVER reads or persists service/environment spec manifest bodies (the `GetService` `Spec` field is never mapped), pipeline spec bodies, template version schema bodies, or deployment/service-instance input parameter values. The accepted SDK surface excludes every mutation, sync-status/config reader, deployment-output reader, and provisioned-resource reader by construction, proven by a reflection guard test on the adapter interface. |
| `servicequotas` | Applied AWS Service Quotas quotas (`aws_servicequotas_service_quota`) per service in the claimed account and region: quota identity (service code, service name, quota code, quota name, quota ARN), the applied value, the AWS-published default value, an `overridden` flag set only when the applied value and default are both known and differ (an approved increase or decrease), the adjustable/global flags, the unit, the rate period (unit and magnitude for rate quotas), the applied level (ACCOUNT/RESOURCE/ALL), the optional resource-level quota context (context id, scope, scope type), and the CloudWatch usage-metric identity (namespace, name, dimensions, recommended statistic). Relationships: none. A quota references an AWS service *code*, not a scanned resource, so the scanner emits no cross-service edge to avoid dangling the graph; the service code is recorded as a quota attribute instead. The quota node publishes its resource_id as the API-supplied quota ARN (falling back to a stable `<service_code>/<quota_code>` key), so no ARN is ever synthesized and there is no hardcoded-partition risk. Metadata only: the scanner reads only ListServices, ListServiceQuotas, and ListAWSDefaultServiceQuotas (joining applied values against defaults by quota code in memory), records the usage metric as identity only (never a metric sample value), and never requests, modifies, or deletes a quota, never associates or templates a quota-increase, and never reads quota-change request history; the accepted SDK surface is `List`-only by construction, proven by a reflection guard test on the adapter interface. |
| `auditmanager` | AWS Audit Manager assessments (ARN, id, compliance standard, status, framework reference, assessment-reports destination type, in-scope account ids, lifecycle timestamps), assessment frameworks (ARN, id, compliance standard, framework type Standard/Custom, control-set and control counts, timestamps), and controls (ARN, id, control type Standard/Custom/Core, evidence data-source category names, timestamps). Relationships: assessment-to-framework (`aws_auditmanager_framework` by the framework ARN the framework node publishes), assessment-to-S3-reports-bucket (`aws_s3_bucket` by the partition-aware synthesized `arn:<partition>:s3:::<bucket>` parsed from the `s3://` assessment-reports destination, derived from the boundary partition, never a hardcoded `arn:aws:`), assessment-to-KMS-key (`aws_kms_key` by the account-level settings customer managed key ARN from GetSettings, `target_arn` set only when ARN-shaped), and assessment-to-account for each in-scope account (`aws_account` by the partition-aware `arn:<partition>:iam::<account-id>:root` the config/access-analyzer/ds scanners also target). In-scope service names are deprecated by AWS (returned empty) and recorded as an assessment attribute, not an edge. Metadata only: the scanner reads GetAccountStatus, ListAssessments, GetAssessment, ListAssessmentFrameworks, ListControls, GetSettings, and ListTagsForResource, and never reads or persists collected audit evidence, evidence finder records, change logs, delegation comments, control narratives (testing information, action-plan instructions, control-mapping source bodies), or assessment report URLs; the accepted SDK surface excludes GetEvidence and every evidence reader, GetChangeLogs, GetDelegations, GetAssessmentReportUrl, GetControl, the insights readers, and every mutation by construction, proven by a reflection guard test on the adapter interface. An account that has not enabled Audit Manager returns an empty result with an `auditmanager_not_registered` warning rather than failing the scan. No `ESHU_AWS_REDACTION_KEY` required. |
| `rolesanywhere` | AWS IAM Roles Anywhere trust anchors (name, ARN, id, enabled state, source type AWS_ACM_PCA/CERTIFICATE_BUNDLE/SELF_SIGNED_REPOSITORY, the backing ACM PCA CA ARN for ACM-PCA anchors, lifecycle timestamps), profiles (name, ARN, id, enabled state, session duration, accept-role-session-name and require-instance-properties flags, a boolean session-policy-configured presence flag, attribute-mapping count, the assumable IAM role ARNs, and the managed-policy ARNs), and imported certificate revocation lists (name, ARN, id, enabled state, associated trust-anchor ARN). Relationships, each keyed to the identity the target scanner publishes: profile-to-IAM-role (`aws_iam_role` by the role ARN the IAM scanner publishes, deduplicated, emitted per assumable role), trust-anchor-to-ACM-PCA (`aws_acmpca_certificate_authority` by the CA ARN the acmpca scanner publishes, emitted only for AWS_ACM_PCA trust anchors that report a CA ARN), and CRL-to-trust-anchor (internal `aws_rolesanywhere_trust_anchor` by the trust-anchor ARN this scanner publishes, emitted only when AWS reports an associated trust anchor). The scanner never synthesizes an ARN; it forwards reported ARNs verbatim, so GovCloud (`aws-us-gov`) and China (`aws-cn`) partitions are preserved and never rewritten to a literal `arn:aws:`. Metadata only: the scanner reads only ListTrustAnchors, ListProfiles, ListCrls, and ListTagsForResource, and NEVER reads or persists certificate private material, PEM x509 certificate bundles, CRL body bytes (never calls GetCrl), inline session policy documents, certificate attribute-mapping rule contents, or vended session credentials (never calls GetSubject/ListSubjects); the accepted SDK surface is `List`-only by construction, proven by a reflection guard test on the adapter interface. No `ESHU_AWS_REDACTION_KEY` required. |
| `verifiedpermissions` | Amazon Verified Permissions policy stores (ARN, policy store id, validation mode OFF/STRICT, deletion-protection flag, encryption-state label DEFAULT/KMS, Cedar language version, lifecycle timestamps, tags), policies (id, type STATIC/TEMPLATE_LINKED, effect Permit/Forbid, parent policy store id — metadata only, the Cedar policy statement body is NEVER read), and identity sources (id, principal Cedar entity type, provider kind cognito/oidc, the non-secret provider reference, and an application-client-id COUNT). Relationships: policy-in-store and identity-source-in-store (internal, `target_type=aws_verifiedpermissions_policy_store`, keyed by the policy store ARN the store node publishes; policy and identity source nodes have no API ARN, so each node publishes the qualified `<policy-store-id>/<id>` and sources its own edge on that value) and identity-source-to-Cognito-user-pool (`target_type=aws_cognito_user_pool`, keyed by the BARE user pool id parsed out of the reported `arn:<partition>:cognito-idp:...:userpool/<user-pool-id>` ARN — the exact resource_id the `cognito` scanner publishes — with `target_arn` left empty and the full ARN carried as an edge attribute; a malformed user pool ARN or an OIDC provider emits no edge rather than dangling one). Metadata only: the scanner reads only ListPolicyStores, GetPolicyStore (with Tags), ListPolicies, and ListIdentitySources, and NEVER reads or persists Cedar policy statement bodies (no GetPolicy), the schema body (no GetSchema), policy template bodies (no GetPolicyTemplate), the customer-managed KMS key ARN or encryption context (reduced to the DEFAULT/KMS label), or the application client id values (reduced to a count); it never evaluates an authorization request (no IsAuthorized/BatchIsAuthorized) and never mutates Verified Permissions state. The accepted SDK surface excludes every body read, authorization evaluation, and mutation by construction, proven by a reflection guard test on the adapter interface. Partition derived from the reported ARNs, never a hardcoded `arn:aws:`. No `ESHU_AWS_REDACTION_KEY` required. |
| `cleanrooms` | AWS Clean Rooms collaborations (`aws_cleanrooms_collaboration`: id, ARN, name, creator account id and display name, member status, analytics engine, lifecycle timestamps), configured tables (`aws_cleanrooms_configured_table`: id, ARN, name, analysis method, the analysis-rule *type* names, the count of allowed columns, and the backing-table reference kind glue/athena/snowflake), and memberships (`aws_cleanrooms_membership`: id, ARN, the associated collaboration identity, member abilities, status, lifecycle timestamps). Relationships: configured-table-to-Glue-table (`target_type=aws_glue_table`, keyed by the `<database>/<table>` resource_id the `glue` scanner publishes for a table node — not an ARN, so `target_arn` stays empty — emitted only when the backing table is a Glue table and both Glue names are present; Athena and Snowflake backing tables record their reference kind only and emit no edge because no Athena/Snowflake table scanner exists to join) and membership-in-collaboration (internal `aws_cleanrooms_collaboration` keyed by the collaboration ARN the collaboration node publishes). An edge is skipped, never dangled, when its target identity is missing. Metadata only: the scanner reads only ListCollaborations, ListConfiguredTables, GetConfiguredTable (called only to resolve the Glue backing-table reference and the allowed-column count), ListMemberships, and ListTagsForResource, and NEVER runs a protected query or job, reads a protected-query result, reads an analysis-rule or analysis-template body, persists allowed-column names (only their count), or maps the Snowflake `SecretArn`/Athena output location or any query/connection identifier, and never mutates Clean Rooms state. The accepted SDK surface is List/Get reads only by construction, proven by a reflection guard test on the adapter interface. |

IAM, Route 53, and CloudFront are global-style families. Use a concrete global
region label such as `aws-global` so claims keep the
`(account_id, region, service_kind)` shape. Organizations and Identity Center
(`ssoadmin`) use the `us-east-1` control-plane endpoint and require
management-account or delegated-administrator credentials. Global Accelerator
(`globalaccelerator`) reports global accelerators but its control plane is
reachable only at the `us-west-2` endpoint, so schedule a `us-west-2` claim; the
SDK adapter pins its client region to `us-west-2` regardless of the claim
region.

WAFv2 is dual-scope: a regional claim scans the REGIONAL scope for its region,
and an `aws-global` claim scans the global CLOUDFRONT scope through the
us-east-1 control-plane endpoint. Schedule both shapes to cover regional and
CloudFront web ACLs.

## Data Boundaries

The collector does not read S3 object contents, SQS messages, DynamoDB table
data, EFS file contents or NFS file system policy bodies, FSx file contents,
FSx Active Directory self-managed credentials (the Windows and ONTAP
self-managed AD password, service-account user name, administrators group, DNS
server IPs, and domain-join secret ARN), FSx ONTAP fsxadmin passwords, FSx
storage virtual machine admin passwords, RDS database
contents, DocumentDB database document contents, DocumentDB collections,
DocumentDB indexes, DocumentDB cluster parameter values, DocumentDB master
user passwords or secrets, DocumentDB snapshot
contents, Neptune master user passwords or secrets, Neptune cluster parameter
values, Neptune snapshot contents, Neptune Analytics graph vertex or edge
contents, Neptune Analytics `ExecuteQuery` results, Redshift warehouse queries, Redshift table data,
Redshift snapshot contents, Redshift master user passwords or admin passwords,
ElastiCache cache keys, cache values, AUTH tokens, user passwords, user access
strings, or snapshot data, MemoryDB cache keys, cache values, AUTH tokens, user
passwords, user access strings, or snapshot data, CloudWatch log events, CloudWatch metric data
points, CloudWatch dashboard body JSON, CloudWatch Contributor Insights rule
definitions, Secrets Manager secret
values, SSM parameter values, API Gateway execution payloads, Lambda code
packages, CloudFront origin payloads, private keys, raw SNS endpoints, raw
EventBridge target inputs, Athena query result rows, Athena named-query SQL
bodies, Athena prepared-statement query bodies, Athena query history strings,
Glue job script bodies, Glue default-argument values, Glue connection
passwords or JDBC credential URLs, Glue workflow graph payloads, Glue table
column statistics with sample values, Glue classifier custom patterns, MSK
Kafka topic or message data, MSK broker logs, MSK broker `server.properties`
bodies, MSK configuration revision bodies, MSK bootstrap broker endpoints, MSK
SCRAM secret material, Amazon MQ broker user passwords, Amazon MQ
configuration XML bodies, Amazon MQ queue or topic message contents, Step
Functions execution input or output, Step
Functions execution history events, Step Functions activity task tokens, AWS
Backup recovery point contents, AWS Backup vault access policy bodies, AWS
Backup framework control input parameter values, AWS Backup recovery-point
restore metadata values, or IAM/resource policy JSON unless a service
package explicitly documents a sanitized metadata-only exception. Step Functions state machine definitions
are persisted only as state names, state types, structural transitions, and
Task Resource ARNs; Parameters, ResultPath, ResultSelector, InputPath,
OutputPath, and Result literal contents are excluded.
GuardDuty finding bodies, GuardDuty filter criteria, GuardDuty threat intel set
list contents, and GuardDuty IP set list contents are also out of scope.
Inspector v2 finding details (CVE, package version, affected host ARN),
Inspector v2 filter criteria expressions, filter descriptions, filter reasons,
and CIS scan results are out of scope; the Inspector v2 scanner emits account
status, enabled scan features, member accounts, filter names, and CIS scan
configuration metadata only, and makes no finding-listing or finding-aggregation
call.

Amazon Macie is the highest-redaction scanner in the collector because Macie's
product is detecting personally identifiable information. Macie sensitive-data
findings (the PII detection results themselves), custom data identifier regular
expression bodies (which are themselves descriptions of the sensitive data the
customer is detecting), allow list contents, findings filter criteria, and
classification-job bucket-criteria expressions and explicit bucket lists are out
of scope. The Macie scanner never calls `GetSensitiveDataOccurrences`,
`GetSensitiveDataOccurrencesAvailability`, `GetFindings`, `ListFindings`,
`GetCustomDataIdentifier`, `BatchGetCustomDataIdentifiers`,
`TestCustomDataIdentifier`, `GetAllowList`, `GetFindingsFilter`,
`DescribeClassificationJob`, `DescribeBuckets`, or any mutation API; the
accepted SDK surface excludes them by construction, proven by a reflective guard
test on the SDK adapter `apiClient` interface and by struct-reflection tests on
the scanner-owned types, which carry identity and counts only and have no field
able to hold a regex body, list contents, finding detail, or criteria. The
scanner emits the Macie session status, member accounts (email-free),
classification-job metadata with an aggregate bucket-criteria summary count,
allow list and custom data identifier identities, findings filter identities,
and aggregate finding counts by severity only. Member email addresses are
personal contact data and are never read.
CloudTrail audit event payloads, Lake query strings, Lake query result rows,
event selector bodies, and dashboard widget query SQL stay outside the
collector contract; the CloudTrail scanner emits trail and Lake configuration
only, summarizing selectors as bounded counts.
AWS Config recorded configuration item bodies (full resource snapshots),
per-resource compliance evaluation result bodies, and custom-rule Lambda code
are out of scope. The Config scanner emits configuration recorder, delivery
channel, config rule, conformance pack, configuration aggregator, and retention
configuration metadata only; it never calls `GetResourceConfigHistory`,
`BatchGetResourceConfig`, `GetDiscoveredResourceCounts`,
`GetComplianceDetailsByConfigRule`, `GetComplianceDetailsByResource`,
`GetCustomRulePolicy`, or any `Put`/`Delete`/`Start`/`Stop` mutation.
Aggregate per-rule compliance from `DescribeConformancePackCompliance` is read
only to derive the conformance pack member-rule set and count. The config rule
resource-type scope is carried as a rule attribute; it is not emitted as an edge
to a synthetic resource-type node. Synthesized account root ARNs for the
aggregator-to-source-account edge derive the partition from the aggregator ARN,
so GovCloud and China edges stay correct.

Transit gateway routes, transit gateway multicast group memberships, and
transit gateway policy table rule bodies are out of scope. The Transit Gateway
scanner reads transit gateway, route table, attachment, peering attachment,
multicast domain, and policy table identity, ownership, state, and bounded
option metadata only; it never calls `SearchTransitGatewayRoutes` or
`GetTransitGatewayPolicyTableEntries`, and never mutates transit gateway
resources or changes route-table association or propagation. Cross-account peer
transit gateway identities are surfaced exactly as AWS reports them and flagged
for org-context resolution; the scanner does not resolve the remote account.

CloudFormation is the highest template-body redaction surface in the collector.
Stack and stack-set template bodies, parameter values (including NoEcho and
SSM-resolved values), change-set bodies, stack policies, and drift property
documents are out of scope. The scanner never calls `GetTemplate`,
`GetTemplateSummary`, `DescribeChangeSet`, `GetStackPolicy`, or any
`Detect*Drift` or mutation API; the accepted SDK surface excludes them by
construction, proven by reflective guard tests on both the scanner-owned
`Client` interface and the SDK adapter `apiClient` interface. Parameter mapping
keeps keys only, the stack-set `TemplateBody` is never carried, drift results
are reduced to per-status counts, and secret-like stack output values are
redacted by output key through the shared redact library. Stack name, ARN,
status, capabilities, role ARN, tags, and timestamps are persisted as
metadata.

ACM certificate body PEM and ACM-issued private key material are out of scope.
The ACM scanner never calls `GetCertificate` or `ExportCertificate`, and ACM
Private CA (acm-pca) APIs are not exercised.

AppSync schema SDL bodies, resolver request/response mapping templates (VTL or
JS), pipeline function code bodies, and API key values are out of scope. The
schema SDL exposes the data model and PII field names; resolver mapping
templates often contain inline authorization conditions; function code is
customer IP; and API key values are bearer credentials. The AppSync scanner
reaches the control plane through `ListGraphqlApis`, `ListDataSources`,
`ListTypes`, `ListResolvers`, `ListFunctions`, `ListApiKeys`, and
`GetSchemaCreationStatus` only. It never calls `EvaluateMappingTemplate`,
`EvaluateCode`, `GetIntrospectionSchema`, `StartSchemaCreation`,
`GetDataSourceIntrospection`, or any mutation API such as
`CreateGraphqlApi`/`UpdateGraphqlApi`/`DeleteGraphqlApi`,
`Create`/`Update`/`Delete` of a resolver, data source, function, or API key. The
accepted SDK surface and the scanner-owned types exclude those bodies by
construction, proven by reflective guard tests on the SDK adapter interface and
a struct-reflection test on the scanner types. The schema is reduced to a
creation status and a type count; type definitions (`Type.Definition`) are never
read. Data-source backing-resource targets (Lambda, DynamoDB, OpenSearch, HTTP,
RDS) are recorded as relationship evidence without the HTTP endpoint
authorization config or the RDS secret store ARN.

KMS key policy Statement bodies, KMS grant encryption contexts, KMS key
material, and the output of any KMS cryptographic operation are out of scope.
The KMS scanner reaches the control plane only through List- and
Describe-class APIs; it never calls Encrypt, Decrypt, GenerateDataKey,
GenerateDataKeyPair, GenerateDataKeyPairWithoutPlaintext,
GenerateDataKeyWithoutPlaintext, Sign, Verify, ReEncrypt, GenerateMac,
VerifyMac, DeriveSharedSecret, GetPublicKey, GenerateRandom, or any key
lifecycle mutation. Only the bounded list of policy revision names from
ListKeyPolicies is persisted; the scanner does not call GetKeyPolicy.

WAFv2 IP set address lists, regex pattern set bodies, and rule `Statement`
bodies are out of scope. The WAFv2 scanner persists the IP set address count
and IP version, the regex pattern count, web ACL and rule group rule counts,
managed rule set references (vendor and name), and reference ARNs only. IP set
addresses (which commonly include private CIDR and threat intel), regex
detection bodies, and `Statement` search strings
(`AndStatement`/`OrStatement`/`NotStatement`/`ByteMatchStatement`) are never
read into facts. The scanner reaches the control plane through List- and
Get-class APIs only; it never calls a mutation API such as `CreateWebACL`,
`UpdateWebACL`, `DeleteWebACL`, `AssociateWebACL`, `DisassociateWebACL`, any
`Create`/`Update`/`Delete` rule group, IP set, or regex pattern set operation,
or `PutLoggingConfiguration`. WAF Classic (v1) is out of scope by construction:
the scanner imports only the WAFv2 SDK, which cannot surface `waf` or
`waf-regional` v1 resources.

Security Hub finding aggregate counts are metadata-only when grouped by bounded
posture fields such as severity, standard, control, compliance status, and
workflow status. Security Hub finding bodies, resource IDs from findings,
resource details, remediation text, product fields, user-defined fields, note
text, network/process details, and insight filter expressions remain outside
the collector contract.

Cognito user records are PII and are out of scope. The Cognito scanner never
calls ListUsers, AdminGetUser, AdminListGroupsForUser, or ListUsersInGroup;
those methods are absent from both the scanner `Client` interface and the SDK
adapter's API interfaces, enforced by reflection tests. App-client ClientSecret,
identity-provider ProviderDetails (client_secret, google_client_secret, and
similar federation secrets), custom message Lambda templates, MFA SMS
configuration access tokens, and identity-pool role-mapping rule bodies are
never persisted. The scanner reports control-plane metadata only and routes
operator-supplied free text (identity-pool developer provider names and group
descriptions) through the shared AWS redaction path.

Organizations policy attachment metadata is in scope: policy ID, policy name,
policy type, and target binding. Policy document bodies, statements,
conditions, action lists, and guardrail text are out of scope by default.
Account email and account name values must pass through the shared AWS
redaction path before persistence.

IAM Identity Center (`ssoadmin`) permission set metadata is in scope: name,
description, session duration, relay state, and managed-policy ARN or
customer-managed-policy name references. Permission set inline policy bodies
(`GetInlinePolicyForPermissionSet`) and permissions boundary bodies
(`GetPermissionsBoundaryForPermissionSet`) are out of scope by default; they
encode the org least-privilege model and live in IAM. Customer-managed policy
bodies are never read; only the attachment name and path are persisted.
Application instance access-scope attributes (`GetApplicationAccessScope`,
`ListApplicationAccessScopes`) are out of scope because they can carry
sensitive group filters. Principal display names resolved from the connected
identity store pass through the shared AWS redaction path; only the identity
store display name is read, never addresses, emails, phone numbers, structured
name, or group memberships.

Kinesis stream records (PutRecord, PutRecords, GetRecords, GetShardIterator
class), Kinesis Video media fragments (GetMedia, PutMedia,
GetMediaForFragmentList), the Firehose processing-configuration Lambda body,
Firehose HTTP endpoint access keys, Firehose Splunk HEC tokens, and Firehose
Redshift passwords or SecretsManager material stay outside the collector
contract. The Kinesis scanner reaches the three sub-service control planes only
through List- and Describe-class APIs and never mutates a data stream, delivery
stream, or video stream.

It also does not call AWS mutation APIs. If a scanner needs a new API family,
update the owning service package README with source APIs, forbidden data
classes, emitted evidence, and verification.

Access Analyzer has an extra security boundary: external finding bodies,
archive-rule filter criteria, policy-generation results, and per-action
unused-access details are not persisted. The scanner keeps aggregate finding
counts by status and resource type, plus per-resource unused-access
last-accessed timestamps.

SageMaker is a metadata-only control-plane scan. The scanner never invokes
endpoints (it never calls InvokeEndpoint or InvokeEndpointAsync, which live in
the separate `sagemakerruntime` API the collector does not link) and never
calls a mutation API. It never persists hyperparameter values (training or
tuning), training/processing/transform input or output data references,
notebook lifecycle-config script bodies, container environment maps, or
pipeline definition bodies. These exclusions are enforced by omission from the
scanner-owned types and by a reflection gate over the SDK adapter that fails
the build if a forbidden method ever becomes reachable.

OpenSearch is a metadata-only control-plane scan that never reaches the
OpenSearch HTTP index/search/data API (`_search`, `_msearch`, `_index`, `_doc`,
`_bulk`, and similar). That API is reachable only over the domain HTTP endpoint,
which the scanner never constructs; the SDK adapter's two interfaces
(`domainAPI` and `serverlessAPI`) carry control-plane reads only and no
`GetIndex` method on either service. A reflection gate over both interfaces
fails the build if a mutation verb, inbound-connection acceptance, or any
index/search/data method ever becomes reachable. Master user passwords are out
of scope: `DescribeDomain` does not return the password, and the scanner-owned
`Domain` type has no password-shaped field, enforced by a struct-shape test.
Domain endpoint contents, the domain `Endpoints` map, the access policy body,
custom package bodies, and serverless saved-object bodies are never persisted.
Only IAM role ARNs referenced by the domain access policy are resolved for
domain-to-IAM-role relationship evidence; the policy body itself is dropped.

Bedrock is a high-redaction metadata-only control-plane scan over the
`bedrock` and `bedrock-agent` SDKs. The collector never links the inference
data-plane modules: it never calls bedrock-runtime (InvokeModel,
InvokeModelWithResponseStream, Converse, ConverseStream) or bedrock-agent-runtime
(InvokeAgent, Retrieve, RetrieveAndGenerate), and it never calls a mutation API.
Three high-IP surfaces are filtered: agent instructions (the system prompt) and
prompt-override configurations, guardrail topic and content policy bodies, and
knowledge base ingested document content and chunks. The scanner also drops
action-group API schema bodies and custom-model hyperparameter and training-input
data. These exclusions are enforced three ways: the scanner-owned types have no
field for any forbidden payload (proven by a struct-reflection gate), the SDK
adapter never calls the operations that return policy bodies or ingested content
(GetGuardrail, GetKnowledgeBaseDocuments), and a reflection gate over both SDK
adapter interfaces fails the build if any inference or mutation method ever
becomes reachable.

AWS Batch is a metadata-only control-plane scan. The scanner never submits,
cancels, or terminates jobs, never registers or deregisters job definitions,
and never mutates a compute environment, job queue, job definition, or
scheduling policy. Those operations are absent from both the scanner `Client`
interface and the SDK adapter's API interface, enforced by a reflection gate
that fails the build if a forbidden method becomes reachable. Job-definition
container command lists (`ContainerProperties.Command`) and job parameters are
never read: the scanner-owned `Container` and `JobDefinition` types do not
declare those fields. Container environment values may carry secrets, so they
are never persisted in clear text; they route through the shared AWS redaction
path and persist only as HMAC markers, which is why the scanner requires
`ESHU_AWS_REDACTION_KEY`. Container secret references persist only as the
Secrets Manager or SSM Parameter Store ARN reference (a relationship edge); the
resolved secret value is never read. Scheduling policy fair-share weight state
(`FairsharePolicy`) is never persisted because priority weights may reveal
business-sensitive tenant ranking. Recent jobs are listed only across active
states (SUBMITTED, PENDING, RUNNABLE, STARTING, RUNNING), bounded per state, and
persist identity, status, and the job-definition reference only.

## Evidence And Telemetry

Scanner packages emit reported `aws_resource`, `aws_relationship`,
`aws_image_reference`, `aws_dns_record`, and `aws_warning` facts. Reducers must
corroborate them before promoting workload, deployment, ownership, drift, or
unmanaged-resource truth.

Runtime spans include `aws.collector.claim.process`,
`aws.credentials.assume_role`, `aws.service.scan`, and
`aws.service.pagination.page`. The metric catalog lives in
[Ingestion And Collector Metrics](../reference/telemetry/metrics-ingestion-collectors.md).

## Change Rules

When adding or widening a scanner, keep it metadata-only unless an active design
record says otherwise. Add scanner tests, SDK-adapter tests, command-side target
validation tests, and registry support through `awsruntime.SupportedServiceKinds`.
Run the performance evidence gate when the scanner adds pagination fanout, claim
concurrency, batch sizing, queue pressure, or downstream reducer work.

## Related Docs

- [AWS Cloud Collector](collector-aws-cloud.md)
- [AWS Collector Security And Config](collector-aws-cloud-security.md)
- [Collector Runtime Services](../deployment/service-runtimes-collectors.md)
