# AGENTS.md - internal/collector/kuberneteslive guidance

## Read First

1. `go/internal/collector/kuberneteslive/README.md` - flow, telemetry, invariants
2. `go/internal/collector/kuberneteslive/source.go` - snapshot orchestration
3. `go/internal/collector/kuberneteslive/builder.go` - object-to-fact mapping
4. `go/internal/collector/kuberneteslive/secrets_iam_observations.go` -
   Kubernetes secrets/IAM source-fact mapping
5. `go/internal/collector/kuberneteslive/envelope.go` - fact envelope contract
6. `go/internal/collector/service.go` - shared collector commit boundary
7. `docs/internal/design/388-kubernetes-live-collector.md` - design and scope
8. `go/internal/telemetry/README.md` - metric and span contract

## Invariants This Package Enforces

- METADATA-ONLY. Never emit Secret values, ConfigMap data payloads, environment
  variable values, or container logs. Only image refs, env var NAMES, ports,
  service account, selector, label metadata, ServiceAccount annotation keys,
  bounded secret-reference counts, RBAC rule summaries, and fingerprinted RBAC
  subject metadata are allowed in payloads. Add a redaction test for any new
  field.
- READ-ONLY. The `Client` interface exposes only list methods. Do not add a
  create, update, patch, delete, exec, attach, portforward, or log method.
- Do not import client-go in this package. The Kubernetes API is the `Client`
  interface; the typed adapter lives in `clientgo`.
- Object identity is `(cluster_id, api_group, version, resource, namespace,
  name, uid)`. Never key identity on the API server URL or labels.
- The generation id must depend only on `cluster_id` and observation time, never
  on partial state, so all facts in a snapshot share one generation id.
- A forbidden or partial list emits a warning and marks the generation partial;
  it must not abort the snapshot or assert completeness.
- This package emits facts only. It must never write graph state or decide
  canonical ownership, drift, effective RBAC, IAM posture, or trust-chain truth.
- Metric labels must not contain namespace names, object names, or image names.

## Common Changes And How To Scope Them

- Add a resource family with a `Source.Next` test that asserts emitted fact
  kinds, scope kind, generation id sharing, and warning behavior, plus a
  `clientgo` adapter test using the fake clientset.
- Add a relationship type by extending `RelationshipType`, the builder edge
  derivation, and a test that proves ambiguity warnings for unresolved targets.
- Add telemetry by updating `source.go`, `go/internal/telemetry`, and the docs
  that list metric type, labels, and purpose.

## Anti-Patterns

- Reading any Secret value, ConfigMap data, env value, or log.
- Writing facts to Postgres or graph directly from this package.
- Inferring cluster identity from the API server URL.
- Treating a forbidden list as "no resources" instead of a partial warning.
- Inventing an owner identity for an owner reference that was not collected.

## Evidence

### CRI-resolved image digest from pod containerStatuses ImageID (#5432)

No-Regression Evidence: `go test ./internal/collector/kuberneteslive/... ./internal/reducer/... -count=1` passes with byte-identical behavior for all pre-existing correlation paths. The CRI-digest path is additive — when no resolved digest exists (Deployments, ReplicaSets, pending pods), behavior stays byte-identical to before. Five new regression tests (`kubernetes_correlation_cri_digest_test.go`) prove: (1) tag-form ref with CRI digest + matching source → exact, edge-eligible; (2) tag without CRI digest stays derived/provenance-only; (3) CRI digest without source observation → unresolved, never tag-derived; (4) CRI-digest-promoted workload produces a RUNS_IMAGE edge; (5) tag without CRI digest produces no edge. Collector tests (`TestAdapterMapsPodContainerStatusDigest`, `TestAdapterDeploymentHasNoResolvedDigest`, `TestNormalizeCRIImageID`) prove the mapping and normalization. Cardinality shim (`TestDigestJoinCardinalityShim`, coherent 6-ref fixture using the same repository for all refs and source manifests): 33% edge-eligible before (2 digest-pinned refs) → 50% edge-eligible after (2 digest-pinned + 1 CRI-digest-promoted tag ref). The B-7 golden-corpus gate unit tests pass (`test-verify-golden-corpus-gate.sh`, `go test ./cmd/golden-corpus-gate/`). Cassette updated with tag-referenced Pod + resolved digest (only on the Pod; Deployment/ReplicaSet entries carry no resolved_image_digest — they use workloadFromPodSpec which never reads status). schema_version 1.1.0 registered in specs/fact-kind-registry.v1.yaml via schema_version_overrides. B-12 snapshot gains rc-153 (RUNS_IMAGE min ≥ 3, non-vacuous). Full Docker B-7 gate run on this branch head: `scripts/verify-golden-corpus-gate.sh` PASS (433 pass, 0 required-fail, 0 advisory-warn; rc-153 RUNS_IMAGE count=3 ≥ 3, KubernetesWorkload nodes=3).
No-Observability-Change: No new metric instrument, metric label, span, structured log field, status field, queue domain, worker count, batch size, or runtime knob. The `resolved_image_digest` payload field is a new optional key on `kubernetes_live.pod_template` containers — malformed values surface through the existing `input_invalid` dead-letter path. The CRI-digest promotion to exact reuses the existing `materialized[digest]` tally and `kubernetes correlation materialization completed` log. The adapter's only `.Status` reads are the pod `ImageID` (this entry, #5432) plus, added by #5431 and described in the entry below, the pod `.Status.Phase` and the Deployment/ReplicaSet `.Status.ReadyReplicas`/`.Status.AvailableReplicas`. Each is metadata — a content fingerprint or an observed count/phase, never a Secret, env, or log value — so the metadata-only invariant is preserved.

### Observed-vs-desired runtime-status fields on pod_template (#5431)

No-Regression Evidence: additive optional workload-level fields only — `desired_replicas`, `ready_replicas`, `available_replicas`, `pod_phase` — on `kuberneteslivev1.PodTemplate`. No existing field, JSON key, or emission path changes; `go test ./internal/collector/kuberneteslive/... ./internal/facts -count=1` passes byte-identical for every pre-existing path. New regression tests: `TestAdapterMapsDeploymentReplicaStatus` and `TestAdapterMapsReplicaSetReplicaStatus` prove `.Spec.Replicas`/`.Status.ReadyReplicas`/`.Status.AvailableReplicas` map onto the workload with `PodPhase` nil; `TestAdapterDeploymentNilSpecReplicasLeavesDesiredNil` proves an unset `Spec.Replicas` leaves `DesiredReplicas` nil rather than fabricating zero; `TestAdapterMapsPodPhase` proves `.Status.Phase` maps onto `PodPhase` with all three replica fields nil; `TestAdapterPodEmptyPhaseLeavesPodPhaseNil` proves an empty phase leaves `PodPhase` nil rather than emitting an empty string. `TestNewPodTemplateEnvelopeEmitsRuntimeStatus` and `TestNewPodTemplateEnvelopePodPhaseOmittedWhenAbsentReplicasSet` lock the encode-seam wire contract (a field added to the struct but not `EncodeKubernetesLivePodTemplate` would be silently dropped). schema_version bumped 1.1.0 -> 1.2.0 (minor, additive) in `specs/fact-kind-registry.v1.yaml` via `schema_version_overrides`. Cassette (`testdata/cassettes/kuberneteslive/supply-chain-demo.json`) updated: all 3 `pod_template` facts bumped to schema_version 1.2.0; the deployment and replicaset facts gain `desired_replicas`/`ready_replicas`/`available_replicas`; the pod fact gains `pod_phase`. B-12 snapshot (`testdata/golden/e2e-20repo-snapshot.json`) note updated additively on `node_counts.KubernetesWorkload` — no node or edge count change, this is fact-level emission only; no reducer, graph writer, or query surface is touched (deferred to the #5435 materialization capstone). `go test ./cmd/golden-corpus-gate/ -count=1`, `bash scripts/test-verify-golden-corpus-gate.sh`, `go test ./cmd/fact-kind-registry/... -count=1`, `bash scripts/verify-fact-kind-registry.sh`, and `bash scripts/test-verify-fact-kind-registry.sh` all pass. JSON Schema regenerated via `go run ./internal/schemagen/cmd` from the `sdk/go/factschema` module root (not hand-edited); the embedded `fixturepack/schema` copy refreshed via `cp`.
No-Observability-Change: No new metric instrument, metric label, span, structured log field, status field, queue domain, worker count, batch size, or runtime knob. The four new payload fields are optional keys on `kubernetes_live.pod_template` — a missing or malformed value surfaces through the existing `input_invalid` dead-letter path, not a new one. No reducer, graph write, or query handler reads these fields yet, so there is no new signal to instrument.

### StatefulSet, DaemonSet, Job, and CronJob workload kinds (#5433)

No-Regression Evidence: additive resource-family listing only — no existing `Client` method, `ListResult` shape, or `pod_template`/`relationship`/`warning` fact contract changes; `schema_version` stays `1.2.0` (no bump — the 4 new kinds reuse the #5431 `desired_replicas`/`ready_replicas`/`available_replicas`/`pod_phase` fields verbatim). `go test ./internal/collector/kuberneteslive/... ./internal/facts ./cmd/collector-kubernetes-live -count=1 -race` passes byte-identical for every pre-#5433 path (233 passed, 4 packages, race-clean). Status-mapping decisions, verified against the pinned `k8s.io/api@v0.34.1`:
- **StatefulSet**: `DesiredReplicas` <- `.Spec.Replicas` (`*int32`, nil when unset); `ReadyReplicas` <- `.Status.ReadyReplicas`; `AvailableReplicas` <- `.Status.AvailableReplicas` — this version of `StatefulSetStatus` DOES carry `AvailableReplicas` (added upstream in 1.22), confirmed by reading the struct directly (`go doc k8s.io/api/apps/v1.StatefulSetStatus` / module-cache source), so it maps directly like Deployment/ReplicaSet, not left nil. `PodPhase` nil.
- **DaemonSet** (design decision, documented on `ListDaemonSets` and in `clientgo/README.md`): `DaemonSetStatus` has no replica-count fields at all (a DaemonSet is scheduled per node, not scaled to a replica count), so its per-node scheduling counts are surfaced as the workload's replica-equivalent on an OBSERVED basis: `DesiredReplicas` <- `.Status.DesiredNumberScheduled`, `ReadyReplicas` <- `.Status.NumberReady`, `AvailableReplicas` <- `.Status.NumberAvailable`. These are int32 (not pointers) upstream, so an observed all-zero DaemonSet (no nodes matched yet) reports zero, not nil — proved by `TestAdapterDaemonSetZeroSchedulingCountsAreObserved`.
- **Job**: no replica concept; `DesiredReplicas`/`ReadyReplicas`/`AvailableReplicas`/`PodPhase` all nil, only the pod template spec is emitted via `workloadFromPodSpec` on `.Spec.Template.Spec`.
- **CronJob**: same as Job (all status nil); the pod template is read from the nested `.Spec.JobTemplate.Spec.Template.Spec`, proved by `TestAdapterMapsCronJobNestedPodSpecOnlyNoReplicaStatus`.

New regression tests in `clientgo/client_workload_kinds_test.go` mirror `client_runtime_status_test.go`: `TestAdapterMapsStatefulSetReplicaStatus`, `TestAdapterStatefulSetNilSpecReplicasLeavesDesiredNil`, `TestAdapterMapsDaemonSetSchedulingCounts`, `TestAdapterDaemonSetZeroSchedulingCountsAreObserved`, `TestAdapterMapsJobPodSpecOnlyNoReplicaStatus`, `TestAdapterMapsCronJobNestedPodSpecOnlyNoReplicaStatus`. Redaction tests in `clientgo/client_workload_kinds_redaction_test.go` mirror `client_redaction_test.go`'s METADATA-ONLY proof: `TestPodTemplateRedactionExcludesStatefulSetStatusDetail` and `TestPodTemplateRedactionExcludesDaemonSetStatusDetail` plant sentinel strings across every non-mapped `.Status` field (conditions, revisions, scheduling/misscheduled counts, observed generation, collision count) and prove the emitted payload carries only the mapped replica values, no sentinel leakage. `source_test.go`'s `TestSourceHappyPathEmitsTypedFacts` now also feeds a StatefulSet through the real `Source.Next` path end to end (3 `pod_template` facts). `clientgo/client.go` stayed under the 500-line cap by moving the 4 new `List*` methods into a new sibling file, `clientgo/client_workload_kinds.go` (140 lines); `client.go` is back to 390 lines (net zero after the extraction).

`builder.go`'s `collectWorkloads` lists all 4 new kinds via the same `paginate` bounded-continuation-token path used by the pre-existing 3; `ownerScope` gained `statefulset`/`daemonset`/`job`/`cronjob` cases so owner-reference resolution (e.g. a Pod owned by a Job) reports the correct resource-scope warning instead of falling through to the raw lowercase kind string. `envelope.go` needed no change (kind-agnostic); the `secrets_iam_observations.go` lane is untouched (RBAC/ServiceAccount collection is orthogonal to workload kinds).

Golden-corpus lockstep: `testdata/cassettes/kuberneteslive/supply-chain-demo.json` gains 2 new `kubernetes_live.pod_template` facts (schema_version 1.2.0, unchanged) — a StatefulSet with replica status (`desired_replicas: 2, ready_replicas: 2, available_replicas: 2`) and a DaemonSet with scheduling-count status mapped onto the replica fields (`desired_replicas: 3, ready_replicas: 3, available_replicas: 2`), both using images (`ghcr.io/eshu-hq/internal-tools/{db-migrator,node-agent}:1.0.0`) that do not correlate to the `ociregistry` cassette's single manifest, so the existing `RUNS_IMAGE` evidence (min 3, rc-153) is unaffected. Job/CronJob get no cassette fixture (collection-path coverage lands via the clientgo adapter tests instead; both leave `pod_template` status fields nil so there is no new status-mapping shape to fixture). `testdata/golden/e2e-20repo-snapshot.json`'s `node_counts.KubernetesWorkload` bumps `min` 3 -> 5 (the 2 new cassette facts each materialize into a distinct `KubernetesWorkload` node via the existing `KubernetesWorkloadMaterializationHandler`, which is kind-agnostic) with the note updated additively. `go test ./cmd/golden-corpus-gate/ -count=1` (81 passed) and `bash scripts/test-verify-golden-corpus-gate.sh` (pass) both green on this branch; the full Docker `scripts/verify-golden-corpus-gate.sh` run is left to the orchestrator's heavier-gate pass per this session's scope.

RBAC lockstep: `go/cmd/collector-kubernetes-live/README.md`'s RBAC block was already stale before #5433 (missing `serviceaccounts` and the `rbac.authorization.k8s.io` families that #388's secrets/IAM lane had already added) — corrected to list every collected family grouped by API group. The Helm chart's actual `ClusterRole` (`deploy/helm/eshu/templates/rbac-kubernetes-live-collector.yaml`) was ALSO missing `statefulsets`/`daemonsets` (apps) and `jobs`/`cronjobs` (batch) — a real gap, not just a doc gap: an operator deploying the chart today would get `forbidden_resource` warnings forever for the 4 new kinds. Fixed in the same change and verified with `helm template deploy/helm/eshu -f <scratch values with one in_cluster target> -s templates/rbac-kubernetes-live-collector.yaml`, confirming the rendered `ClusterRole` rules include all 4 new resources under the correct API groups.

Performance Evidence (`collect_workloads_bench_test.go`, fake clientset, Apple M5 Max, `-benchtime=200x -count=3`): `BenchmarkCollectWorkloadsThreeKinds` (pre-#5433 shape: deployments+replicasets+pods populated, 50 objects each = 150 objects, StatefulSet/DaemonSet/Job/CronJob lists still called but return empty) averages ~930μs/op, ~1.61MB/op, ~22,259 allocs/op across 3 runs. `BenchmarkCollectWorkloadsSevenKinds` (post-#5433: all 7 kinds populated, 50 objects each = 350 objects) averages ~2158μs/op, ~3.85MB/op, ~51,890 allocs/op. The object count grows 2.33x (150 -> 350) and wall time grows 2.32x, allocations grow 2.33x — confirming the added cost is linear in listed-object count, not superlinear, matching the analytical expectation: each new kind is one more bounded, paginated (`Limit: 200`, continue-token) LIST round-trip through the identical `paginate` helper the pre-existing 3 kinds already use, with per-object mapping cost (`workloadFromPodSpec`) identical across kinds. No new lock, transaction, or shared-state path is introduced; each `List*` call is independent and sequential within `collectWorkloads`, matching the existing serial-producer concurrency model documented in `README.md`'s Concurrency section.

No-Observability-Change: No new metric instrument, metric label, span, structured log field, status field, queue domain, worker count, batch size, or runtime knob. The 4 new kinds emit the pre-existing `eshu_dp_kubernetes_resources_listed_total{resource_scope,result}` metric with 4 new `resource_scope` label values (`statefulsets`, `daemonsets`, `jobs`, `cronjobs`) — the label is already a closed, low-cardinality enum (`ResourceScope*` constants) and never carries a namespace or object name, so this is within the existing label contract, not a new one. The adapter's only additional `.Status` reads beyond the #5431/#5432 set are `StatefulSetStatus.ReadyReplicas`/`.AvailableReplicas` (same fields Deployment/ReplicaSet already read) and `DaemonSetStatus.DesiredNumberScheduled`/`.NumberReady`/`.NumberAvailable` — each a bounded numeric count, never a condition message, reason, revision string, or other free-text detail; the redaction tests above prove no such detail leaks. Job and CronJob add zero new `.Status` reads.

### Annotation capture allowlisted to identity-binding keys, review fix on #5471 F2

No-Regression Evidence: the #5471 F2 wiring that carries `WorkloadObject.Meta.Annotations` into the `kubernetes_live.pod_template` fact's `annotations` field was already correct end to end (`TestSourceEmitsWorkloadAnnotations`); this is a capture-boundary fix, not a wiring change. `clientgo.objectMeta` previously called `copyStringMap(meta.Annotations)`, copying every annotation on every collected workload verbatim — unbounded, since Kubernetes annotation values have no size limit the API enforces per-key (only a 256KiB total-annotations-per-object cap), and `kubectl.kubernetes.io/last-applied-configuration` in particular is commonly the full applied manifest re-serialized as JSON (KB to tens of KB) and can embed Secret `data`/`stringData` verbatim when the applied object was a Secret. `objectMeta` now calls a new `filterIdentityAnnotations`, which restricts capture to a fixed `identityAnnotationAllowlist` (`argocd.argoproj.io/tracking-id`, `app.kubernetes.io/instance`, `app.kubernetes.io/name`) — the only annotations any known consumer (Lane B's `BINDS_LIVE_WORKLOAD` correlation) needs. New regression tests in `clientgo/client_annotations_redaction_test.go` drive a real Deployment fixture with a mix of allowlisted and non-allowlisted (including a synthetic large last-applied-configuration-shaped blob) annotations through the real `Adapter.ListDeployments` and `NewPodTemplateEnvelope`: `TestPodTemplateAnnotationsAllowlistFiltersUnboundedMap` proves only the 3 allowlisted keys survive into the payload and the bloat/non-identity keys are dropped; `TestPodTemplateAnnotationsAbsentWhenNoAllowlistedKeys` proves a workload with no allowlisted key emits a nil/absent `annotations` field, matching pre-existing "no annotations observed" behavior. `mustPodTemplatePayload` (the shared redaction-test helper) previously omitted `Annotations` from the `PodTemplateObservation` it builds despite its own doc comment claiming an exact mirror of `addWorkload`'s field copies — fixed to add `Annotations: workload.Meta.Annotations`, a no-op for every pre-existing caller (their fixtures set no annotations, so `sortedStringMap(nil)` still yields nil). No fact-kind, schema-version, or wire-shape change: `annotations` stays `map[string]string`, optional, same JSON key; only the captured content narrows to match the field's documented identity-binding intent. `specs/fact-kind-registry.v1.yaml` `schema_version` and the fixturepack `kubernetes_live.pod_template.valid.json` example were already allowlist-shaped (only `argocd.argoproj.io/tracking-id`) and needed no change. `go test ./internal/collector/kuberneteslive/... ./sdk/go/factschema/... ./sdk/go/collector/... ./internal/facts/... -count=1` all pass; `go build ./...` passes; `golangci-lint run ./internal/collector/kuberneteslive/...` and `./sdk/go/factschema/kuberneteslive/...` report no issues; `gofmt`/`gofumpt -l` and `git diff --check` are clean.

No-Observability-Change: No new metric instrument, metric label, span, structured log field, status field, queue domain, worker count, batch size, or runtime knob. This is a capture-boundary filter on an existing optional payload field (`annotations`); no reducer, graph write, or query handler reads this field's content differently, and the existing `input_invalid` dead-letter path is unaffected since a missing allowlisted key was already representable (absent/nil map) before this change.

### selector_match Service->Pod relationship edges (#5437)

No-Regression Evidence: additive relationship kind only — `RelationshipSelectorMatch` extends the existing closed `RelationshipType` enum; no existing `Client` method, fact contract, or `owner_reference`/`ingress_to_service` emission path changes. `collectServices` gained a `serviceSelectorIndex` alongside the pre-existing `serviceIndex` (a Service with an empty selector is excluded — Kubernetes never treats that as "match every Pod"); `collectWorkloads`/`addWorkload` now returns the workload identity so a `podLabelIndex` can be built for Pods only (gated on `entry.resourceScope == ResourceScopePods`, since a Service selector targets Pods, never Deployments/ReplicaSets/other workload kinds); a new `matchSelectors` pass runs after `collectWorkloads` and emits one `kubernetes_live.relationship` fact per matching (Service, Pod) pair in the same namespace via the existing `NewRelationshipEnvelope`, the same constructor `owner_reference`/`ingress_to_service` already use. `go test ./internal/collector/kuberneteslive/... -count=1 -race` passes (68 tests). Three new regression tests (`builder_selector_match_test.go`, driven through the real `Source.Next` path, same pattern as `TestSourceJobOwnedByCronJobEmitsOwnerEdge`) prove: (1) a Service selector that is a subset of a Pod's labels emits exactly one edge; (2) a selector with a key/value the Pod's labels do not carry emits no edge and no fallback warning; (3) one Service selector matching two Pods, and one Pod matching two Services, both fan out to the expected edge set (4 edges from 2 Services x up to 2 matching Pods each). A reducer-side test (`go/internal/reducer/kubernetes_correlation_selector_match_test.go`) proves the collector's real `factschema.EncodeKubernetesLiveRelationship` output (the exact encoder `NewRelationshipEnvelope` calls) decodes through the reducer's existing generic `ingestRelationship` and lands ambiguous/provenance-only/never-exact — the reducer's `relKubernetesSelectorMatch` classification branch (`kubernetes_correlation_classify.go`) was already correct for this issue and required no change. The B-7 golden-corpus gate unit tests pass (`go test ./cmd/golden-corpus-gate/ -count=1`, `bash scripts/test-verify-golden-corpus-gate.sh`); the cassette gained a second Pod plus two `selector_match` facts proving the one-selector-to-many-pods case (see `testdata/golden/e2e-20repo-snapshot.json`'s `node_counts.KubernetesWorkload` note); the full Docker `scripts/verify-golden-corpus-gate.sh` run is left to the orchestrator's heavier-gate pass per this session's scope.

Cold-review follow-up (4 findings fixed in a follow-on change): (1) the golden-corpus gate's `list_kubernetes_correlations`/`GET /api/v0/kubernetes/correlations` query shapes asserted only `minimum_results: 1`, already satisfied by the pre-existing `owner_reference` correlation, so a broken `selector_match` pass would pass undetected — fixed by pinning `required_json_values: {"correlations[].relationship_type": "selector_match"}` on both shapes (`testdata/golden/e2e-20repo-snapshot.json`), proven to gate by a differential: green with the cassette's two `selector_match` facts present, red when temporarily removed. (2) no test used a cross-namespace Service/Pod pair, so `matchSelectors`'s namespace guard had zero regression coverage — fixed with `TestSourceSelectorMatchNamespaceIsolation` (Service ns=`a`, Pod ns=`b`, labels satisfy the selector), proven RED by temporarily deleting the guard. (3) `selectorMatchesLabels`'s `len(selector)==0` branch was provably unreachable from `matchSelectors` (`collectServices` already excludes empty selectors) but untested in isolation — fixed with a direct unit test `TestSelectorMatchesLabelsEmptySelectorNeverMatches` (proven RED by deleting the guard) plus a builder-level `TestSourceHeadlessServiceEmitsNoSelectorMatchEdge` proving the exclusion holds end to end. (4) `matchSelectors` was an unbenchmarked full cross product of every Service against every Pod in the cluster, with the namespace check inside the inner loop (not reducing iteration) — fixed by bucketing `podLabelIndex` from `[]podLabelEntry` to `map[string][]podLabelEntry` keyed by namespace (built in `collectWorkloads`), so `matchSelectors` looks up only the matching namespace's Pods per Service; see Performance Evidence below.

Performance Evidence (`selector_match_bench_test.go`, generationBuilder driven directly so only the bucketed matching pass and its envelope construction are timed — fixture/index construction happens before `b.ResetTimer`, Apple M5 Max, `-benchmem -count=3`, default 1s benchtime): `BenchmarkMatchSelectorsSmallCluster` (5 namespaces x 20 Services x 200 Pods = 100 Services, 1,000 Pods) averages ~45.0ms/op, ~66.45MB/op, ~960,270 allocs/op across 3 runs. `BenchmarkMatchSelectorsLargeCluster` (50 namespaces x 20 Services x 200 Pods = 1,000 Services, 10,000 Pods — the requested realistic large-cluster shape) averages ~471.8ms/op, ~684.60MB/op, ~9,600,410 allocs/op. Namespace count grows 10x (5 -> 50) with per-namespace density held constant; ns/op grows ~10.5x, allocs/op grows exactly 10.0x, and bytes/op grows ~10.3x — confirming the bucketed pass is linear in namespace/cluster-size growth, not the quadratic-in-cluster-wide-Service/Pod-count behavior the pre-fix full cross product exhibited (a cluster with N Services and M Pods spread evenly across the same namespace count would have scaled the old pass by N x M regardless of namespace distribution). No new lock, transaction, or shared-state path: `matchSelectors` remains a single-pass read of two already-built, read-only indexes with no concurrent writer.

No-Observability-Change: No new metric instrument, metric label, span, structured log field, status field, queue domain, worker count, batch size, or runtime knob. `selector_match` reuses the existing `eshu_dp_kubernetes_facts_emitted_total{fact_kind}` metric (fact_kind=`kubernetes_live.relationship`, already emitted for `owner_reference`/`ingress_to_service`) and the existing `kubernetes_live.snapshot` span; there is no new resource-scope label or warning reason, since a selector mismatch is normal absence of an edge, not an error condition.

### Identity-edge correlation decisions carried an empty cluster_id (reducer follow-on, #5437)

No-Regression Evidence: behavior fix — `owner_reference` and `selector_match` correlation decisions were derived correctly (`relationship_type`, `outcome`, `drift_kind`) but written with `cluster_id=""`, so `GET /api/v0/kubernetes/correlations?cluster_id=...` and the MCP `list_kubernetes_correlations` (both filter on `payload->>'cluster_id' = $3`) silently dropped every identity-edge correlation; only image correlations (which get `cluster_id` from the pod template, not from `edgeClusterID`) survived the filter. Root cause: `go/internal/reducer/kubernetes_correlation_classify.go`'s `classifyIdentityEdge` set `ClusterID: edgeClusterID(edge)`, and `edgeClusterID` reconstructed the cluster id by string-parsing `edge.fromObjectID` for a `"k8s://<cluster>/..."` scheme. Real collector object ids (`kuberneteslive/identity.go` `ObjectID()`, an opaque sha256 via `facts.StableID`) and the cassette ids (`kubernetes_live:supply-chain-demo:...`) are never `k8s://`-prefixed, so the parse always returned `""`; only hand-authored unit-test fixtures in this package happen to use the `k8s://` scheme, which is why the existing reducer unit tests passed while the real pipeline never populated `cluster_id`. The `kubernetes_live.relationship` fact already carries the real cluster id (`kuberneteslivev1.Relationship.ClusterID *string`; every cassette relationship fact sets it), but `go/internal/reducer/kubernetes_correlation_index.go`'s `ingestRelationship` discarded it — the `kubernetesIdentityEdge` struct had no field to hold it. Fix: added `kubernetesIdentityEdge.clusterID`, populated by `ingestRelationship` via `derefString(relationship.ClusterID)` (the same helper `podTemplate.ClusterID` already uses), and `classifyIdentityEdge` now sets `ClusterID: edge.clusterID` directly; the now-dead `edgeClusterID` string-parse helper is removed (`rg 'edgeClusterID'` confirmed no other caller before removal). A new regression test, `TestBuildKubernetesCorrelationDecisionsIdentityEdgeUsesRelationshipClusterID` (`go/internal/reducer/kubernetes_correlation_cluster_id_test.go`), builds `owner_reference` and `selector_match` relationship facts with opaque (non-`k8s://`) object ids carrying a real `cluster_id` and asserts the decision's `ClusterID` matches — proven RED against the old `edgeClusterID(edge)` derivation (both subtests failed with `ClusterID = ""`) before the fix, GREEN after. `go test ./internal/reducer/... ./internal/collector/kuberneteslive/... ./internal/query/... -count=1 -race` passes (4,435 tests); the existing `k8sRelationshipFact` test helper in `kubernetes_correlation_helpers_test.go` already sets `cluster_id` on every relationship fixture (equal to the `k8s://`-embedded cluster id), so no existing identity-edge test needed fixture changes. `go build ./...` and `golangci-lint run ./internal/reducer/... ./internal/query/...` are clean.

No-Observability-Change: No new metric instrument, metric label, span, structured log field, status field, queue domain, worker count, batch size, or runtime knob. This is a data-correctness fix to an existing decision field (`cluster_id`) already carried on every `KubernetesCorrelationDecision`; no new signal is added, and no existing signal's cardinality or meaning changes.
