{
  "odu": "odu:ifa-symbol-runtime-family",
  "note": "Hand-derived from reducer.ExtractSymbolRuntimeIntentRows's real extraction path over symbol_runtime_family_odu.go's fixture, filtered to the handles_route domain's upsert rows. Two intent-level dedupe facts collapse here to ONE edge: HandleWidgets serves exactly one route entry (GET /widgets) in this fixture, so no method collapse is exercised by this live fixture at all -- the GET+POST same-path collapse (two intent rows, one shared partition key, ONE resulting graph edge because the MERGE identity is (Function, HANDLES_ROUTE, Endpoint) with no method dimension) is proven deterministically by a synthetic offline unit test instead (materialized_edges_handles_route_test.go), never by asserting http_method here. source_entity_id is HandleWidgets' canonical Function uid (content.CanonicalEntityID). target_entity_id is the :Endpoint node's true MERGE-key id, stableAPIEndpointID(repo_id, workload_id, path) -- NOT the (repo_id, path) property pair the write-time MATCH uses -- derived here via the SAME reducer.ExtractWorkloadCandidates -> reducer.BuildProjectionRows seam the live workload-materialization handler runs, independently reproduced (not hand-typed) by TestSymbolRuntimeFamilyEndpointIDLiteral.",
  "edges": [
    {
      "relationship_type": "HANDLES_ROUTE",
      "source_entity_id": "content-entity:e_b802e874e4a3",
      "target_entity_id": "endpoint:f8c7b9f9a7ebdd90ea596f67"
    }
  ]
}
