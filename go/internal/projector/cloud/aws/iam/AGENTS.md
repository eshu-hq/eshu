# AGENTS.md — AWS IAM projector namespace

Read `README.md`, `doc.go`, and `../AGENTS.md` before changing this tree.
Keep this directory documentation-only. The `trust` and `instance/profile`
leaves select AWS IAM trigger facts; root projector owns fan-out and reducer
handlers own canonical edge materialization.
