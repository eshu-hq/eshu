# AGENTS.md — AWS IAM instance projector namespace

Read `README.md`, `doc.go`, `../AGENTS.md`, and the `profile` leaf instructions
before changing this tree. Keep the namespace documentation-only; the profile
leaf selects a trigger and the reducer owns HAS_ROLE retraction and writes.
