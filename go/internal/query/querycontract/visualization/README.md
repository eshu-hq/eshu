# querycontract/visualization

Visualization-packet contract shared by the query handler families.

## Dependencies

Outbound: the parent `querycontract` for `TruthEnvelope` and the `TruthLevel*`
constants, plus `querycontract/evidence` for citation handles. The parent must
not import this package.

## Telemetry

None.
