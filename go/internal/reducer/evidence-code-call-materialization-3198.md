# Code-Call Materialization Evidence: Python Import-Bound Fallback Precision

No-Regression Evidence: `go test ./internal/reducer -run TestCallResolutionGoldensPython -count=1 -v` failed before the fix because the Python #3156 corpus still resolved `from third_party.db import connect; connect()` to an unrelated local `connect` via `repo_unique_name`, and still labeled `from pkg import handler; handler()` as `repo_unique_name` instead of the explicit `import_binding` tier. After the fix, `go test ./internal/reducer -run 'TestCallResolutionGoldensPython|TestExtractCodeCallRows(PrefersPythonAliasedImportBeforeRepoFallback|ResolvesPythonModuleAndFromImports|BlocksTypeScriptDirectImportFallbackToRepoUnique)' -count=1 -v`, `go test ./internal/reducer ./internal/resolutionparity ./cmd/reducer -count=1`, and `go test ./internal/resolutionparity -count=1` passed. Baseline and after measurement both use the same in-memory reducer extraction path with no graph backend, database write, queue, or runtime service involved; the input shape is the bounded source-backed Python call-resolution corpus plus focused reducer rows, and terminal row counts stay bounded by existing call-row extraction. The checked-in tier golden moved only the Python source corpus from weak `repo_unique_name` rows to `import_binding`, which is the intended accuracy improvement.

No-Observability-Change: Python import-bound fallback precision only changes the in-memory resolver branch over existing parser import rows, repository prescan import maps, and the code entity index before code-call rows are emitted. It adds no graph query, graph write shape, queue table, worker, lease, batch setting, runtime knob, metric instrument, metric label, span, route, status field, or log key. Operators still diagnose code-call extraction through the existing `code call materialization completed` log fields and reducer execution spans/counters.
