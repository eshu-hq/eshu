# Code Interproc Why Trail Evidence (#3194)

No-Regression Evidence: `go test ./internal/parser/interproc -run 'TestFindingTrail' -count=1`, `go test ./internal/parser/dataflowemit -run TestInterprocFindingRowCarriesWhyTrail -count=1`, `go test ./internal/collector -run 'TestBuildInterprocTaintEvidencePreservesWhyTrail|TestInterprocEvidenceFactEnvelope' -count=1`, `go test ./internal/reducer -run 'TestExtractCodeInterprocEvidenceRows|TestValueFlowFixpointEvidenceLoaderProjectsDurableInputs' -count=1`, `go test ./internal/storage/postgres -run TestCodeInterprocEvidenceFromEnvelope -count=1`, `go test ./internal/storage/cypher -run TestCodeInterprocEvidenceWriterMatchesFunctionsAndMergesEdge -count=1`, `go test ./internal/query -run 'TestHandleRelationshipStorySurfacesValueFlowWhyTrail|TestOpenAPIRelationshipSchemaDocumentsProvenanceBlock' -count=1`, and `go test ./internal/mcp -run 'TestCodeRelationshipValueFlow' -count=1` prove the bounded ordered value-flow trail is produced by the solver with sink-kind-specific unsuppressed witnesses, preserved through parser, collector, and fixpoint payloads, projected without changing the `TAINT_FLOWS_TO` evidence UID or sending unused nested graph-batch payloads, persisted as JSON text plus a primitive truncation flag on the edge, and surfaced through API/MCP provenance and MCP schemas with the answer-level truth envelope unchanged.

No-Observability-Change: this change adds no worker, queue domain, metric name, metric label, runtime knob, backend branch, retry loop, or new graph-write route. The write still flows through the existing code_interproc_evidence reducer materialization, Postgres fact loader, `CodeInterprocEvidenceWriter`, statement metadata, executor/group-executor dispatch, retry wrapping, and existing failure logging. The only graph payload additions are `rel.why_trail_json` and `rel.why_trail_truncated` on reducer-owned `TAINT_FLOWS_TO` evidence edges.
