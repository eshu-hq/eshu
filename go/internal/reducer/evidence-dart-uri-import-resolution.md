# Dart URI import resolution

No-Regression Evidence: `go test ./internal/reducer -run 'TestCodeCall.*Dart|Dart.*Import' -count=1 -v` failed before Dart URI imports could bind a bare call to the imported file and before unresolved imported calls blocked weak repository fallback, then passed after the Dart resolver used parser import rows plus the prescan `imports_map` to resolve one imported target as `import_binding`. The focused gate also proves uppercase class constructor calls do not fall through to a repo-unique decoy when their lowercase Dart import file cannot be resolved, export-only Dart directives do not resolve as lexical imports, package imports resolve only under the caller package root, and external package imports do not bind to coincidentally named local files. `go test ./internal/parser/dart -run TestParseCapturesDartBuckets -count=1 -v` failed before Dart parser import rows identified `library_import` versus `library_export`, then passed after rows carried `import_type=import` or `import_type=export`. `go test ./internal/resolutionparity -run TestGoldenCallGraphCorrectnessHarness/dart_import_binding -count=1 -v` proves the source-backed parser plus reducer path emits the exact imported class-constructor edge while a same-name decoy file stays unselected.

Performance Evidence: Baseline behavior for cross-file Dart calls was either no row or weak `repo_unique_name` fallback over the existing code entity index; after this change the resolver adds one bounded in-memory pass over the caller file's existing Dart directive rows and the prescan `imports_map` candidate paths for the call name. Backend/version: local Go reducer unit path on the current repository branch, no graph backend involved. Input shape: three file facts, one repository import map, one caller import/export row, one call row, and one decoy callee. Terminal row count stays one resolved CALLS row for the positive import case and zero rows for unresolved/export/external-package decoy cases. The broad focused package gate `go test ./internal/parser/dart ./internal/reducer ./internal/resolutionparity -count=1` stayed green, showing no measurable test-level regression for the touched reducer and parser correctness gates.

No-Observability-Change: Dart URI import resolution is an in-memory resolver branch over existing parser import rows, repository prescan import maps, and the existing code entity index. It adds no graph query, graph write shape, queue table, worker, lease, batch setting, runtime knob, metric instrument, metric label, span, route, or log key. Operators still diagnose code-call extraction through the existing `code call materialization completed` log fields and reducer execution spans/counters.
