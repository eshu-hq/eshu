# Groovy Class Resolution Evidence

No-Regression Evidence: `go test ./internal/parser/groovy -run TestParseWithParserAddsGroovyClassQualifiedCallMetadata -count=1 -v` failed before Groovy tree-sitter call extraction carried `full_name`, `inferred_obj_type`, and `lang` for class-qualified calls such as `DeployHelper.deployApp`, then passed after the parser preserved that receiver evidence. `go test ./internal/parser/groovy -run TestParseWithParserKeepsGroovyLowercaseReceiverCallsUnqualified -count=1 -v` failed before lowercase instance receiver calls such as `service.deployApp()` stayed unqualified, then passed after the parser limited `full_name` metadata to class receiver evidence. `go test ./internal/reducer -run TestCodeCallResolutionMethodGroovyClassQualifiedCall -count=1 -v` failed before the reducer classified the target as `repo_unique_name`, then passed after the Groovy resolver selected the exact `DeployHelper.deployApp` method with `type_inferred` provenance while a same-name `OtherHelper.deployApp` decoy stayed unselected. `go test ./internal/reducer -run TestCodeCallGroovyMissingClassQualifiedReceiverBlocksRepoUniqueFallback -count=1 -v` proves a missing class-qualified receiver does not fall through to a wrong repository-wide method. `go test ./internal/resolutionparity -run TestGoldenCallGraphCorrectnessHarness/groovy_class_qualified -count=1 -v` proves the source-backed parser plus reducer path emits the exact caller-to-callee edge and keeps the same-name decoy unselected.

No-Regression Evidence: macOS CI run `27824436338` failed `go/internal/storage/cypher` with `TestCanonicalNodeWriterRetractCoversProjectableEntityLabels` after `ShellCommand` became a schema-backed projectable label. `go test ./internal/storage/cypher -run TestCanonicalNodeWriterRetractCoversProjectableEntityLabels -count=1 -v` reproduced that failure locally before `ShellCommand` joined the code-entity retract family, then passed after stale entity cleanup covered the label.

No-Observability-Change: Groovy class-qualified resolution is an in-memory parser metadata and resolver change over existing `function_calls`, function/class context rows, and the existing code entity index. `ShellCommand` cleanup now uses the existing label-scoped `entity_retract` canonical phase, statement metadata, phase metrics, retry wrapping, and phase failure logs. The change adds no metric name, metric label, worker, queue domain, runtime knob, backend branch, route, status field, or log key.
