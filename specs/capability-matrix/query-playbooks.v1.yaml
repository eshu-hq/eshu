capabilities:
  - capability: query.playbooks
    tools: [list_query_playbooks, resolve_query_playbook]
    profiles:
      local_lightweight:
        max_truth_level: exact
      local_authoritative:
        max_truth_level: exact
      local_full_stack:
        max_truth_level: exact
      production:
        max_truth_level: exact
