apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  generateName: ephemeral-preview-
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/acme/ephemeral-preview
  ref:
    branch: main
