apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ambiguous-consumer
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: shared
      sourceRef:
        kind: HelmRepository
        name: shared-repo
        namespace: flux-system
